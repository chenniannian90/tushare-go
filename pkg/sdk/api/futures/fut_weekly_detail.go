// Code generated by tushare-gen. DO NOT EDIT.

package futures

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// FutWeeklyDetailRequest 表示 期货主要品种交易周报 API 的请求
type FutWeeklyDetailRequest struct {
	Week string `json:"week,omitempty"` // 周期（每年第几周，e.g. 202001 表示2020第1周）
	Prd string `json:"prd,omitempty"` // 期货品种（支持多品种输入，逗号分隔）
	StartWeek string `json:"start_week,omitempty"` // 开始周期
	EndWeek string `json:"end_week,omitempty"` // 结束周期
	Exchange string `json:"exchange,omitempty"` // 交易所（请参考交易所说明）
	Fields string `json:"fields,omitempty"` // 提取的字段，e.g. fields='prd,name,vol'
}

// FutWeeklyDetailItem 表示单个 期货主要品种交易周报 数据项
type FutWeeklyDetailItem struct {
	Exchange string `json:"exchange"` // 交易所代码
	Prd string `json:"prd"` // 期货品种代码
	Name string `json:"name"` // 品种名称
	Vol int `json:"vol"` // 成交量（手）
	VolYoy float64 `json:"vol_yoy"` // 同比增减（%）
	Amount float64 `json:"amount"` // 成交金额（亿元）
	AmoutYoy float64 `json:"amout_yoy"` // 同比增减（%）
	Cumvol int `json:"cumvol"` // 年累计成交总量（手）
	CumvolYoy float64 `json:"cumvol_yoy"` // 同比增减（%）
	Cumamt float64 `json:"cumamt"` // 年累计成交金额（亿元）
	CumamtYoy float64 `json:"cumamt_yoy"` // 同比增减（%）
	OpenInterest int `json:"open_interest"` // 持仓量（手）
	InterestWow float64 `json:"interest_wow"` // 环比增减（%）
	McClose float64 `json:"mc_close"` // 本周主力合约收盘价
	CloseWow float64 `json:"close_wow"` // 环比涨跌（%）
	Week string `json:"week"` // 周期
	WeekDate string `json:"week_date"` // 周日期
}

// FutWeeklyDetail 调用 期货主要品种交易周报 API
// 获取期货交易所主要品种每周交易统计信息，数据从2010年3月开始
// 注意：积分要求等使用限制请参考Tushare官方文档
func FutWeeklyDetail(ctx context.Context, client *sdk.Client, req *FutWeeklyDetailRequest) ([]FutWeeklyDetailItem, error) {
	params := map[string]interface{}{}
	if req.Week != "" {
		params["week"] = req.Week
	}
	if req.Prd != "" {
		params["prd"] = req.Prd
	}
	if req.StartWeek != "" {
		params["start_week"] = req.StartWeek
	}
	if req.EndWeek != "" {
		params["end_week"] = req.EndWeek
	}
	if req.Exchange != "" {
		params["exchange"] = req.Exchange
	}
	if req.Fields != "" {
		params["fields"] = req.Fields
	}

	fields := []string{"exchange", "prd", "name", "vol", "vol_yoy", "amount", "amout_yoy", "cumvol", "cumvol_yoy", "cumamt", "cumamt_yoy", "open_interest", "interest_wow", "mc_close", "close_wow", "week", "week_date"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "fut_weekly_detail", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]FutWeeklyDetailItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 exchange 的简单类型
		// 对 string 类型尝试多种转换
		var exchange string
		if v, ok := item["exchange"].(string); ok {
			exchange = v
		} else if v, ok := item["exchange"].(float64); ok {
			exchange = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["exchange"].(int); ok {
			exchange = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 exchange 类型")
		}
		// 处理 prd 的简单类型
		// 对 string 类型尝试多种转换
		var prd string
		if v, ok := item["prd"].(string); ok {
			prd = v
		} else if v, ok := item["prd"].(float64); ok {
			prd = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["prd"].(int); ok {
			prd = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 prd 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 vol 的简单类型
		vol, ok := item["vol"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 vol 类型")
		}
		// 处理 vol_yoy 的简单类型
		volYoy, ok := item["vol_yoy"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 vol_yoy 类型")
		}
		// 处理 amount 的简单类型
		amount, ok := item["amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amount 类型")
		}
		// 处理 amout_yoy 的简单类型
		amoutYoy, ok := item["amout_yoy"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amout_yoy 类型")
		}
		// 处理 cumvol 的简单类型
		cumvol, ok := item["cumvol"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 cumvol 类型")
		}
		// 处理 cumvol_yoy 的简单类型
		cumvolYoy, ok := item["cumvol_yoy"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 cumvol_yoy 类型")
		}
		// 处理 cumamt 的简单类型
		cumamt, ok := item["cumamt"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 cumamt 类型")
		}
		// 处理 cumamt_yoy 的简单类型
		cumamtYoy, ok := item["cumamt_yoy"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 cumamt_yoy 类型")
		}
		// 处理 open_interest 的简单类型
		openInterest, ok := item["open_interest"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 open_interest 类型")
		}
		// 处理 interest_wow 的简单类型
		interestWow, ok := item["interest_wow"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 interest_wow 类型")
		}
		// 处理 mc_close 的简单类型
		mcClose, ok := item["mc_close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 mc_close 类型")
		}
		// 处理 close_wow 的简单类型
		closeWow, ok := item["close_wow"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close_wow 类型")
		}
		// 处理 week 的简单类型
		// 对 string 类型尝试多种转换
		var week string
		if v, ok := item["week"].(string); ok {
			week = v
		} else if v, ok := item["week"].(float64); ok {
			week = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["week"].(int); ok {
			week = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 week 类型")
		}
		// 处理 week_date 的简单类型
		// 对 string 类型尝试多种转换
		var weekDate string
		if v, ok := item["week_date"].(string); ok {
			weekDate = v
		} else if v, ok := item["week_date"].(float64); ok {
			weekDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["week_date"].(int); ok {
			weekDate = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 week_date 类型")
		}
		items[i] = FutWeeklyDetailItem{
			Exchange: exchange,
			Prd: prd,
			Name: name,
			Vol: vol,
			VolYoy: volYoy,
			Amount: amount,
			AmoutYoy: amoutYoy,
			Cumvol: cumvol,
			CumvolYoy: cumvolYoy,
			Cumamt: cumamt,
			CumamtYoy: cumamtYoy,
			OpenInterest: openInterest,
			InterestWow: interestWow,
			McClose: mcClose,
			CloseWow: closeWow,
			Week: week,
			WeekDate: weekDate,
		}
	}

	return items, nil
}
