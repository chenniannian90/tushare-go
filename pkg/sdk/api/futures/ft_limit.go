// Code generated by tushare-gen. DO NOT EDIT.

package futures

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// FtLimitRequest 表示 期货合约涨跌停价格 API 的请求
type FtLimitRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 合约代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（格式：YYYYMMDD）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
	Cont string `json:"cont,omitempty"` // 合约代码（例如：cont='CU')
	Exchange string `json:"exchange,omitempty"` // 交易所代码 （例如：exchange='DCE')
}

// FtLimitItem 表示单个 期货合约涨跌停价格 数据项
type FtLimitItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	TsCode string `json:"ts_code"` // TS股票代码
	Name string `json:"name"` // 合约名称
	UpLimit float64 `json:"up_limit"` // 涨停价
	DownLimit float64 `json:"down_limit"` // 跌停价
	MRatio float64 `json:"m_ratio"` // 最低交易保证金率（%）
	Cont string `json:"cont"` // 合约代码
	Exchange string `json:"exchange"` // 交易所代码
}

// FtLimit 调用 期货合约涨跌停价格 API
// 获取所有期货合约每天的涨跌停价格及最低保证金率，数据开始于2005年。
// 注意：积分要求等使用限制请参考Tushare官方文档
func FtLimit(ctx context.Context, client *sdk.Client, req *FtLimitRequest) ([]FtLimitItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Cont != "" {
		params["cont"] = req.Cont
	}
	if req.Exchange != "" {
		params["exchange"] = req.Exchange
	}

	fields := []string{"trade_date", "ts_code", "name", "up_limit", "down_limit", "m_ratio", "cont", "exchange"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "ft_limit", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]FtLimitItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ft_limit")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ft_limit")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if item["name"] == nil {
			// 字段值为 null，使用零值
			name = ""
		} else if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ft_limit")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 up_limit 的简单类型
		upLimit, ok := item["up_limit"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 up_limit 类型")
		}
		// 处理 down_limit 的简单类型
		downLimit, ok := item["down_limit"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 down_limit 类型")
		}
		// 处理 m_ratio 的简单类型
		mRatio, ok := item["m_ratio"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 m_ratio 类型")
		}
		// 处理 cont 的简单类型
		// 对 string 类型尝试多种转换
		var cont string
		if item["cont"] == nil {
			// 字段值为 null，使用零值
			cont = ""
		} else if v, ok := item["cont"].(string); ok {
			cont = v
		} else if v, ok := item["cont"].(float64); ok {
			cont = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["cont"].(int); ok {
			cont = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["cont"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ft_limit")
			log.Printf("字段: cont")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["cont"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 cont 类型")
		}
		// 处理 exchange 的简单类型
		// 对 string 类型尝试多种转换
		var exchange string
		if item["exchange"] == nil {
			// 字段值为 null，使用零值
			exchange = ""
		} else if v, ok := item["exchange"].(string); ok {
			exchange = v
		} else if v, ok := item["exchange"].(float64); ok {
			exchange = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["exchange"].(int); ok {
			exchange = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["exchange"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ft_limit")
			log.Printf("字段: exchange")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["exchange"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 exchange 类型")
		}
		items[i] = FtLimitItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			Name: name,
			UpLimit: upLimit,
			DownLimit: downLimit,
			MRatio: mRatio,
			Cont: cont,
			Exchange: exchange,
		}
	}

	return items, nil
}
