// Code generated by tushare-gen. DO NOT EDIT.

package futures

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// FutSettleRequest 表示 每日结算参数 API 的请求
type FutSettleRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期 （trade_date/ts_code至少需要输入一个参数）
	TsCode string `json:"ts_code,omitempty"` // 合约代码
	StartDate string `json:"start_date,omitempty"` // 开始日期(YYYYMMDD格式，下同)
	EndDate string `json:"end_date,omitempty"` // 结束日期
	Exchange string `json:"exchange,omitempty"` // 交易所代码
}

// FutSettleItem 表示单个 每日结算参数 数据项
type FutSettleItem struct {
	TsCode string `json:"ts_code"` // 合约代码
	TradeDate string `json:"trade_date"` // 交易日期
	Settle float64 `json:"settle"` // 结算价
	TradingFeeRate float64 `json:"trading_fee_rate"` // 交易手续费率
	TradingFee float64 `json:"trading_fee"` // 交易手续费
	DeliveryFee float64 `json:"delivery_fee"` // 交割手续费
	BHedgingMarginRate float64 `json:"b_hedging_margin_rate"` // 买套保交易保证金率
	SHedgingMarginRate float64 `json:"s_hedging_margin_rate"` // 卖套保交易保证金率
	LongMarginRate float64 `json:"long_margin_rate"` // 买投机交易保证金率
	ShortMarginRate float64 `json:"short_margin_rate"` // 卖投机交易保证金率
	OffsetTodayFee float64 `json:"offset_today_fee"` // 平今仓手续率
	Exchange string `json:"exchange"` // 交易所
}

// FutSettle 调用 每日结算参数 API
// 获取每日结算参数数据，包括交易和交割费率等
// 注意：积分要求等使用限制请参考Tushare官方文档
func FutSettle(ctx context.Context, client *sdk.Client, req *FutSettleRequest) ([]FutSettleItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Exchange != "" {
		params["exchange"] = req.Exchange
	}

	fields := []string{"ts_code", "trade_date", "settle", "trading_fee_rate", "trading_fee", "delivery_fee", "b_hedging_margin_rate", "s_hedging_margin_rate", "long_margin_rate", "short_margin_rate", "offset_today_fee", "exchange"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "fut_settle", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]FutSettleItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 settle 的简单类型
		settle, ok := item["settle"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 settle 类型")
		}
		// 处理 trading_fee_rate 的简单类型
		tradingFeeRate, ok := item["trading_fee_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 trading_fee_rate 类型")
		}
		// 处理 trading_fee 的简单类型
		tradingFee, ok := item["trading_fee"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 trading_fee 类型")
		}
		// 处理 delivery_fee 的简单类型
		deliveryFee, ok := item["delivery_fee"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 delivery_fee 类型")
		}
		// 处理 b_hedging_margin_rate 的简单类型
		bHedgingMarginRate, ok := item["b_hedging_margin_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 b_hedging_margin_rate 类型")
		}
		// 处理 s_hedging_margin_rate 的简单类型
		sHedgingMarginRate, ok := item["s_hedging_margin_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 s_hedging_margin_rate 类型")
		}
		// 处理 long_margin_rate 的简单类型
		longMarginRate, ok := item["long_margin_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 long_margin_rate 类型")
		}
		// 处理 short_margin_rate 的简单类型
		shortMarginRate, ok := item["short_margin_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 short_margin_rate 类型")
		}
		// 处理 offset_today_fee 的简单类型
		offsetTodayFee, ok := item["offset_today_fee"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 offset_today_fee 类型")
		}
		// 处理 exchange 的简单类型
		// 对 string 类型尝试多种转换
		var exchange string
		if v, ok := item["exchange"].(string); ok {
			exchange = v
		} else if v, ok := item["exchange"].(float64); ok {
			exchange = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["exchange"].(int); ok {
			exchange = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 exchange 类型")
		}
		items[i] = FutSettleItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			Settle: settle,
			TradingFeeRate: tradingFeeRate,
			TradingFee: tradingFee,
			DeliveryFee: deliveryFee,
			BHedgingMarginRate: bHedgingMarginRate,
			SHedgingMarginRate: sHedgingMarginRate,
			LongMarginRate: longMarginRate,
			ShortMarginRate: shortMarginRate,
			OffsetTodayFee: offsetTodayFee,
			Exchange: exchange,
		}
	}

	return items, nil
}
