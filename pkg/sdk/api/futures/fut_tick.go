// Code generated by tushare-gen. DO NOT EDIT.

package futures

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// FutTickRequest 表示 历史Tick行情 API 的请求
type FutTickRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 合约代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（YYYYMMDD格式）
	StartTime string `json:"start_time,omitempty"` // 开始时间（格式：HH:MM:SS）
	EndTime string `json:"end_time,omitempty"` // 结束时间（格式：HH:MM:SS）
}

// FutTickItem 表示单个 历史Tick行情 数据项
type FutTickItem struct {
	TradeTime string `json:"trade_time"` // 成交时间
	TsCode string `json:"ts_code"` // 合约代码
	LastPrice float64 `json:"last_price"` // 最新价
	BidPrice1 float64 `json:"bid_price1"` // 买一价
	BidVolume1 float64 `json:"bid_volume1"` // 买一量
	AskPrice1 float64 `json:"ask_price1"` // 卖一价
	AskVolume1 float64 `json:"ask_volume1"` // 卖一量
	OpenInterest float64 `json:"open_interest"` // 持仓量
	Volume float64 `json:"volume"` // 成交量
	Open float64 `json:"open"` // 开盘价
	High float64 `json:"high"` // 最高价
	Low float64 `json:"low"` // 最低价
}

// FutTick 调用 历史Tick行情 API
func FutTick(ctx context.Context, client *sdk.Client, req *FutTickRequest) ([]FutTickItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartTime != "" {
		params["start_time"] = req.StartTime
	}
	if req.EndTime != "" {
		params["end_time"] = req.EndTime
	}

	fields := []string{"trade_time", "ts_code", "last_price", "bid_price1", "bid_volume1", "ask_price1", "ask_volume1", "open_interest", "volume", "open", "high", "low"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "fut_tick", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]FutTickItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_time 的简单类型
		tradeTime, ok := item["trade_time"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_time 类型")
		}
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 last_price 的简单类型
		lastPrice, ok := item["last_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 last_price 类型")
		}
		// 处理 bid_price1 的简单类型
		bidPrice1, ok := item["bid_price1"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bid_price1 类型")
		}
		// 处理 bid_volume1 的简单类型
		bidVolume1, ok := item["bid_volume1"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bid_volume1 类型")
		}
		// 处理 ask_price1 的简单类型
		askPrice1, ok := item["ask_price1"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ask_price1 类型")
		}
		// 处理 ask_volume1 的简单类型
		askVolume1, ok := item["ask_volume1"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ask_volume1 类型")
		}
		// 处理 open_interest 的简单类型
		openInterest, ok := item["open_interest"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 open_interest 类型")
		}
		// 处理 volume 的简单类型
		volume, ok := item["volume"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 volume 类型")
		}
		// 处理 open 的简单类型
		open, ok := item["open"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 open 类型")
		}
		// 处理 high 的简单类型
		high, ok := item["high"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 high 类型")
		}
		// 处理 low 的简单类型
		low, ok := item["low"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 low 类型")
		}
		items[i] = FutTickItem{
			TradeTime: tradeTime,
			TsCode: tsCode,
			LastPrice: lastPrice,
			BidPrice1: bidPrice1,
			BidVolume1: bidVolume1,
			AskPrice1: askPrice1,
			AskVolume1: askVolume1,
			OpenInterest: openInterest,
			Volume: volume,
			Open: open,
			High: high,
			Low: low,
		}
	}

	return items, nil
}
