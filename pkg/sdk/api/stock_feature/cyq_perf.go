// Code generated by tushare-gen. DO NOT EDIT.

package stock_feature

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// CyqPerfRequest 表示 每日筹码及胜率 API 的请求
type CyqPerfRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（YYYYMMDD）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// CyqPerfItem 表示单个 每日筹码及胜率 数据项
type CyqPerfItem struct {
	TsCode string `json:"ts_code"` // 股票代码
	TradeDate string `json:"trade_date"` // 交易日期
	HisLow float64 `json:"his_low"` // 历史最低价
	HisHigh float64 `json:"his_high"` // 历史最高价
	Cost5pct float64 `json:"cost_5pct"` // 5分位成本
	Cost15pct float64 `json:"cost_15pct"` // 15分位成本
	Cost50pct float64 `json:"cost_50pct"` // 50分位成本
	Cost85pct float64 `json:"cost_85pct"` // 85分位成本
	Cost95pct float64 `json:"cost_95pct"` // 95分位成本
	WeightAvg float64 `json:"weight_avg"` // 加权平均成本
	WinnerRate float64 `json:"winner_rate"` // 胜率
}

// CyqPerf 调用 每日筹码及胜率 API
// 获取A股每日筹码平均成本和胜率情况，每天18~19点左右更新，数据从2018年开始 来源：Tushare社区
// 注意：积分要求等使用限制请参考Tushare官方文档
func CyqPerf(ctx context.Context, client *sdk.Client, req *CyqPerfRequest) ([]CyqPerfItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "his_low", "his_high", "cost_5pct", "cost_15pct", "cost_50pct", "cost_85pct", "cost_95pct", "weight_avg", "winner_rate"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "cyq_perf", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]CyqPerfItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 his_low 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var hisLow float64
		if item["his_low"] == nil {
			// 字段值为 null，使用零值
			hisLow = 0
		} else if v, ok := item["his_low"].(float64); ok {
			hisLow = v
		} else if v, ok := item["his_low"].(int); ok {
			hisLow = float64(v)
		} else if v, ok := item["his_low"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				hisLow = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					hisLow = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["his_low"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: his_low")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["his_low"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 his_low 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["his_low"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: his_low")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["his_low"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 his_low 类型，期望 float64/int/string")
		}
		// 处理 his_high 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var hisHigh float64
		if item["his_high"] == nil {
			// 字段值为 null，使用零值
			hisHigh = 0
		} else if v, ok := item["his_high"].(float64); ok {
			hisHigh = v
		} else if v, ok := item["his_high"].(int); ok {
			hisHigh = float64(v)
		} else if v, ok := item["his_high"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				hisHigh = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					hisHigh = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["his_high"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: his_high")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["his_high"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 his_high 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["his_high"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: his_high")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["his_high"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 his_high 类型，期望 float64/int/string")
		}
		// 处理 cost_5pct 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var cost5pct float64
		if item["cost_5pct"] == nil {
			// 字段值为 null，使用零值
			cost5pct = 0
		} else if v, ok := item["cost_5pct"].(float64); ok {
			cost5pct = v
		} else if v, ok := item["cost_5pct"].(int); ok {
			cost5pct = float64(v)
		} else if v, ok := item["cost_5pct"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				cost5pct = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					cost5pct = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["cost_5pct"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: cost_5pct")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["cost_5pct"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 cost_5pct 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["cost_5pct"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: cost_5pct")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["cost_5pct"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 cost_5pct 类型，期望 float64/int/string")
		}
		// 处理 cost_15pct 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var cost15pct float64
		if item["cost_15pct"] == nil {
			// 字段值为 null，使用零值
			cost15pct = 0
		} else if v, ok := item["cost_15pct"].(float64); ok {
			cost15pct = v
		} else if v, ok := item["cost_15pct"].(int); ok {
			cost15pct = float64(v)
		} else if v, ok := item["cost_15pct"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				cost15pct = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					cost15pct = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["cost_15pct"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: cost_15pct")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["cost_15pct"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 cost_15pct 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["cost_15pct"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: cost_15pct")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["cost_15pct"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 cost_15pct 类型，期望 float64/int/string")
		}
		// 处理 cost_50pct 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var cost50pct float64
		if item["cost_50pct"] == nil {
			// 字段值为 null，使用零值
			cost50pct = 0
		} else if v, ok := item["cost_50pct"].(float64); ok {
			cost50pct = v
		} else if v, ok := item["cost_50pct"].(int); ok {
			cost50pct = float64(v)
		} else if v, ok := item["cost_50pct"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				cost50pct = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					cost50pct = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["cost_50pct"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: cost_50pct")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["cost_50pct"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 cost_50pct 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["cost_50pct"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: cost_50pct")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["cost_50pct"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 cost_50pct 类型，期望 float64/int/string")
		}
		// 处理 cost_85pct 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var cost85pct float64
		if item["cost_85pct"] == nil {
			// 字段值为 null，使用零值
			cost85pct = 0
		} else if v, ok := item["cost_85pct"].(float64); ok {
			cost85pct = v
		} else if v, ok := item["cost_85pct"].(int); ok {
			cost85pct = float64(v)
		} else if v, ok := item["cost_85pct"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				cost85pct = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					cost85pct = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["cost_85pct"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: cost_85pct")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["cost_85pct"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 cost_85pct 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["cost_85pct"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: cost_85pct")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["cost_85pct"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 cost_85pct 类型，期望 float64/int/string")
		}
		// 处理 cost_95pct 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var cost95pct float64
		if item["cost_95pct"] == nil {
			// 字段值为 null，使用零值
			cost95pct = 0
		} else if v, ok := item["cost_95pct"].(float64); ok {
			cost95pct = v
		} else if v, ok := item["cost_95pct"].(int); ok {
			cost95pct = float64(v)
		} else if v, ok := item["cost_95pct"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				cost95pct = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					cost95pct = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["cost_95pct"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: cost_95pct")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["cost_95pct"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 cost_95pct 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["cost_95pct"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: cost_95pct")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["cost_95pct"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 cost_95pct 类型，期望 float64/int/string")
		}
		// 处理 weight_avg 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var weightAvg float64
		if item["weight_avg"] == nil {
			// 字段值为 null，使用零值
			weightAvg = 0
		} else if v, ok := item["weight_avg"].(float64); ok {
			weightAvg = v
		} else if v, ok := item["weight_avg"].(int); ok {
			weightAvg = float64(v)
		} else if v, ok := item["weight_avg"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				weightAvg = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					weightAvg = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["weight_avg"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: weight_avg")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["weight_avg"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 weight_avg 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["weight_avg"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: weight_avg")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["weight_avg"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 weight_avg 类型，期望 float64/int/string")
		}
		// 处理 winner_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var winnerRate float64
		if item["winner_rate"] == nil {
			// 字段值为 null，使用零值
			winnerRate = 0
		} else if v, ok := item["winner_rate"].(float64); ok {
			winnerRate = v
		} else if v, ok := item["winner_rate"].(int); ok {
			winnerRate = float64(v)
		} else if v, ok := item["winner_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				winnerRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					winnerRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["winner_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cyq_perf")
					log.Printf("字段: winner_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["winner_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 winner_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["winner_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cyq_perf")
			log.Printf("字段: winner_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["winner_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 winner_rate 类型，期望 float64/int/string")
		}
		items[i] = CyqPerfItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			HisLow: hisLow,
			HisHigh: hisHigh,
			Cost5pct: cost5pct,
			Cost15pct: cost15pct,
			Cost50pct: cost50pct,
			Cost85pct: cost85pct,
			Cost95pct: cost95pct,
			WeightAvg: weightAvg,
			WinnerRate: winnerRate,
		}
	}

	return items, nil
}
