// Code generated by tushare-gen. DO NOT EDIT.

package api

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// LimitListRequest 表示 limit_list API 的请求
type LimitListRequest struct {
	TradeDate string `json:"trade_date,omitempty"`
	TsCode string `json:"ts_code,omitempty"`
	LimitType string `json:"limit_type,omitempty"`
}

// LimitListItem 表示单个 limit_list 数据项
type LimitListItem struct {
	TsCode string `json:"ts_code"`
	TradeDate string `json:"trade_date"`
	Name string `json:"name"`
	Close float64 `json:"close"`
	PctChg float64 `json:"pct_chg"`
	Amplitude float64 `json:"amplitude"`
	LimitAmount float64 `json:"limit_amount"`
}

// LimitList 调用 limit_list API
func LimitList(ctx context.Context, client *sdk.Client, req *LimitListRequest) ([]LimitListItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.LimitType != "" {
		params["limit_type"] = req.LimitType
	}

	fields := []string{"ts_code", "trade_date", "name", "close", "pct_chg", "amplitude", "limit_amount"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "limit_list", params, fields, &result); err != nil {
		return nil, err
	}

	items := make([]LimitListItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 close 的简单类型
		close, ok := item["close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close 类型")
		}
		// 处理 pct_chg 的简单类型
		pctChg, ok := item["pct_chg"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_chg 类型")
		}
		// 处理 amplitude 的简单类型
		amplitude, ok := item["amplitude"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amplitude 类型")
		}
		// 处理 limit_amount 的简单类型
		limitAmount, ok := item["limit_amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 limit_amount 类型")
		}
		items[i] = LimitListItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			Name: name,
			Close: close,
			PctChg: pctChg,
			Amplitude: amplitude,
			LimitAmount: limitAmount,
		}
	}

	return items, nil
}
