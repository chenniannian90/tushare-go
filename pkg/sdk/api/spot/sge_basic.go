// Code generated by tushare-gen. DO NOT EDIT.

package spot

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// SgeBasicRequest 表示 上海黄金基础信息 API 的请求
type SgeBasicRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 合约代码 （支持多个，逗号分隔，不输入为获取全部）
}

// SgeBasicItem 表示单个 上海黄金基础信息 数据项
type SgeBasicItem struct {
	TsCode string `json:"ts_code"` // 品种代码
	TsName string `json:"ts_name"` // 品种名称
	TradeType string `json:"trade_type"` // 交易类型
	TUnit float64 `json:"t_unit"` // 交易单位(克/手)
	PUnit float64 `json:"p_unit"` // 报价单位
	MinChange float64 `json:"min_change"` // 最小变动价位
	PriceLimit float64 `json:"price_limit"` // 每日价格最大波动限制
	MinVol int `json:"min_vol"` // 最小单笔报价量(手)
	MaxVol int `json:"max_vol"` // 最大单笔报价量(手)
	TradeMode string `json:"trade_mode"` // 交易期限
	MarginRate float64 `json:"margin_rate"` // 保证金比例
	LiqRate float64 `json:"liq_rate"` // 违约金比例(%)
	TradeTime string `json:"trade_time"` // 交易时间
	ListDate string `json:"list_date"` // 上市日期
}

// SgeBasic 调用 上海黄金基础信息 API
func SgeBasic(ctx context.Context, client *sdk.Client, req *SgeBasicRequest) ([]SgeBasicItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}

	fields := []string{"ts_code", "ts_name", "trade_type", "t_unit", "p_unit", "min_change", "price_limit", "min_vol", "max_vol", "trade_mode", "margin_rate", "liq_rate", "trade_time", "list_date"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "sge_basic", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]SgeBasicItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 ts_name 的简单类型
		tsName, ok := item["ts_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_name 类型")
		}
		// 处理 trade_type 的简单类型
		tradeType, ok := item["trade_type"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_type 类型")
		}
		// 处理 t_unit 的简单类型
		tUnit, ok := item["t_unit"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 t_unit 类型")
		}
		// 处理 p_unit 的简单类型
		pUnit, ok := item["p_unit"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 p_unit 类型")
		}
		// 处理 min_change 的简单类型
		minChange, ok := item["min_change"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 min_change 类型")
		}
		// 处理 price_limit 的简单类型
		priceLimit, ok := item["price_limit"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 price_limit 类型")
		}
		// 处理 min_vol 的简单类型
		minVol, ok := item["min_vol"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 min_vol 类型")
		}
		// 处理 max_vol 的简单类型
		maxVol, ok := item["max_vol"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 max_vol 类型")
		}
		// 处理 trade_mode 的简单类型
		tradeMode, ok := item["trade_mode"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_mode 类型")
		}
		// 处理 margin_rate 的简单类型
		marginRate, ok := item["margin_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 margin_rate 类型")
		}
		// 处理 liq_rate 的简单类型
		liqRate, ok := item["liq_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 liq_rate 类型")
		}
		// 处理 trade_time 的简单类型
		tradeTime, ok := item["trade_time"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_time 类型")
		}
		// 处理 list_date 的简单类型
		listDate, ok := item["list_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 list_date 类型")
		}
		items[i] = SgeBasicItem{
			TsCode: tsCode,
			TsName: tsName,
			TradeType: tradeType,
			TUnit: tUnit,
			PUnit: pUnit,
			MinChange: minChange,
			PriceLimit: priceLimit,
			MinVol: minVol,
			MaxVol: maxVol,
			TradeMode: tradeMode,
			MarginRate: marginRate,
			LiqRate: liqRate,
			TradeTime: tradeTime,
			ListDate: listDate,
		}
	}

	return items, nil
}
