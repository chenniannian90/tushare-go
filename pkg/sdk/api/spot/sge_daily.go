// Code generated by tushare-gen. DO NOT EDIT.

package spot

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// SgeDailyRequest 表示 上海黄金现货日行情 API 的请求
type SgeDailyRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 合约代码，可通过基础信息获得
	TradeDate string `json:"trade_date,omitempty"` // 交易日期
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// SgeDailyItem 表示单个 上海黄金现货日行情 数据项
type SgeDailyItem struct {
	TsCode string `json:"ts_code"` // 现货合约代码
	TradeDate string `json:"trade_date"` // 交易日
	Close float64 `json:"close"` // 收盘点(元/克)
	Open float64 `json:"open"` // 开盘点(元/克)
	High float64 `json:"high"` // 最高点(元/克)
	Low float64 `json:"low"` // 最低点(元/克)
	PriceAvg float64 `json:"price_avg"` // 加权平均价(元/克)
	Change float64 `json:"change"` // 涨跌点位(元/克)
	PctChange float64 `json:"pct_change"` // 涨跌幅
	Vol float64 `json:"vol"` // 成交量(千克)
	Amount float64 `json:"amount"` // 成交金额(元)
	Oi float64 `json:"oi"` // 市场持仓
	SettleVol float64 `json:"settle_vol"` // 交收量
	SettleDire string `json:"settle_dire"` // 持仓方向
}

// SgeDaily 调用 上海黄金现货日行情 API
// 获取上海黄金交易所现货合约日线行情
// 注意：积分要求等使用限制请参考Tushare官方文档
func SgeDaily(ctx context.Context, client *sdk.Client, req *SgeDailyRequest) ([]SgeDailyItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "close", "open", "high", "low", "price_avg", "change", "pct_change", "vol", "amount", "oi", "settle_vol", "settle_dire"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "sge_daily", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]SgeDailyItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: sge_daily")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: sge_daily")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 close 的简单类型
		close, ok := item["close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close 类型")
		}
		// 处理 open 的简单类型
		open, ok := item["open"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 open 类型")
		}
		// 处理 high 的简单类型
		high, ok := item["high"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 high 类型")
		}
		// 处理 low 的简单类型
		low, ok := item["low"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 low 类型")
		}
		// 处理 price_avg 的简单类型
		priceAvg, ok := item["price_avg"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 price_avg 类型")
		}
		// 处理 change 的简单类型
		change, ok := item["change"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 change 类型")
		}
		// 处理 pct_change 的简单类型
		pctChange, ok := item["pct_change"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_change 类型")
		}
		// 处理 vol 的简单类型
		vol, ok := item["vol"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 vol 类型")
		}
		// 处理 amount 的简单类型
		amount, ok := item["amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amount 类型")
		}
		// 处理 oi 的简单类型
		oi, ok := item["oi"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 oi 类型")
		}
		// 处理 settle_vol 的简单类型
		settleVol, ok := item["settle_vol"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 settle_vol 类型")
		}
		// 处理 settle_dire 的简单类型
		// 对 string 类型尝试多种转换
		var settleDire string
		if item["settle_dire"] == nil {
			// 字段值为 null，使用零值
			settleDire = ""
		} else if v, ok := item["settle_dire"].(string); ok {
			settleDire = v
		} else if v, ok := item["settle_dire"].(float64); ok {
			settleDire = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["settle_dire"].(int); ok {
			settleDire = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["settle_dire"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: sge_daily")
			log.Printf("字段: settle_dire")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["settle_dire"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 settle_dire 类型")
		}
		items[i] = SgeDailyItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			Close: close,
			Open: open,
			High: high,
			Low: low,
			PriceAvg: priceAvg,
			Change: change,
			PctChange: pctChange,
			Vol: vol,
			Amount: amount,
			Oi: oi,
			SettleVol: settleVol,
			SettleDire: settleDire,
		}
	}

	return items, nil
}
