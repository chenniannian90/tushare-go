// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// KplListRequest 表示 榜单数据（开盘啦） API 的请求
type KplListRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期
	Tag string `json:"tag,omitempty"` // 板单类型（涨停/炸板/跌停/自然涨停/竞价，默认为涨停)
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// KplListItem 表示单个 榜单数据（开盘啦） 数据项
type KplListItem struct {
	TsCode string `json:"ts_code"` // 代码
	Name string `json:"name"` // 名称
	TradeDate string `json:"trade_date"` // 交易时间
	LuTime string `json:"lu_time"` // 涨停时间
	LdTime string `json:"ld_time"` // 跌停时间
	OpenTime string `json:"open_time"` // 开板时间
	LastTime string `json:"last_time"` // 最后涨停时间
	LuDesc string `json:"lu_desc"` // 涨停原因
	Tag string `json:"tag"` // 标签
	Theme string `json:"theme"` // 板块
	NetChange float64 `json:"net_change"` // 主力净额(元)
	BidAmount float64 `json:"bid_amount"` // 竞价成交额(元)
	Status string `json:"status"` // 状态（N连板）
	BidChange float64 `json:"bid_change"` // 竞价净额
	BidTurnover float64 `json:"bid_turnover"` // 竞价换手%
	LuBidVol float64 `json:"lu_bid_vol"` // 涨停委买额
	PctChg float64 `json:"pct_chg"` // 涨跌幅%
	BidPctChg float64 `json:"bid_pct_chg"` // 竞价涨幅%
	RtPctChg float64 `json:"rt_pct_chg"` // 实时涨幅%
	LimitOrder float64 `json:"limit_order"` // 封单
	Amount float64 `json:"amount"` // 成交额
	TurnoverRate float64 `json:"turnover_rate"` // 换手率%
	FreeFloat float64 `json:"free_float"` // 实际流通
	LuLimitOrder float64 `json:"lu_limit_order"` // 最大封单
}

// KplList 调用 榜单数据（开盘啦） API
// 获取开盘啦涨停、跌停、炸板等榜单数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func KplList(ctx context.Context, client *sdk.Client, req *KplListRequest) ([]KplListItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.Tag != "" {
		params["tag"] = req.Tag
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "name", "trade_date", "lu_time", "ld_time", "open_time", "last_time", "lu_desc", "tag", "theme", "net_change", "bid_amount", "status", "bid_change", "bid_turnover", "lu_bid_vol", "pct_chg", "bid_pct_chg", "rt_pct_chg", "limit_order", "amount", "turnover_rate", "free_float", "lu_limit_order"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "kpl_list", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]KplListItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if item["name"] == nil {
			// 字段值为 null，使用零值
			name = ""
		} else if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 lu_time 的简单类型
		// 对 string 类型尝试多种转换
		var luTime string
		if item["lu_time"] == nil {
			// 字段值为 null，使用零值
			luTime = ""
		} else if v, ok := item["lu_time"].(string); ok {
			luTime = v
		} else if v, ok := item["lu_time"].(float64); ok {
			luTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["lu_time"].(int); ok {
			luTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["lu_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: lu_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["lu_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 lu_time 类型")
		}
		// 处理 ld_time 的简单类型
		// 对 string 类型尝试多种转换
		var ldTime string
		if item["ld_time"] == nil {
			// 字段值为 null，使用零值
			ldTime = ""
		} else if v, ok := item["ld_time"].(string); ok {
			ldTime = v
		} else if v, ok := item["ld_time"].(float64); ok {
			ldTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ld_time"].(int); ok {
			ldTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ld_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: ld_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ld_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ld_time 类型")
		}
		// 处理 open_time 的简单类型
		// 对 string 类型尝试多种转换
		var openTime string
		if item["open_time"] == nil {
			// 字段值为 null，使用零值
			openTime = ""
		} else if v, ok := item["open_time"].(string); ok {
			openTime = v
		} else if v, ok := item["open_time"].(float64); ok {
			openTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["open_time"].(int); ok {
			openTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["open_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: open_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["open_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 open_time 类型")
		}
		// 处理 last_time 的简单类型
		// 对 string 类型尝试多种转换
		var lastTime string
		if item["last_time"] == nil {
			// 字段值为 null，使用零值
			lastTime = ""
		} else if v, ok := item["last_time"].(string); ok {
			lastTime = v
		} else if v, ok := item["last_time"].(float64); ok {
			lastTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["last_time"].(int); ok {
			lastTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["last_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: last_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["last_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 last_time 类型")
		}
		// 处理 lu_desc 的简单类型
		// 对 string 类型尝试多种转换
		var luDesc string
		if item["lu_desc"] == nil {
			// 字段值为 null，使用零值
			luDesc = ""
		} else if v, ok := item["lu_desc"].(string); ok {
			luDesc = v
		} else if v, ok := item["lu_desc"].(float64); ok {
			luDesc = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["lu_desc"].(int); ok {
			luDesc = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["lu_desc"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: lu_desc")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["lu_desc"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 lu_desc 类型")
		}
		// 处理 tag 的简单类型
		// 对 string 类型尝试多种转换
		var tag string
		if item["tag"] == nil {
			// 字段值为 null，使用零值
			tag = ""
		} else if v, ok := item["tag"].(string); ok {
			tag = v
		} else if v, ok := item["tag"].(float64); ok {
			tag = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["tag"].(int); ok {
			tag = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["tag"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: tag")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["tag"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 tag 类型")
		}
		// 处理 theme 的简单类型
		// 对 string 类型尝试多种转换
		var theme string
		if item["theme"] == nil {
			// 字段值为 null，使用零值
			theme = ""
		} else if v, ok := item["theme"].(string); ok {
			theme = v
		} else if v, ok := item["theme"].(float64); ok {
			theme = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["theme"].(int); ok {
			theme = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["theme"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: theme")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["theme"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 theme 类型")
		}
		// 处理 net_change 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var netChange float64
		if item["net_change"] == nil {
			// 字段值为 null，使用零值
			netChange = 0
		} else if v, ok := item["net_change"].(float64); ok {
			netChange = v
		} else if v, ok := item["net_change"].(int); ok {
			netChange = float64(v)
		} else if v, ok := item["net_change"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				netChange = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					netChange = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["net_change"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: net_change")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["net_change"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 net_change 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["net_change"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: net_change")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["net_change"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 net_change 类型，期望 float64/int/string")
		}
		// 处理 bid_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var bidAmount float64
		if item["bid_amount"] == nil {
			// 字段值为 null，使用零值
			bidAmount = 0
		} else if v, ok := item["bid_amount"].(float64); ok {
			bidAmount = v
		} else if v, ok := item["bid_amount"].(int); ok {
			bidAmount = float64(v)
		} else if v, ok := item["bid_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				bidAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					bidAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["bid_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: bid_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["bid_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 bid_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["bid_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: bid_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["bid_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 bid_amount 类型，期望 float64/int/string")
		}
		// 处理 status 的简单类型
		// 对 string 类型尝试多种转换
		var status string
		if item["status"] == nil {
			// 字段值为 null，使用零值
			status = ""
		} else if v, ok := item["status"].(string); ok {
			status = v
		} else if v, ok := item["status"].(float64); ok {
			status = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["status"].(int); ok {
			status = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["status"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: status")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["status"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 status 类型")
		}
		// 处理 bid_change 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var bidChange float64
		if item["bid_change"] == nil {
			// 字段值为 null，使用零值
			bidChange = 0
		} else if v, ok := item["bid_change"].(float64); ok {
			bidChange = v
		} else if v, ok := item["bid_change"].(int); ok {
			bidChange = float64(v)
		} else if v, ok := item["bid_change"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				bidChange = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					bidChange = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["bid_change"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: bid_change")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["bid_change"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 bid_change 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["bid_change"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: bid_change")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["bid_change"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 bid_change 类型，期望 float64/int/string")
		}
		// 处理 bid_turnover 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var bidTurnover float64
		if item["bid_turnover"] == nil {
			// 字段值为 null，使用零值
			bidTurnover = 0
		} else if v, ok := item["bid_turnover"].(float64); ok {
			bidTurnover = v
		} else if v, ok := item["bid_turnover"].(int); ok {
			bidTurnover = float64(v)
		} else if v, ok := item["bid_turnover"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				bidTurnover = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					bidTurnover = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["bid_turnover"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: bid_turnover")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["bid_turnover"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 bid_turnover 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["bid_turnover"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: bid_turnover")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["bid_turnover"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 bid_turnover 类型，期望 float64/int/string")
		}
		// 处理 lu_bid_vol 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var luBidVol float64
		if item["lu_bid_vol"] == nil {
			// 字段值为 null，使用零值
			luBidVol = 0
		} else if v, ok := item["lu_bid_vol"].(float64); ok {
			luBidVol = v
		} else if v, ok := item["lu_bid_vol"].(int); ok {
			luBidVol = float64(v)
		} else if v, ok := item["lu_bid_vol"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				luBidVol = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					luBidVol = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["lu_bid_vol"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: lu_bid_vol")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["lu_bid_vol"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 lu_bid_vol 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["lu_bid_vol"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: lu_bid_vol")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["lu_bid_vol"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 lu_bid_vol 类型，期望 float64/int/string")
		}
		// 处理 pct_chg 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var pctChg float64
		if item["pct_chg"] == nil {
			// 字段值为 null，使用零值
			pctChg = 0
		} else if v, ok := item["pct_chg"].(float64); ok {
			pctChg = v
		} else if v, ok := item["pct_chg"].(int); ok {
			pctChg = float64(v)
		} else if v, ok := item["pct_chg"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				pctChg = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					pctChg = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["pct_chg"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: pct_chg")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["pct_chg"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 pct_chg 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pct_chg"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: pct_chg")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pct_chg"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pct_chg 类型，期望 float64/int/string")
		}
		// 处理 bid_pct_chg 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var bidPctChg float64
		if item["bid_pct_chg"] == nil {
			// 字段值为 null，使用零值
			bidPctChg = 0
		} else if v, ok := item["bid_pct_chg"].(float64); ok {
			bidPctChg = v
		} else if v, ok := item["bid_pct_chg"].(int); ok {
			bidPctChg = float64(v)
		} else if v, ok := item["bid_pct_chg"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				bidPctChg = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					bidPctChg = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["bid_pct_chg"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: bid_pct_chg")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["bid_pct_chg"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 bid_pct_chg 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["bid_pct_chg"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: bid_pct_chg")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["bid_pct_chg"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 bid_pct_chg 类型，期望 float64/int/string")
		}
		// 处理 rt_pct_chg 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var rtPctChg float64
		if item["rt_pct_chg"] == nil {
			// 字段值为 null，使用零值
			rtPctChg = 0
		} else if v, ok := item["rt_pct_chg"].(float64); ok {
			rtPctChg = v
		} else if v, ok := item["rt_pct_chg"].(int); ok {
			rtPctChg = float64(v)
		} else if v, ok := item["rt_pct_chg"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				rtPctChg = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					rtPctChg = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["rt_pct_chg"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: rt_pct_chg")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["rt_pct_chg"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 rt_pct_chg 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["rt_pct_chg"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: rt_pct_chg")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["rt_pct_chg"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 rt_pct_chg 类型，期望 float64/int/string")
		}
		// 处理 limit_order 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var limitOrder float64
		if item["limit_order"] == nil {
			// 字段值为 null，使用零值
			limitOrder = 0
		} else if v, ok := item["limit_order"].(float64); ok {
			limitOrder = v
		} else if v, ok := item["limit_order"].(int); ok {
			limitOrder = float64(v)
		} else if v, ok := item["limit_order"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				limitOrder = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					limitOrder = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["limit_order"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: limit_order")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["limit_order"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 limit_order 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["limit_order"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: limit_order")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["limit_order"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 limit_order 类型，期望 float64/int/string")
		}
		// 处理 amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var amount float64
		if item["amount"] == nil {
			// 字段值为 null，使用零值
			amount = 0
		} else if v, ok := item["amount"].(float64); ok {
			amount = v
		} else if v, ok := item["amount"].(int); ok {
			amount = float64(v)
		} else if v, ok := item["amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				amount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					amount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 amount 类型，期望 float64/int/string")
		}
		// 处理 turnover_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var turnoverRate float64
		if item["turnover_rate"] == nil {
			// 字段值为 null，使用零值
			turnoverRate = 0
		} else if v, ok := item["turnover_rate"].(float64); ok {
			turnoverRate = v
		} else if v, ok := item["turnover_rate"].(int); ok {
			turnoverRate = float64(v)
		} else if v, ok := item["turnover_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				turnoverRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					turnoverRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["turnover_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: turnover_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["turnover_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 turnover_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["turnover_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: turnover_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["turnover_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 turnover_rate 类型，期望 float64/int/string")
		}
		// 处理 free_float 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var freeFloat float64
		if item["free_float"] == nil {
			// 字段值为 null，使用零值
			freeFloat = 0
		} else if v, ok := item["free_float"].(float64); ok {
			freeFloat = v
		} else if v, ok := item["free_float"].(int); ok {
			freeFloat = float64(v)
		} else if v, ok := item["free_float"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				freeFloat = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					freeFloat = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["free_float"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: free_float")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["free_float"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 free_float 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["free_float"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: free_float")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["free_float"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 free_float 类型，期望 float64/int/string")
		}
		// 处理 lu_limit_order 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var luLimitOrder float64
		if item["lu_limit_order"] == nil {
			// 字段值为 null，使用零值
			luLimitOrder = 0
		} else if v, ok := item["lu_limit_order"].(float64); ok {
			luLimitOrder = v
		} else if v, ok := item["lu_limit_order"].(int); ok {
			luLimitOrder = float64(v)
		} else if v, ok := item["lu_limit_order"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				luLimitOrder = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					luLimitOrder = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["lu_limit_order"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: kpl_list")
					log.Printf("字段: lu_limit_order")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["lu_limit_order"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 lu_limit_order 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["lu_limit_order"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: kpl_list")
			log.Printf("字段: lu_limit_order")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["lu_limit_order"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 lu_limit_order 类型，期望 float64/int/string")
		}
		items[i] = KplListItem{
			TsCode: tsCode,
			Name: name,
			TradeDate: tradeDate,
			LuTime: luTime,
			LdTime: ldTime,
			OpenTime: openTime,
			LastTime: lastTime,
			LuDesc: luDesc,
			Tag: tag,
			Theme: theme,
			NetChange: netChange,
			BidAmount: bidAmount,
			Status: status,
			BidChange: bidChange,
			BidTurnover: bidTurnover,
			LuBidVol: luBidVol,
			PctChg: pctChg,
			BidPctChg: bidPctChg,
			RtPctChg: rtPctChg,
			LimitOrder: limitOrder,
			Amount: amount,
			TurnoverRate: turnoverRate,
			FreeFloat: freeFloat,
			LuLimitOrder: luLimitOrder,
		}
	}

	return items, nil
}
