// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// LimitStepRequest 表示 涨停股票连板天梯 API 的请求
type LimitStepRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（格式：YYYYMMDD，下同）
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
	Nums string `json:"nums,omitempty"` // 连板次数，支持多个输入，例如nums='2,3'
}

// LimitStepItem 表示单个 涨停股票连板天梯 数据项
type LimitStepItem struct {
	TsCode string `json:"ts_code"` // 代码
	Name string `json:"name"` // 名称
	TradeDate string `json:"trade_date"` // 交易日期
	Nums string `json:"nums"` // 连板次数
}

// LimitStep 调用 涨停股票连板天梯 API
// 获取每天连板个数晋级的股票，可以分析出每天连续涨停进阶个数，判断强势热度
// 注意：积分要求等使用限制请参考Tushare官方文档
func LimitStep(ctx context.Context, client *sdk.Client, req *LimitStepRequest) ([]LimitStepItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Nums != "" {
		params["nums"] = req.Nums
	}

	fields := []string{"ts_code", "name", "trade_date", "nums"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "limit_step", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]LimitStepItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_step")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if item["name"] == nil {
			// 字段值为 null，使用零值
			name = ""
		} else if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_step")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_step")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 nums 的简单类型
		// 对 string 类型尝试多种转换
		var nums string
		if item["nums"] == nil {
			// 字段值为 null，使用零值
			nums = ""
		} else if v, ok := item["nums"].(string); ok {
			nums = v
		} else if v, ok := item["nums"].(float64); ok {
			nums = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["nums"].(int); ok {
			nums = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["nums"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_step")
			log.Printf("字段: nums")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["nums"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 nums 类型")
		}
		items[i] = LimitStepItem{
			TsCode: tsCode,
			Name: name,
			TradeDate: tradeDate,
			Nums: nums,
		}
	}

	return items, nil
}
