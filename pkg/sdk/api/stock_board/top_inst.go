// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// TopInstRequest 表示 龙虎榜机构交易单 API 的请求
type TopInstRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期
	TsCode string `json:"ts_code,omitempty"` // TS代码
}

// TopInstItem 表示单个 龙虎榜机构交易单 数据项
type TopInstItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	TsCode string `json:"ts_code"` // TS代码
	Exalter string `json:"exalter"` // 营业部名称
	Side string `json:"side"` // 买卖类型0：买入金额最大的前5名， 1：卖出金额最大的前5名
	Buy float64 `json:"buy"` // 买入额（元）
	BuyRate float64 `json:"buy_rate"` // 买入占总成交比例
	Sell float64 `json:"sell"` // 卖出额（元）
	SellRate float64 `json:"sell_rate"` // 卖出占总成交比例
	NetBuy float64 `json:"net_buy"` // 净成交额（元）
	Reason string `json:"reason"` // 上榜理由
}

// TopInst 调用 龙虎榜机构交易单 API
// 龙虎榜机构成交明细
// 注意：积分要求等使用限制请参考Tushare官方文档
func TopInst(ctx context.Context, client *sdk.Client, req *TopInstRequest) ([]TopInstItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}

	fields := []string{"trade_date", "ts_code", "exalter", "side", "buy", "buy_rate", "sell", "sell_rate", "net_buy", "reason"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "top_inst", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]TopInstItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 exalter 的简单类型
		// 对 string 类型尝试多种转换
		var exalter string
		if item["exalter"] == nil {
			// 字段值为 null，使用零值
			exalter = ""
		} else if v, ok := item["exalter"].(string); ok {
			exalter = v
		} else if v, ok := item["exalter"].(float64); ok {
			exalter = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["exalter"].(int); ok {
			exalter = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["exalter"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: exalter")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["exalter"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 exalter 类型")
		}
		// 处理 side 的简单类型
		// 对 string 类型尝试多种转换
		var side string
		if item["side"] == nil {
			// 字段值为 null，使用零值
			side = ""
		} else if v, ok := item["side"].(string); ok {
			side = v
		} else if v, ok := item["side"].(float64); ok {
			side = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["side"].(int); ok {
			side = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["side"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: side")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["side"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 side 类型")
		}
		// 处理 buy 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buy float64
		if item["buy"] == nil {
			// 字段值为 null，使用零值
			buy = 0
		} else if v, ok := item["buy"].(float64); ok {
			buy = v
		} else if v, ok := item["buy"].(int); ok {
			buy = float64(v)
		} else if v, ok := item["buy"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buy = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buy = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: top_inst")
					log.Printf("字段: buy")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: buy")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy 类型，期望 float64/int/string")
		}
		// 处理 buy_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyRate float64
		if item["buy_rate"] == nil {
			// 字段值为 null，使用零值
			buyRate = 0
		} else if v, ok := item["buy_rate"].(float64); ok {
			buyRate = v
		} else if v, ok := item["buy_rate"].(int); ok {
			buyRate = float64(v)
		} else if v, ok := item["buy_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: top_inst")
					log.Printf("字段: buy_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: buy_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_rate 类型，期望 float64/int/string")
		}
		// 处理 sell 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var sell float64
		if item["sell"] == nil {
			// 字段值为 null，使用零值
			sell = 0
		} else if v, ok := item["sell"].(float64); ok {
			sell = v
		} else if v, ok := item["sell"].(int); ok {
			sell = float64(v)
		} else if v, ok := item["sell"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				sell = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					sell = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["sell"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: top_inst")
					log.Printf("字段: sell")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["sell"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 sell 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["sell"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: sell")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["sell"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 sell 类型，期望 float64/int/string")
		}
		// 处理 sell_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var sellRate float64
		if item["sell_rate"] == nil {
			// 字段值为 null，使用零值
			sellRate = 0
		} else if v, ok := item["sell_rate"].(float64); ok {
			sellRate = v
		} else if v, ok := item["sell_rate"].(int); ok {
			sellRate = float64(v)
		} else if v, ok := item["sell_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				sellRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					sellRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["sell_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: top_inst")
					log.Printf("字段: sell_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["sell_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 sell_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["sell_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: sell_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["sell_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 sell_rate 类型，期望 float64/int/string")
		}
		// 处理 net_buy 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var netBuy float64
		if item["net_buy"] == nil {
			// 字段值为 null，使用零值
			netBuy = 0
		} else if v, ok := item["net_buy"].(float64); ok {
			netBuy = v
		} else if v, ok := item["net_buy"].(int); ok {
			netBuy = float64(v)
		} else if v, ok := item["net_buy"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				netBuy = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					netBuy = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["net_buy"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: top_inst")
					log.Printf("字段: net_buy")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["net_buy"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 net_buy 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["net_buy"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: net_buy")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["net_buy"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 net_buy 类型，期望 float64/int/string")
		}
		// 处理 reason 的简单类型
		// 对 string 类型尝试多种转换
		var reason string
		if item["reason"] == nil {
			// 字段值为 null，使用零值
			reason = ""
		} else if v, ok := item["reason"].(string); ok {
			reason = v
		} else if v, ok := item["reason"].(float64); ok {
			reason = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["reason"].(int); ok {
			reason = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["reason"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: top_inst")
			log.Printf("字段: reason")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["reason"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 reason 类型")
		}
		items[i] = TopInstItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			Exalter: exalter,
			Side: side,
			Buy: buy,
			BuyRate: buyRate,
			Sell: sell,
			SellRate: sellRate,
			NetBuy: netBuy,
			Reason: reason,
		}
	}

	return items, nil
}
