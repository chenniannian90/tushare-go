// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// StkAuctionRequest 表示 开盘竞价成交（当日） API 的请求
type StkAuctionRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（YYYYMMDD格式，下同)
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// StkAuctionItem 表示单个 开盘竞价成交（当日） 数据项
type StkAuctionItem struct {
	TsCode string `json:"ts_code"` // 股票代码
	TradeDate string `json:"trade_date"` // 数据日期
	Vol int `json:"vol"` // 成交量（股）
	Price int `json:"price"` // 成交均价（元）
	Amount float64 `json:"amount"` // 成交金额（元）
	PreClose float64 `json:"pre_close"` // 昨收价（元）
	TurnoverRate float64 `json:"turnover_rate"` // 换手率（%）
	VolumeRatio float64 `json:"volume_ratio"` // 量比
	FloatShare float64 `json:"float_share"` // 流通股本（万股）
}

// StkAuction 调用 开盘竞价成交（当日） API
// 获取当日个股和ETF的集合竞价成交情况，每天9点25~29分之间可以获取当日的集合竞价成交数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func StkAuction(ctx context.Context, client *sdk.Client, req *StkAuctionRequest) ([]StkAuctionItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "vol", "price", "amount", "pre_close", "turnover_rate", "volume_ratio", "float_share"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "stk_auction", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]StkAuctionItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 vol 的简单类型
		vol, ok := item["vol"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 vol 类型")
		}
		// 处理 price 的简单类型
		price, ok := item["price"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 price 类型")
		}
		// 处理 amount 的简单类型
		amount, ok := item["amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amount 类型")
		}
		// 处理 pre_close 的简单类型
		preClose, ok := item["pre_close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pre_close 类型")
		}
		// 处理 turnover_rate 的简单类型
		turnoverRate, ok := item["turnover_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 turnover_rate 类型")
		}
		// 处理 volume_ratio 的简单类型
		volumeRatio, ok := item["volume_ratio"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 volume_ratio 类型")
		}
		// 处理 float_share 的简单类型
		floatShare, ok := item["float_share"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 float_share 类型")
		}
		items[i] = StkAuctionItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			Vol: vol,
			Price: price,
			Amount: amount,
			PreClose: preClose,
			TurnoverRate: turnoverRate,
			VolumeRatio: volumeRatio,
			FloatShare: floatShare,
		}
	}

	return items, nil
}
