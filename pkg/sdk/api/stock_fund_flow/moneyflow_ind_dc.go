// Code generated by tushare-gen. DO NOT EDIT.

package stock_fund_flow

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// MoneyflowIndDcRequest 表示 板块资金流向（DC） API 的请求
type MoneyflowIndDcRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（YYYYMMDD格式，下同）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
	ContentType string `json:"content_type,omitempty"` // 资金类型(行业、概念、地域)
}

// MoneyflowIndDcItem 表示单个 板块资金流向（DC） 数据项
type MoneyflowIndDcItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	ContentType string `json:"content_type"` // 数据类型
	TsCode string `json:"ts_code"` // DC板块代码（行业、概念、地域）
	Name string `json:"name"` // 板块名称
	PctChange float64 `json:"pct_change"` // 板块涨跌幅（%）
	Close float64 `json:"close"` // 板块最新指数
	NetAmount float64 `json:"net_amount"` // 今日主力净流入 净额（元）
	NetAmountRate float64 `json:"net_amount_rate"` // 今日主力净流入净占比%
	BuyElgAmount float64 `json:"buy_elg_amount"` // 今日超大单净流入 净额（元）
	BuyElgAmountRate float64 `json:"buy_elg_amount_rate"` // 今日超大单净流入 净占比%
	BuyLgAmount float64 `json:"buy_lg_amount"` // 今日大单净流入 净额（元）
	BuyLgAmountRate float64 `json:"buy_lg_amount_rate"` // 今日大单净流入 净占比%
	BuyMdAmount float64 `json:"buy_md_amount"` // 今日中单净流入 净额（元）
	BuyMdAmountRate float64 `json:"buy_md_amount_rate"` // 今日中单净流入 净占比%
	BuySmAmount float64 `json:"buy_sm_amount"` // 今日小单净流入 净额（元）
	BuySmAmountRate float64 `json:"buy_sm_amount_rate"` // 今日小单净流入 净占比%
	BuySmAmountStock string `json:"buy_sm_amount_stock"` // 今日主力净流入最大股
	Rank int `json:"rank"` // 序号
}

// MoneyflowIndDc 调用 板块资金流向（DC） API
// 获取东方财富板块资金流向，每天盘后更新
// 注意：积分要求等使用限制请参考Tushare官方文档
func MoneyflowIndDc(ctx context.Context, client *sdk.Client, req *MoneyflowIndDcRequest) ([]MoneyflowIndDcItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.ContentType != "" {
		params["content_type"] = req.ContentType
	}

	fields := []string{"trade_date", "content_type", "ts_code", "name", "pct_change", "close", "net_amount", "net_amount_rate", "buy_elg_amount", "buy_elg_amount_rate", "buy_lg_amount", "buy_lg_amount_rate", "buy_md_amount", "buy_md_amount_rate", "buy_sm_amount", "buy_sm_amount_rate", "buy_sm_amount_stock", "rank"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "moneyflow_ind_dc", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]MoneyflowIndDcItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 content_type 的简单类型
		// 对 string 类型尝试多种转换
		var contentType string
		if item["content_type"] == nil {
			// 字段值为 null，使用零值
			contentType = ""
		} else if v, ok := item["content_type"].(string); ok {
			contentType = v
		} else if v, ok := item["content_type"].(float64); ok {
			contentType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["content_type"].(int); ok {
			contentType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["content_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: content_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["content_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 content_type 类型")
		}
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if item["name"] == nil {
			// 字段值为 null，使用零值
			name = ""
		} else if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 pct_change 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var pctChange float64
		if item["pct_change"] == nil {
			// 字段值为 null，使用零值
			pctChange = 0
		} else if v, ok := item["pct_change"].(float64); ok {
			pctChange = v
		} else if v, ok := item["pct_change"].(int); ok {
			pctChange = float64(v)
		} else if v, ok := item["pct_change"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				pctChange = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					pctChange = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["pct_change"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: pct_change")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["pct_change"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 pct_change 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pct_change"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: pct_change")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pct_change"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pct_change 类型，期望 float64/int/string")
		}
		// 处理 close 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var close float64
		if item["close"] == nil {
			// 字段值为 null，使用零值
			close = 0
		} else if v, ok := item["close"].(float64); ok {
			close = v
		} else if v, ok := item["close"].(int); ok {
			close = float64(v)
		} else if v, ok := item["close"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				close = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					close = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["close"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: close")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["close"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 close 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["close"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: close")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["close"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 close 类型，期望 float64/int/string")
		}
		// 处理 net_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var netAmount float64
		if item["net_amount"] == nil {
			// 字段值为 null，使用零值
			netAmount = 0
		} else if v, ok := item["net_amount"].(float64); ok {
			netAmount = v
		} else if v, ok := item["net_amount"].(int); ok {
			netAmount = float64(v)
		} else if v, ok := item["net_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				netAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					netAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["net_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: net_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["net_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 net_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["net_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: net_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["net_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 net_amount 类型，期望 float64/int/string")
		}
		// 处理 net_amount_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var netAmountRate float64
		if item["net_amount_rate"] == nil {
			// 字段值为 null，使用零值
			netAmountRate = 0
		} else if v, ok := item["net_amount_rate"].(float64); ok {
			netAmountRate = v
		} else if v, ok := item["net_amount_rate"].(int); ok {
			netAmountRate = float64(v)
		} else if v, ok := item["net_amount_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				netAmountRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					netAmountRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["net_amount_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: net_amount_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["net_amount_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 net_amount_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["net_amount_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: net_amount_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["net_amount_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 net_amount_rate 类型，期望 float64/int/string")
		}
		// 处理 buy_elg_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyElgAmount float64
		if item["buy_elg_amount"] == nil {
			// 字段值为 null，使用零值
			buyElgAmount = 0
		} else if v, ok := item["buy_elg_amount"].(float64); ok {
			buyElgAmount = v
		} else if v, ok := item["buy_elg_amount"].(int); ok {
			buyElgAmount = float64(v)
		} else if v, ok := item["buy_elg_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyElgAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyElgAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_elg_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: buy_elg_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_elg_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_elg_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_elg_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_elg_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_elg_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_elg_amount 类型，期望 float64/int/string")
		}
		// 处理 buy_elg_amount_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyElgAmountRate float64
		if item["buy_elg_amount_rate"] == nil {
			// 字段值为 null，使用零值
			buyElgAmountRate = 0
		} else if v, ok := item["buy_elg_amount_rate"].(float64); ok {
			buyElgAmountRate = v
		} else if v, ok := item["buy_elg_amount_rate"].(int); ok {
			buyElgAmountRate = float64(v)
		} else if v, ok := item["buy_elg_amount_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyElgAmountRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyElgAmountRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_elg_amount_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: buy_elg_amount_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_elg_amount_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_elg_amount_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_elg_amount_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_elg_amount_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_elg_amount_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_elg_amount_rate 类型，期望 float64/int/string")
		}
		// 处理 buy_lg_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyLgAmount float64
		if item["buy_lg_amount"] == nil {
			// 字段值为 null，使用零值
			buyLgAmount = 0
		} else if v, ok := item["buy_lg_amount"].(float64); ok {
			buyLgAmount = v
		} else if v, ok := item["buy_lg_amount"].(int); ok {
			buyLgAmount = float64(v)
		} else if v, ok := item["buy_lg_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyLgAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyLgAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_lg_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: buy_lg_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_lg_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_lg_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_lg_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_lg_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_lg_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_lg_amount 类型，期望 float64/int/string")
		}
		// 处理 buy_lg_amount_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyLgAmountRate float64
		if item["buy_lg_amount_rate"] == nil {
			// 字段值为 null，使用零值
			buyLgAmountRate = 0
		} else if v, ok := item["buy_lg_amount_rate"].(float64); ok {
			buyLgAmountRate = v
		} else if v, ok := item["buy_lg_amount_rate"].(int); ok {
			buyLgAmountRate = float64(v)
		} else if v, ok := item["buy_lg_amount_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyLgAmountRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyLgAmountRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_lg_amount_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: buy_lg_amount_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_lg_amount_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_lg_amount_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_lg_amount_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_lg_amount_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_lg_amount_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_lg_amount_rate 类型，期望 float64/int/string")
		}
		// 处理 buy_md_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyMdAmount float64
		if item["buy_md_amount"] == nil {
			// 字段值为 null，使用零值
			buyMdAmount = 0
		} else if v, ok := item["buy_md_amount"].(float64); ok {
			buyMdAmount = v
		} else if v, ok := item["buy_md_amount"].(int); ok {
			buyMdAmount = float64(v)
		} else if v, ok := item["buy_md_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyMdAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyMdAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_md_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: buy_md_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_md_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_md_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_md_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_md_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_md_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_md_amount 类型，期望 float64/int/string")
		}
		// 处理 buy_md_amount_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyMdAmountRate float64
		if item["buy_md_amount_rate"] == nil {
			// 字段值为 null，使用零值
			buyMdAmountRate = 0
		} else if v, ok := item["buy_md_amount_rate"].(float64); ok {
			buyMdAmountRate = v
		} else if v, ok := item["buy_md_amount_rate"].(int); ok {
			buyMdAmountRate = float64(v)
		} else if v, ok := item["buy_md_amount_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyMdAmountRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyMdAmountRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_md_amount_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: buy_md_amount_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_md_amount_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_md_amount_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_md_amount_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_md_amount_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_md_amount_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_md_amount_rate 类型，期望 float64/int/string")
		}
		// 处理 buy_sm_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buySmAmount float64
		if item["buy_sm_amount"] == nil {
			// 字段值为 null，使用零值
			buySmAmount = 0
		} else if v, ok := item["buy_sm_amount"].(float64); ok {
			buySmAmount = v
		} else if v, ok := item["buy_sm_amount"].(int); ok {
			buySmAmount = float64(v)
		} else if v, ok := item["buy_sm_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buySmAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buySmAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_sm_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: buy_sm_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_sm_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_sm_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_sm_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_sm_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_sm_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_sm_amount 类型，期望 float64/int/string")
		}
		// 处理 buy_sm_amount_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buySmAmountRate float64
		if item["buy_sm_amount_rate"] == nil {
			// 字段值为 null，使用零值
			buySmAmountRate = 0
		} else if v, ok := item["buy_sm_amount_rate"].(float64); ok {
			buySmAmountRate = v
		} else if v, ok := item["buy_sm_amount_rate"].(int); ok {
			buySmAmountRate = float64(v)
		} else if v, ok := item["buy_sm_amount_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buySmAmountRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buySmAmountRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_sm_amount_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: moneyflow_ind_dc")
					log.Printf("字段: buy_sm_amount_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_sm_amount_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_sm_amount_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_sm_amount_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_sm_amount_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_sm_amount_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_sm_amount_rate 类型，期望 float64/int/string")
		}
		// 处理 buy_sm_amount_stock 的简单类型
		// 对 string 类型尝试多种转换
		var buySmAmountStock string
		if item["buy_sm_amount_stock"] == nil {
			// 字段值为 null，使用零值
			buySmAmountStock = ""
		} else if v, ok := item["buy_sm_amount_stock"].(string); ok {
			buySmAmountStock = v
		} else if v, ok := item["buy_sm_amount_stock"].(float64); ok {
			buySmAmountStock = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["buy_sm_amount_stock"].(int); ok {
			buySmAmountStock = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_sm_amount_stock"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: moneyflow_ind_dc")
			log.Printf("字段: buy_sm_amount_stock")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_sm_amount_stock"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_sm_amount_stock 类型")
		}
		// 处理 rank 的简单类型
		// 处理 int 类型 - JSON 数字解析为 float64，需要转换
		var rank int
		if item["rank"] == nil {
			// 字段值为 null，使用零值
			rank = 0
		} else if v, ok := item["rank"].(float64); ok {
			rank = int(v)
		} else if v, ok := item["rank"].(int); ok {
			rank = v
		} else {
			return nil, fmt.Errorf("无效的 rank 类型，期望 int 或 float64")
		}
		items[i] = MoneyflowIndDcItem{
			TradeDate: tradeDate,
			ContentType: contentType,
			TsCode: tsCode,
			Name: name,
			PctChange: pctChange,
			Close: close,
			NetAmount: netAmount,
			NetAmountRate: netAmountRate,
			BuyElgAmount: buyElgAmount,
			BuyElgAmountRate: buyElgAmountRate,
			BuyLgAmount: buyLgAmount,
			BuyLgAmountRate: buyLgAmountRate,
			BuyMdAmount: buyMdAmount,
			BuyMdAmountRate: buyMdAmountRate,
			BuySmAmount: buySmAmount,
			BuySmAmountRate: buySmAmountRate,
			BuySmAmountStock: buySmAmountStock,
			Rank: rank,
		}
	}

	return items, nil
}
