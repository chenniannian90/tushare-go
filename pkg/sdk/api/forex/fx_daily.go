// Code generated by tushare-gen. DO NOT EDIT.

package forex

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// FxDailyRequest 表示 外汇日线行情 API 的请求
type FxDailyRequest struct {
	TsCode string `json:"ts_code,omitempty"` // TS代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（GMT，日期是格林尼治时间，比北京时间晚一天）
	StartDate string `json:"start_date,omitempty"` // 开始日期（GMT）
	EndDate string `json:"end_date,omitempty"` // 结束日期（GMT）
	Exchange string `json:"exchange,omitempty"` // 交易商，目前只有FXCM
}

// FxDailyItem 表示单个 外汇日线行情 数据项
type FxDailyItem struct {
	TsCode string `json:"ts_code"` // 外汇代码
	TradeDate string `json:"trade_date"` // 交易日期
	BidOpen float64 `json:"bid_open"` // 买入开盘价
	BidClose float64 `json:"bid_close"` // 买入收盘价
	BidHigh float64 `json:"bid_high"` // 买入最高价
	BidLow float64 `json:"bid_low"` // 买入最低价
	AskOpen float64 `json:"ask_open"` // 卖出开盘价
	AskClose float64 `json:"ask_close"` // 卖出收盘价
	AskHigh float64 `json:"ask_high"` // 卖出最高价
	AskLow float64 `json:"ask_low"` // 卖出最低价
	TickQty int `json:"tick_qty"` // 报价笔数
	Exchange string `json:"exchange"` // 交易商
}

// FxDaily 调用 外汇日线行情 API
func FxDaily(ctx context.Context, client *sdk.Client, req *FxDailyRequest) ([]FxDailyItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Exchange != "" {
		params["exchange"] = req.Exchange
	}

	fields := []string{"ts_code", "trade_date", "bid_open", "bid_close", "bid_high", "bid_low", "ask_open", "ask_close", "ask_high", "ask_low", "tick_qty", "exchange"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "fx_daily", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]FxDailyItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 bid_open 的简单类型
		bidOpen, ok := item["bid_open"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bid_open 类型")
		}
		// 处理 bid_close 的简单类型
		bidClose, ok := item["bid_close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bid_close 类型")
		}
		// 处理 bid_high 的简单类型
		bidHigh, ok := item["bid_high"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bid_high 类型")
		}
		// 处理 bid_low 的简单类型
		bidLow, ok := item["bid_low"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bid_low 类型")
		}
		// 处理 ask_open 的简单类型
		askOpen, ok := item["ask_open"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ask_open 类型")
		}
		// 处理 ask_close 的简单类型
		askClose, ok := item["ask_close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ask_close 类型")
		}
		// 处理 ask_high 的简单类型
		askHigh, ok := item["ask_high"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ask_high 类型")
		}
		// 处理 ask_low 的简单类型
		askLow, ok := item["ask_low"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ask_low 类型")
		}
		// 处理 tick_qty 的简单类型
		tickQty, ok := item["tick_qty"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 tick_qty 类型")
		}
		// 处理 exchange 的简单类型
		exchange, ok := item["exchange"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 exchange 类型")
		}
		items[i] = FxDailyItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			BidOpen: bidOpen,
			BidClose: bidClose,
			BidHigh: bidHigh,
			BidLow: bidLow,
			AskOpen: askOpen,
			AskClose: askClose,
			AskHigh: askHigh,
			AskLow: askLow,
			TickQty: tickQty,
			Exchange: exchange,
		}
	}

	return items, nil
}
