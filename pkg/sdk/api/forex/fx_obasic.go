// Code generated by tushare-gen. DO NOT EDIT.

package forex

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// FxObasicRequest 表示 外汇基础信息（海外） API 的请求
type FxObasicRequest struct {
	Exchange string `json:"exchange,omitempty"` // 交易商
	Classify string `json:"classify,omitempty"` // 分类
	TsCode string `json:"ts_code,omitempty"` // TS代码
}

// FxObasicItem 表示单个 外汇基础信息（海外） 数据项
type FxObasicItem struct {
	TsCode string `json:"ts_code"` // 外汇代码
	Name string `json:"name"` // 名称
	Classify string `json:"classify"` // 分类
	Exchange string `json:"exchange"` // 交易商
	MinUnit float64 `json:"min_unit"` // 最小交易单位
	MaxUnit float64 `json:"max_unit"` // 最大交易单位
	Pip float64 `json:"pip"` // 点
	PipCost float64 `json:"pip_cost"` // 点值
	TragetSpread float64 `json:"traget_spread"` // 目标差价
	MinStopDistance float64 `json:"min_stop_distance"` // 最小止损距离（点子）
	TradingHours string `json:"trading_hours"` // 交易时间
	BreakTime string `json:"break_time"` // 休市时间
}

// FxObasic 调用 外汇基础信息（海外） API
// 获取海外外汇基础信息，目前只有FXCM交易商的数据数量：单次可提取全部数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func FxObasic(ctx context.Context, client *sdk.Client, req *FxObasicRequest) ([]FxObasicItem, error) {
	params := map[string]interface{}{}
	if req.Exchange != "" {
		params["exchange"] = req.Exchange
	}
	if req.Classify != "" {
		params["classify"] = req.Classify
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}

	fields := []string{"ts_code", "name", "classify", "exchange", "min_unit", "max_unit", "pip", "pip_cost", "traget_spread", "min_stop_distance", "trading_hours", "break_time"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "fx_obasic", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]FxObasicItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fx_obasic")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if item["name"] == nil {
			// 字段值为 null，使用零值
			name = ""
		} else if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fx_obasic")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 classify 的简单类型
		// 对 string 类型尝试多种转换
		var classify string
		if item["classify"] == nil {
			// 字段值为 null，使用零值
			classify = ""
		} else if v, ok := item["classify"].(string); ok {
			classify = v
		} else if v, ok := item["classify"].(float64); ok {
			classify = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["classify"].(int); ok {
			classify = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["classify"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fx_obasic")
			log.Printf("字段: classify")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["classify"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 classify 类型")
		}
		// 处理 exchange 的简单类型
		// 对 string 类型尝试多种转换
		var exchange string
		if item["exchange"] == nil {
			// 字段值为 null，使用零值
			exchange = ""
		} else if v, ok := item["exchange"].(string); ok {
			exchange = v
		} else if v, ok := item["exchange"].(float64); ok {
			exchange = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["exchange"].(int); ok {
			exchange = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["exchange"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fx_obasic")
			log.Printf("字段: exchange")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["exchange"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 exchange 类型")
		}
		// 处理 min_unit 的简单类型
		minUnit, ok := item["min_unit"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 min_unit 类型")
		}
		// 处理 max_unit 的简单类型
		maxUnit, ok := item["max_unit"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 max_unit 类型")
		}
		// 处理 pip 的简单类型
		pip, ok := item["pip"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pip 类型")
		}
		// 处理 pip_cost 的简单类型
		pipCost, ok := item["pip_cost"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pip_cost 类型")
		}
		// 处理 traget_spread 的简单类型
		tragetSpread, ok := item["traget_spread"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 traget_spread 类型")
		}
		// 处理 min_stop_distance 的简单类型
		minStopDistance, ok := item["min_stop_distance"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 min_stop_distance 类型")
		}
		// 处理 trading_hours 的简单类型
		// 对 string 类型尝试多种转换
		var tradingHours string
		if item["trading_hours"] == nil {
			// 字段值为 null，使用零值
			tradingHours = ""
		} else if v, ok := item["trading_hours"].(string); ok {
			tradingHours = v
		} else if v, ok := item["trading_hours"].(float64); ok {
			tradingHours = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trading_hours"].(int); ok {
			tradingHours = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trading_hours"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fx_obasic")
			log.Printf("字段: trading_hours")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trading_hours"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trading_hours 类型")
		}
		// 处理 break_time 的简单类型
		// 对 string 类型尝试多种转换
		var breakTime string
		if item["break_time"] == nil {
			// 字段值为 null，使用零值
			breakTime = ""
		} else if v, ok := item["break_time"].(string); ok {
			breakTime = v
		} else if v, ok := item["break_time"].(float64); ok {
			breakTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["break_time"].(int); ok {
			breakTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["break_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fx_obasic")
			log.Printf("字段: break_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["break_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 break_time 类型")
		}
		items[i] = FxObasicItem{
			TsCode: tsCode,
			Name: name,
			Classify: classify,
			Exchange: exchange,
			MinUnit: minUnit,
			MaxUnit: maxUnit,
			Pip: pip,
			PipCost: pipCost,
			TragetSpread: tragetSpread,
			MinStopDistance: minStopDistance,
			TradingHours: tradingHours,
			BreakTime: breakTime,
		}
	}

	return items, nil
}
