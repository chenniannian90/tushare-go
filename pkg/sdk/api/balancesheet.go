// Code generated by tushare-gen. DO NOT EDIT.

package api

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// BalancesheetRequest 表示 balancesheet API 的请求
type BalancesheetRequest struct {
	TsCode string `json:"ts_code,omitempty"`
	AnnDate string `json:"ann_date,omitempty"`
	FAnnDate string `json:"f_ann_date,omitempty"`
	StartDate string `json:"start_date,omitempty"`
	EndDate string `json:"end_date,omitempty"`
	Period string `json:"period,omitempty"`
	ReportType string `json:"report_type,omitempty"`
}

// Validate 验证请求参数
func (r *BalancesheetRequest) Validate() error {
	// 验证 TsCode 格式（如果提供）
	if r.TsCode != "" {
		if len(r.TsCode) != 9 {
			return fmt.Errorf("ts_code 必须为9个字符（例如：000001.SZ），当前为%d个字符", len(r.TsCode))
		}
		suffix := r.TsCode[6:]
		if suffix != ".SZ" && suffix != ".SH" {
			return fmt.Errorf("ts_code 必须以.SZ或.SH结尾，当前为%s", suffix)
		}
	}

	// 验证日期格式（如果提供）(YYYYMMDD)
	dateFields := []string{r.AnnDate, r.FAnnDate, r.StartDate, r.EndDate}
	for _, date := range dateFields {
		if date != "" && len(date) != 8 {
			return fmt.Errorf("日期必须为8个字符（YYYYMMDD格式），当前%s为%d个字符", date, len(date))
		}
	}

	// 验证 ReportType（如果提供）
	if r.ReportType != "" {
		validReportTypes := map[string]bool{
			"0": true, // 合并报表
			"1": true, // 单季合并
			"2": true, // 调整单季合并表
			"3": true, // 调整合并报表
			"4": true, // 母公司报表
			"5": true, // 母公司单季表
		}
		if !validReportTypes[r.ReportType] {
			return fmt.Errorf("report_type 必须为 0、1、2、3、4、5 之一，当前为%s", r.ReportType)
		}
	}

	return nil
}

// BalancesheetItem 表示单个 balancesheet 数据项
type BalancesheetItem struct {
	TsCode string `json:"ts_code"`
	AnnDate string `json:"ann_date"`
	FAnnDate string `json:"f_ann_date"`
	EndDate string `json:"end_date"`
	ReportType string `json:"report_type"`
	TotalAssets float64 `json:"total_assets"`
	TotalHldrEqyExcMinInt float64 `json:"total_hldr_eqy_exc_min_int"`
	TotalLiab float64 `json:"total_liab"`
}

// Balancesheet 调用 balancesheet API
func Balancesheet(ctx context.Context, client *sdk.Client, req *BalancesheetRequest) ([]BalancesheetItem, error) {
	// 验证请求参数
	if err := req.Validate(); err != nil {
		return nil, fmt.Errorf("无效的请求: %w", err)
	}

	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.AnnDate != "" {
		params["ann_date"] = req.AnnDate
	}
	if req.FAnnDate != "" {
		params["f_ann_date"] = req.FAnnDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Period != "" {
		params["period"] = req.Period
	}
	if req.ReportType != "" {
		params["report_type"] = req.ReportType
	}

	fields := []string{"ts_code", "ann_date", "f_ann_date", "end_date", "report_type", "total_assets", "total_hldr_eqy_exc_min_int", "total_liab"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "balancesheet", params, fields, &result); err != nil {
		return nil, err
	}

	items := make([]BalancesheetItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 ann_date 的简单类型
		annDate, ok := item["ann_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ann_date 类型")
		}
		// 处理 f_ann_date 的简单类型
		fAnnDate, ok := item["f_ann_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 f_ann_date 类型")
		}
		// 处理 end_date 的简单类型
		endDate, ok := item["end_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 end_date 类型")
		}
		// 处理 report_type 的简单类型
		reportType, ok := item["report_type"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 report_type 类型")
		}
		// 处理 total_assets 的简单类型
		totalAssets, ok := item["total_assets"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 total_assets 类型")
		}
		// 处理 total_hldr_eqy_exc_min_int 的简单类型
		totalHldrEqyExcMinInt, ok := item["total_hldr_eqy_exc_min_int"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 total_hldr_eqy_exc_min_int 类型")
		}
		// 处理 total_liab 的简单类型
		totalLiab, ok := item["total_liab"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 total_liab 类型")
		}
		items[i] = BalancesheetItem{
			TsCode: tsCode,
			AnnDate: annDate,
			FAnnDate: fAnnDate,
			EndDate: endDate,
			ReportType: reportType,
			TotalAssets: totalAssets,
			TotalHldrEqyExcMinInt: totalHldrEqyExcMinInt,
			TotalLiab: totalLiab,
		}
	}

	return items, nil
}
