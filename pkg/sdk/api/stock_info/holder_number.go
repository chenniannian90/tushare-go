// Code generated by tushare-gen. DO NOT EDIT.

package api

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// HolderNumberRequest 表示 holder_number API 的请求
type HolderNumberRequest struct {
	TsCode string `json:"ts_code,omitempty"`
	AnnDate string `json:"ann_date,omitempty"`
	StartDate string `json:"start_date,omitempty"`
	EndDate string `json:"end_date,omitempty"`
	Period string `json:"period,omitempty"`
}

// HolderNumberItem 表示单个 holder_number 数据项
type HolderNumberItem struct {
	TsCode string `json:"ts_code"`
	AnnDate string `json:"ann_date"`
	EndDate string `json:"end_date"`
	HolderNumber int `json:"holder_number"`
	HolderNumberChg float64 `json:"holder_number_chg"`
}

// HolderNumber 调用 holder_number API
func HolderNumber(ctx context.Context, client *sdk.Client, req *HolderNumberRequest) ([]HolderNumberItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.AnnDate != "" {
		params["ann_date"] = req.AnnDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Period != "" {
		params["period"] = req.Period
	}

	fields := []string{"ts_code", "ann_date", "end_date", "holder_number", "holder_number_chg"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "holder_number", params, fields, &result); err != nil {
		return nil, err
	}

	items := make([]HolderNumberItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 ann_date 的简单类型
		annDate, ok := item["ann_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ann_date 类型")
		}
		// 处理 end_date 的简单类型
		endDate, ok := item["end_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 end_date 类型")
		}
		// 处理 holder_number 的简单类型
		holderNumber, ok := item["holder_number"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 holder_number 类型")
		}
		// 处理 holder_number_chg 的简单类型
		holderNumberChg, ok := item["holder_number_chg"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 holder_number_chg 类型")
		}
		items[i] = HolderNumberItem{
			TsCode: tsCode,
			AnnDate: annDate,
			EndDate: endDate,
			HolderNumber: holderNumber,
			HolderNumberChg: holderNumberChg,
		}
	}

	return items, nil
}
