// Code generated by tushare-gen. DO NOT EDIT.

package stock_margin

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// SlbLenMmRequest 表示 做市借券交易汇总(停） API 的请求
type SlbLenMmRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（YYYYMMDD格式，下同）
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// SlbLenMmItem 表示单个 做市借券交易汇总(停） 数据项
type SlbLenMmItem struct {
	TradeDate string `json:"trade_date"` // 交易日期（YYYYMMDD）
	TsCode string `json:"ts_code"` // 股票代码
	Name string `json:"name"` // 股票名称
	OpeInv float64 `json:"ope_inv"` // 期初余量(万股)
	LentQnt float64 `json:"lent_qnt"` // 融出数量(万股)
	ClsInv float64 `json:"cls_inv"` // 期末余量(万股)
	EndBal float64 `json:"end_bal"` // 期末余额(万元)
}

// SlbLenMm 调用 做市借券交易汇总(停） API
// 做市借券交易汇总
// 注意：积分要求等使用限制请参考Tushare官方文档
func SlbLenMm(ctx context.Context, client *sdk.Client, req *SlbLenMmRequest) ([]SlbLenMmItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"trade_date", "ts_code", "name", "ope_inv", "lent_qnt", "cls_inv", "end_bal"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "slb_len_mm", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]SlbLenMmItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 ope_inv 的简单类型
		opeInv, ok := item["ope_inv"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ope_inv 类型")
		}
		// 处理 lent_qnt 的简单类型
		lentQnt, ok := item["lent_qnt"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 lent_qnt 类型")
		}
		// 处理 cls_inv 的简单类型
		clsInv, ok := item["cls_inv"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 cls_inv 类型")
		}
		// 处理 end_bal 的简单类型
		endBal, ok := item["end_bal"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 end_bal 类型")
		}
		items[i] = SlbLenMmItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			Name: name,
			OpeInv: opeInv,
			LentQnt: lentQnt,
			ClsInv: clsInv,
			EndBal: endBal,
		}
	}

	return items, nil
}
