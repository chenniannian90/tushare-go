// Code generated by tushare-gen. DO NOT EDIT.

package macro_us_rate

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// UsTrycrRequest 表示 国债实际收益率曲线利率 API 的请求
type UsTrycrRequest struct {
	Date string `json:"date,omitempty"` // 日期 （YYYYMMDD格式，下同）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
	Fields string `json:"fields,omitempty"` // 指定输出字段
}

// UsTrycrItem 表示单个 国债实际收益率曲线利率 数据项
type UsTrycrItem struct {
	Date string `json:"date"` // 日期
	Y5 float64 `json:"y5"` // 5年期
	Y7 float64 `json:"y7"` // 7年期
	Y10 float64 `json:"y10"` // 10年期
	Y20 float64 `json:"y20"` // 20年期
	Y30 float64 `json:"y30"` // 30年期
}

// UsTrycr 调用 国债实际收益率曲线利率 API
// 国债实际收益率曲线利率
// 注意：积分要求等使用限制请参考Tushare官方文档
func UsTrycr(ctx context.Context, client *sdk.Client, req *UsTrycrRequest) ([]UsTrycrItem, error) {
	params := map[string]interface{}{}
	if req.Date != "" {
		params["date"] = req.Date
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Fields != "" {
		params["fields"] = req.Fields
	}

	fields := []string{"date", "y5", "y7", "y10", "y20", "y30"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "us_trycr", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]UsTrycrItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 date 的简单类型
		// 对 string 类型尝试多种转换
		var date string
		if item["date"] == nil {
			// 字段值为 null，使用零值
			date = ""
		} else if v, ok := item["date"].(string); ok {
			date = v
		} else if v, ok := item["date"].(float64); ok {
			date = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["date"].(int); ok {
			date = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: us_trycr")
			log.Printf("字段: date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 date 类型")
		}
		// 处理 y5 的简单类型
		y5, ok := item["y5"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y5 类型")
		}
		// 处理 y7 的简单类型
		y7, ok := item["y7"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y7 类型")
		}
		// 处理 y10 的简单类型
		y10, ok := item["y10"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y10 类型")
		}
		// 处理 y20 的简单类型
		y20, ok := item["y20"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y20 类型")
		}
		// 处理 y30 的简单类型
		y30, ok := item["y30"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y30 类型")
		}
		items[i] = UsTrycrItem{
			Date: date,
			Y5: y5,
			Y7: y7,
			Y10: y10,
			Y20: y20,
			Y30: y30,
		}
	}

	return items, nil
}
