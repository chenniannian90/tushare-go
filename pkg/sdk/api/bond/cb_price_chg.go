// Code generated by tushare-gen. DO NOT EDIT.

package bond

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// CbPriceChgRequest 表示 可转债转股价变动 API 的请求
type CbPriceChgRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 转债代码，支持多值输入
}

// CbPriceChgItem 表示单个 可转债转股价变动 数据项
type CbPriceChgItem struct {
	TsCode string `json:"ts_code"` // 转债代码
	BondShortName string `json:"bond_short_name"` // 转债简称
	PublishDate string `json:"publish_date"` // 公告日期
	ChangeDate string `json:"change_date"` // 变动日期
	ConvertPriceInitial float64 `json:"convert_price_initial"` // 初始转股价格
	ConvertpriceBef float64 `json:"convertprice_bef"` // 修正前转股价格
	ConvertpriceAft float64 `json:"convertprice_aft"` // 修正后转股价格
}

// CbPriceChg 调用 可转债转股价变动 API
// 获取可转债转股价变动
// 注意：积分要求等使用限制请参考Tushare官方文档
func CbPriceChg(ctx context.Context, client *sdk.Client, req *CbPriceChgRequest) ([]CbPriceChgItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}

	fields := []string{"ts_code", "bond_short_name", "publish_date", "change_date", "convert_price_initial", "convertprice_bef", "convertprice_aft"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "cb_price_chg", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]CbPriceChgItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_price_chg")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 bond_short_name 的简单类型
		// 对 string 类型尝试多种转换
		var bondShortName string
		if v, ok := item["bond_short_name"].(string); ok {
			bondShortName = v
		} else if v, ok := item["bond_short_name"].(float64); ok {
			bondShortName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["bond_short_name"].(int); ok {
			bondShortName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["bond_short_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_price_chg")
			log.Printf("字段: bond_short_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["bond_short_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 bond_short_name 类型")
		}
		// 处理 publish_date 的简单类型
		// 对 string 类型尝试多种转换
		var publishDate string
		if v, ok := item["publish_date"].(string); ok {
			publishDate = v
		} else if v, ok := item["publish_date"].(float64); ok {
			publishDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["publish_date"].(int); ok {
			publishDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["publish_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_price_chg")
			log.Printf("字段: publish_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["publish_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 publish_date 类型")
		}
		// 处理 change_date 的简单类型
		// 对 string 类型尝试多种转换
		var changeDate string
		if v, ok := item["change_date"].(string); ok {
			changeDate = v
		} else if v, ok := item["change_date"].(float64); ok {
			changeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["change_date"].(int); ok {
			changeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["change_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_price_chg")
			log.Printf("字段: change_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["change_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 change_date 类型")
		}
		// 处理 convert_price_initial 的简单类型
		convertPriceInitial, ok := item["convert_price_initial"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 convert_price_initial 类型")
		}
		// 处理 convertprice_bef 的简单类型
		convertpriceBef, ok := item["convertprice_bef"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 convertprice_bef 类型")
		}
		// 处理 convertprice_aft 的简单类型
		convertpriceAft, ok := item["convertprice_aft"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 convertprice_aft 类型")
		}
		items[i] = CbPriceChgItem{
			TsCode: tsCode,
			BondShortName: bondShortName,
			PublishDate: publishDate,
			ChangeDate: changeDate,
			ConvertPriceInitial: convertPriceInitial,
			ConvertpriceBef: convertpriceBef,
			ConvertpriceAft: convertpriceAft,
		}
	}

	return items, nil
}
