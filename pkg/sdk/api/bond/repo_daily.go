// Code generated by tushare-gen. DO NOT EDIT.

package bond

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// RepoDailyRequest 表示 债券回购日行情 API 的请求
type RepoDailyRequest struct {
	TsCode string `json:"ts_code,omitempty"`
	TradeDate string `json:"trade_date,omitempty"`
	StartDate string `json:"start_date,omitempty"`
	EndDate string `json:"end_date,omitempty"`
}

// RepoDailyItem 表示单个 债券回购日行情 数据项
type RepoDailyItem struct {
	TsCode string `json:"ts_code"`
	TradeDate string `json:"trade_date"`
	RepoMaturity string `json:"repo_maturity"`
	PreClose float64 `json:"pre_close"`
	Open float64 `json:"open"`
	High float64 `json:"high"`
	Low float64 `json:"low"`
	Close float64 `json:"close"`
	Weight float64 `json:"weight"`
	WeightR float64 `json:"weight_r"`
	Amount float64 `json:"amount"`
	Num int `json:"num"`
}

// RepoDaily 调用 债券回购日行情 API
func RepoDaily(ctx context.Context, client *sdk.Client, req *RepoDailyRequest) ([]RepoDailyItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "repo_maturity", "pre_close", "open", "high", "low", "close", "weight", "weight_r", "amount", "num"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "repo_daily", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]RepoDailyItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 repo_maturity 的简单类型
		repoMaturity, ok := item["repo_maturity"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 repo_maturity 类型")
		}
		// 处理 pre_close 的简单类型
		preClose, ok := item["pre_close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pre_close 类型")
		}
		// 处理 open 的简单类型
		open, ok := item["open"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 open 类型")
		}
		// 处理 high 的简单类型
		high, ok := item["high"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 high 类型")
		}
		// 处理 low 的简单类型
		low, ok := item["low"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 low 类型")
		}
		// 处理 close 的简单类型
		close, ok := item["close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close 类型")
		}
		// 处理 weight 的简单类型
		weight, ok := item["weight"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 weight 类型")
		}
		// 处理 weight_r 的简单类型
		weightR, ok := item["weight_r"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 weight_r 类型")
		}
		// 处理 amount 的简单类型
		amount, ok := item["amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amount 类型")
		}
		// 处理 num 的简单类型
		num, ok := item["num"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 num 类型")
		}
		items[i] = RepoDailyItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			RepoMaturity: repoMaturity,
			PreClose: preClose,
			Open: open,
			High: high,
			Low: low,
			Close: close,
			Weight: weight,
			WeightR: weightR,
			Amount: amount,
			Num: num,
		}
	}

	return items, nil
}
