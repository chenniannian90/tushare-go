// Code generated by tushare-gen. DO NOT EDIT.

package bond

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// BcOtcqtRequest 表示 柜台流通式债券报价 API 的请求
type BcOtcqtRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期(YYYYMMDD格式，下同)
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
	TsCode string `json:"ts_code,omitempty"` // TS代码
	Bank string `json:"bank,omitempty"` // 报价机构
}

// BcOtcqtItem 表示单个 柜台流通式债券报价 数据项
type BcOtcqtItem struct {
	TradeDate string `json:"trade_date"` // 报价日期
	QtTime string `json:"qt_time"` // 报价时间
	Bank string `json:"bank"` // 报价机构
	TsCode string `json:"ts_code"` // 债券编码
	Name string `json:"name"` // 债券简称
	Maturity string `json:"maturity"` // 期限
	RemainMaturity string `json:"remain_maturity"` // 剩余期限
	BondType string `json:"bond_type"` // 债券类型
	CouponRate float64 `json:"coupon_rate"` // 票面利率（%）
	BuyPrice float64 `json:"buy_price"` // 投资者买入全价
	SellPrice float64 `json:"sell_price"` // 投资者卖出全价
	BuyYield float64 `json:"buy_yield"` // 投资者买入到期收益率（%）
	SellYield float64 `json:"sell_yield"` // 投资者卖出到期收益率（%）
}

// BcOtcqt 调用 柜台流通式债券报价 API
// 柜台流通式债券报价
// 注意：积分要求等使用限制请参考Tushare官方文档
func BcOtcqt(ctx context.Context, client *sdk.Client, req *BcOtcqtRequest) ([]BcOtcqtItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.Bank != "" {
		params["bank"] = req.Bank
	}

	fields := []string{"trade_date", "qt_time", "bank", "ts_code", "name", "maturity", "remain_maturity", "bond_type", "coupon_rate", "buy_price", "sell_price", "buy_yield", "sell_yield"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "bc_otcqt", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]BcOtcqtItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 qt_time 的简单类型
		// 对 string 类型尝试多种转换
		var qtTime string
		if v, ok := item["qt_time"].(string); ok {
			qtTime = v
		} else if v, ok := item["qt_time"].(float64); ok {
			qtTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["qt_time"].(int); ok {
			qtTime = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 qt_time 类型")
		}
		// 处理 bank 的简单类型
		// 对 string 类型尝试多种转换
		var bank string
		if v, ok := item["bank"].(string); ok {
			bank = v
		} else if v, ok := item["bank"].(float64); ok {
			bank = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["bank"].(int); ok {
			bank = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 bank 类型")
		}
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 maturity 的简单类型
		// 对 string 类型尝试多种转换
		var maturity string
		if v, ok := item["maturity"].(string); ok {
			maturity = v
		} else if v, ok := item["maturity"].(float64); ok {
			maturity = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["maturity"].(int); ok {
			maturity = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 maturity 类型")
		}
		// 处理 remain_maturity 的简单类型
		// 对 string 类型尝试多种转换
		var remainMaturity string
		if v, ok := item["remain_maturity"].(string); ok {
			remainMaturity = v
		} else if v, ok := item["remain_maturity"].(float64); ok {
			remainMaturity = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["remain_maturity"].(int); ok {
			remainMaturity = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 remain_maturity 类型")
		}
		// 处理 bond_type 的简单类型
		// 对 string 类型尝试多种转换
		var bondType string
		if v, ok := item["bond_type"].(string); ok {
			bondType = v
		} else if v, ok := item["bond_type"].(float64); ok {
			bondType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["bond_type"].(int); ok {
			bondType = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 bond_type 类型")
		}
		// 处理 coupon_rate 的简单类型
		couponRate, ok := item["coupon_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 coupon_rate 类型")
		}
		// 处理 buy_price 的简单类型
		buyPrice, ok := item["buy_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 buy_price 类型")
		}
		// 处理 sell_price 的简单类型
		sellPrice, ok := item["sell_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 sell_price 类型")
		}
		// 处理 buy_yield 的简单类型
		buyYield, ok := item["buy_yield"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 buy_yield 类型")
		}
		// 处理 sell_yield 的简单类型
		sellYield, ok := item["sell_yield"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 sell_yield 类型")
		}
		items[i] = BcOtcqtItem{
			TradeDate: tradeDate,
			QtTime: qtTime,
			Bank: bank,
			TsCode: tsCode,
			Name: name,
			Maturity: maturity,
			RemainMaturity: remainMaturity,
			BondType: bondType,
			CouponRate: couponRate,
			BuyPrice: buyPrice,
			SellPrice: sellPrice,
			BuyYield: buyYield,
			SellYield: sellYield,
		}
	}

	return items, nil
}
