// Code generated by tushare-gen. DO NOT EDIT.

package bond

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// CbIssueRequest 表示 可转债发行 API 的请求
type CbIssueRequest struct {
	TsCode string `json:"ts_code,omitempty"` // TS代码
	AnnDate string `json:"ann_date,omitempty"` // 发行公告日
	StartDate string `json:"start_date,omitempty"` // 公告开始日期
	EndDate string `json:"end_date,omitempty"` // 公告结束日期
}

// CbIssueItem 表示单个 可转债发行 数据项
type CbIssueItem struct {
	TsCode string `json:"ts_code"` // 转债代码
	AnnDate string `json:"ann_date"` // 发行公告日
	ResAnnDate string `json:"res_ann_date"` // 发行结果公告日
	PlanIssueSize float64 `json:"plan_issue_size"` // 计划发行总额（元）
	IssueSize float64 `json:"issue_size"` // 发行总额（元）
	IssuePrice float64 `json:"issue_price"` // 发行价格
	IssueType string `json:"issue_type"` // 发行方式
	IssueCost float64 `json:"issue_cost"` // 发行费用（元）
	OnlCode string `json:"onl_code"` // 网上申购代码
	OnlName string `json:"onl_name"` // 网上申购简称
	OnlDate string `json:"onl_date"` // 网上发行日期
	OnlSize float64 `json:"onl_size"` // 网上发行总额（张）
	OnlPchVol float64 `json:"onl_pch_vol"` // 网上发行有效申购数量（张）
	OnlPchNum int `json:"onl_pch_num"` // 网上发行有效申购户数
	OnlPchExcess float64 `json:"onl_pch_excess"` // 网上发行超额认购倍数
	OnlWinningRate float64 `json:"onl_winning_rate"` // 网上发行中签率（%）
	ShdRationCode string `json:"shd_ration_code"` // 老股东配售代码
	ShdRationName string `json:"shd_ration_name"` // 老股东配售简称
	ShdRationDate string `json:"shd_ration_date"` // 老股东配售日
	ShdRationRecordDate string `json:"shd_ration_record_date"` // 老股东配售股权登记日
	ShdRationPayDate string `json:"shd_ration_pay_date"` // 老股东配售缴款日
	ShdRationPrice float64 `json:"shd_ration_price"` // 老股东配售价格
	ShdRationRatio float64 `json:"shd_ration_ratio"` // 老股东配售比例
	ShdRationSize float64 `json:"shd_ration_size"` // 老股东配售数量（张）
	ShdRationVol float64 `json:"shd_ration_vol"` // 老股东配售有效申购数量（张）
	ShdRationNum int `json:"shd_ration_num"` // 老股东配售有效申购户数
	ShdRationExcess float64 `json:"shd_ration_excess"` // 老股东配售超额认购倍数
	OfflSize float64 `json:"offl_size"` // 网下发行总额（张）
	OfflDeposit float64 `json:"offl_deposit"` // 网下发行定金比例（%）
	OfflPchVol float64 `json:"offl_pch_vol"` // 网下发行有效申购数量（张）
	OfflPchNum int `json:"offl_pch_num"` // 网下发行有效申购户数
	OfflPchExcess float64 `json:"offl_pch_excess"` // 网下发行超额认购倍数
	OfflWinningRate float64 `json:"offl_winning_rate"` // 网下发行中签率
	LeadUnderwriter string `json:"lead_underwriter"` // 主承销商
	LeadUnderwriterVol float64 `json:"lead_underwriter_vol"` // 主承销商包销数量（张）
}

// CbIssue 调用 可转债发行 API
// 获取可转债发行数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func CbIssue(ctx context.Context, client *sdk.Client, req *CbIssueRequest) ([]CbIssueItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.AnnDate != "" {
		params["ann_date"] = req.AnnDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "ann_date", "res_ann_date", "plan_issue_size", "issue_size", "issue_price", "issue_type", "issue_cost", "onl_code", "onl_name", "onl_date", "onl_size", "onl_pch_vol", "onl_pch_num", "onl_pch_excess", "onl_winning_rate", "shd_ration_code", "shd_ration_name", "shd_ration_date", "shd_ration_record_date", "shd_ration_pay_date", "shd_ration_price", "shd_ration_ratio", "shd_ration_size", "shd_ration_vol", "shd_ration_num", "shd_ration_excess", "offl_size", "offl_deposit", "offl_pch_vol", "offl_pch_num", "offl_pch_excess", "offl_winning_rate", "lead_underwriter", "lead_underwriter_vol"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "cb_issue", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]CbIssueItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 ann_date 的简单类型
		// 对 string 类型尝试多种转换
		var annDate string
		if item["ann_date"] == nil {
			// 字段值为 null，使用零值
			annDate = ""
		} else if v, ok := item["ann_date"].(string); ok {
			annDate = v
		} else if v, ok := item["ann_date"].(float64); ok {
			annDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ann_date"].(int); ok {
			annDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ann_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: ann_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ann_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ann_date 类型")
		}
		// 处理 res_ann_date 的简单类型
		// 对 string 类型尝试多种转换
		var resAnnDate string
		if item["res_ann_date"] == nil {
			// 字段值为 null，使用零值
			resAnnDate = ""
		} else if v, ok := item["res_ann_date"].(string); ok {
			resAnnDate = v
		} else if v, ok := item["res_ann_date"].(float64); ok {
			resAnnDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["res_ann_date"].(int); ok {
			resAnnDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["res_ann_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: res_ann_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["res_ann_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 res_ann_date 类型")
		}
		// 处理 plan_issue_size 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var planIssueSize float64
		if item["plan_issue_size"] == nil {
			// 字段值为 null，使用零值
			planIssueSize = 0
		} else if v, ok := item["plan_issue_size"].(float64); ok {
			planIssueSize = v
		} else if v, ok := item["plan_issue_size"].(int); ok {
			planIssueSize = float64(v)
		} else if v, ok := item["plan_issue_size"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				planIssueSize = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					planIssueSize = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["plan_issue_size"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: plan_issue_size")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["plan_issue_size"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 plan_issue_size 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["plan_issue_size"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: plan_issue_size")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["plan_issue_size"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 plan_issue_size 类型，期望 float64/int/string")
		}
		// 处理 issue_size 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var issueSize float64
		if item["issue_size"] == nil {
			// 字段值为 null，使用零值
			issueSize = 0
		} else if v, ok := item["issue_size"].(float64); ok {
			issueSize = v
		} else if v, ok := item["issue_size"].(int); ok {
			issueSize = float64(v)
		} else if v, ok := item["issue_size"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				issueSize = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					issueSize = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["issue_size"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: issue_size")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["issue_size"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 issue_size 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["issue_size"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: issue_size")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["issue_size"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 issue_size 类型，期望 float64/int/string")
		}
		// 处理 issue_price 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var issuePrice float64
		if item["issue_price"] == nil {
			// 字段值为 null，使用零值
			issuePrice = 0
		} else if v, ok := item["issue_price"].(float64); ok {
			issuePrice = v
		} else if v, ok := item["issue_price"].(int); ok {
			issuePrice = float64(v)
		} else if v, ok := item["issue_price"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				issuePrice = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					issuePrice = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["issue_price"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: issue_price")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["issue_price"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 issue_price 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["issue_price"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: issue_price")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["issue_price"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 issue_price 类型，期望 float64/int/string")
		}
		// 处理 issue_type 的简单类型
		// 对 string 类型尝试多种转换
		var issueType string
		if item["issue_type"] == nil {
			// 字段值为 null，使用零值
			issueType = ""
		} else if v, ok := item["issue_type"].(string); ok {
			issueType = v
		} else if v, ok := item["issue_type"].(float64); ok {
			issueType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["issue_type"].(int); ok {
			issueType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["issue_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: issue_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["issue_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 issue_type 类型")
		}
		// 处理 issue_cost 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var issueCost float64
		if item["issue_cost"] == nil {
			// 字段值为 null，使用零值
			issueCost = 0
		} else if v, ok := item["issue_cost"].(float64); ok {
			issueCost = v
		} else if v, ok := item["issue_cost"].(int); ok {
			issueCost = float64(v)
		} else if v, ok := item["issue_cost"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				issueCost = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					issueCost = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["issue_cost"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: issue_cost")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["issue_cost"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 issue_cost 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["issue_cost"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: issue_cost")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["issue_cost"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 issue_cost 类型，期望 float64/int/string")
		}
		// 处理 onl_code 的简单类型
		// 对 string 类型尝试多种转换
		var onlCode string
		if item["onl_code"] == nil {
			// 字段值为 null，使用零值
			onlCode = ""
		} else if v, ok := item["onl_code"].(string); ok {
			onlCode = v
		} else if v, ok := item["onl_code"].(float64); ok {
			onlCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["onl_code"].(int); ok {
			onlCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["onl_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: onl_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["onl_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 onl_code 类型")
		}
		// 处理 onl_name 的简单类型
		// 对 string 类型尝试多种转换
		var onlName string
		if item["onl_name"] == nil {
			// 字段值为 null，使用零值
			onlName = ""
		} else if v, ok := item["onl_name"].(string); ok {
			onlName = v
		} else if v, ok := item["onl_name"].(float64); ok {
			onlName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["onl_name"].(int); ok {
			onlName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["onl_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: onl_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["onl_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 onl_name 类型")
		}
		// 处理 onl_date 的简单类型
		// 对 string 类型尝试多种转换
		var onlDate string
		if item["onl_date"] == nil {
			// 字段值为 null，使用零值
			onlDate = ""
		} else if v, ok := item["onl_date"].(string); ok {
			onlDate = v
		} else if v, ok := item["onl_date"].(float64); ok {
			onlDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["onl_date"].(int); ok {
			onlDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["onl_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: onl_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["onl_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 onl_date 类型")
		}
		// 处理 onl_size 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var onlSize float64
		if item["onl_size"] == nil {
			// 字段值为 null，使用零值
			onlSize = 0
		} else if v, ok := item["onl_size"].(float64); ok {
			onlSize = v
		} else if v, ok := item["onl_size"].(int); ok {
			onlSize = float64(v)
		} else if v, ok := item["onl_size"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				onlSize = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					onlSize = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["onl_size"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: onl_size")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["onl_size"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 onl_size 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["onl_size"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: onl_size")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["onl_size"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 onl_size 类型，期望 float64/int/string")
		}
		// 处理 onl_pch_vol 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var onlPchVol float64
		if item["onl_pch_vol"] == nil {
			// 字段值为 null，使用零值
			onlPchVol = 0
		} else if v, ok := item["onl_pch_vol"].(float64); ok {
			onlPchVol = v
		} else if v, ok := item["onl_pch_vol"].(int); ok {
			onlPchVol = float64(v)
		} else if v, ok := item["onl_pch_vol"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				onlPchVol = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					onlPchVol = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["onl_pch_vol"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: onl_pch_vol")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["onl_pch_vol"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 onl_pch_vol 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["onl_pch_vol"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: onl_pch_vol")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["onl_pch_vol"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 onl_pch_vol 类型，期望 float64/int/string")
		}
		// 处理 onl_pch_num 的简单类型
		// 处理 int 类型 - JSON 数字解析为 float64，需要转换
		var onlPchNum int
		if item["onl_pch_num"] == nil {
			// 字段值为 null，使用零值
			onlPchNum = 0
		} else if v, ok := item["onl_pch_num"].(float64); ok {
			onlPchNum = int(v)
		} else if v, ok := item["onl_pch_num"].(int); ok {
			onlPchNum = v
		} else {
			return nil, fmt.Errorf("无效的 onl_pch_num 类型，期望 int 或 float64")
		}
		// 处理 onl_pch_excess 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var onlPchExcess float64
		if item["onl_pch_excess"] == nil {
			// 字段值为 null，使用零值
			onlPchExcess = 0
		} else if v, ok := item["onl_pch_excess"].(float64); ok {
			onlPchExcess = v
		} else if v, ok := item["onl_pch_excess"].(int); ok {
			onlPchExcess = float64(v)
		} else if v, ok := item["onl_pch_excess"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				onlPchExcess = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					onlPchExcess = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["onl_pch_excess"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: onl_pch_excess")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["onl_pch_excess"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 onl_pch_excess 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["onl_pch_excess"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: onl_pch_excess")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["onl_pch_excess"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 onl_pch_excess 类型，期望 float64/int/string")
		}
		// 处理 onl_winning_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var onlWinningRate float64
		if item["onl_winning_rate"] == nil {
			// 字段值为 null，使用零值
			onlWinningRate = 0
		} else if v, ok := item["onl_winning_rate"].(float64); ok {
			onlWinningRate = v
		} else if v, ok := item["onl_winning_rate"].(int); ok {
			onlWinningRate = float64(v)
		} else if v, ok := item["onl_winning_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				onlWinningRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					onlWinningRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["onl_winning_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: onl_winning_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["onl_winning_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 onl_winning_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["onl_winning_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: onl_winning_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["onl_winning_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 onl_winning_rate 类型，期望 float64/int/string")
		}
		// 处理 shd_ration_code 的简单类型
		// 对 string 类型尝试多种转换
		var shdRationCode string
		if item["shd_ration_code"] == nil {
			// 字段值为 null，使用零值
			shdRationCode = ""
		} else if v, ok := item["shd_ration_code"].(string); ok {
			shdRationCode = v
		} else if v, ok := item["shd_ration_code"].(float64); ok {
			shdRationCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["shd_ration_code"].(int); ok {
			shdRationCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_code 类型")
		}
		// 处理 shd_ration_name 的简单类型
		// 对 string 类型尝试多种转换
		var shdRationName string
		if item["shd_ration_name"] == nil {
			// 字段值为 null，使用零值
			shdRationName = ""
		} else if v, ok := item["shd_ration_name"].(string); ok {
			shdRationName = v
		} else if v, ok := item["shd_ration_name"].(float64); ok {
			shdRationName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["shd_ration_name"].(int); ok {
			shdRationName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_name 类型")
		}
		// 处理 shd_ration_date 的简单类型
		// 对 string 类型尝试多种转换
		var shdRationDate string
		if item["shd_ration_date"] == nil {
			// 字段值为 null，使用零值
			shdRationDate = ""
		} else if v, ok := item["shd_ration_date"].(string); ok {
			shdRationDate = v
		} else if v, ok := item["shd_ration_date"].(float64); ok {
			shdRationDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["shd_ration_date"].(int); ok {
			shdRationDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_date 类型")
		}
		// 处理 shd_ration_record_date 的简单类型
		// 对 string 类型尝试多种转换
		var shdRationRecordDate string
		if item["shd_ration_record_date"] == nil {
			// 字段值为 null，使用零值
			shdRationRecordDate = ""
		} else if v, ok := item["shd_ration_record_date"].(string); ok {
			shdRationRecordDate = v
		} else if v, ok := item["shd_ration_record_date"].(float64); ok {
			shdRationRecordDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["shd_ration_record_date"].(int); ok {
			shdRationRecordDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_record_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_record_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_record_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_record_date 类型")
		}
		// 处理 shd_ration_pay_date 的简单类型
		// 对 string 类型尝试多种转换
		var shdRationPayDate string
		if item["shd_ration_pay_date"] == nil {
			// 字段值为 null，使用零值
			shdRationPayDate = ""
		} else if v, ok := item["shd_ration_pay_date"].(string); ok {
			shdRationPayDate = v
		} else if v, ok := item["shd_ration_pay_date"].(float64); ok {
			shdRationPayDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["shd_ration_pay_date"].(int); ok {
			shdRationPayDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_pay_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_pay_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_pay_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_pay_date 类型")
		}
		// 处理 shd_ration_price 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var shdRationPrice float64
		if item["shd_ration_price"] == nil {
			// 字段值为 null，使用零值
			shdRationPrice = 0
		} else if v, ok := item["shd_ration_price"].(float64); ok {
			shdRationPrice = v
		} else if v, ok := item["shd_ration_price"].(int); ok {
			shdRationPrice = float64(v)
		} else if v, ok := item["shd_ration_price"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				shdRationPrice = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					shdRationPrice = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["shd_ration_price"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: shd_ration_price")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["shd_ration_price"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 shd_ration_price 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_price"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_price")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_price"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_price 类型，期望 float64/int/string")
		}
		// 处理 shd_ration_ratio 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var shdRationRatio float64
		if item["shd_ration_ratio"] == nil {
			// 字段值为 null，使用零值
			shdRationRatio = 0
		} else if v, ok := item["shd_ration_ratio"].(float64); ok {
			shdRationRatio = v
		} else if v, ok := item["shd_ration_ratio"].(int); ok {
			shdRationRatio = float64(v)
		} else if v, ok := item["shd_ration_ratio"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				shdRationRatio = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					shdRationRatio = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["shd_ration_ratio"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: shd_ration_ratio")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["shd_ration_ratio"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 shd_ration_ratio 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_ratio"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_ratio")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_ratio"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_ratio 类型，期望 float64/int/string")
		}
		// 处理 shd_ration_size 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var shdRationSize float64
		if item["shd_ration_size"] == nil {
			// 字段值为 null，使用零值
			shdRationSize = 0
		} else if v, ok := item["shd_ration_size"].(float64); ok {
			shdRationSize = v
		} else if v, ok := item["shd_ration_size"].(int); ok {
			shdRationSize = float64(v)
		} else if v, ok := item["shd_ration_size"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				shdRationSize = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					shdRationSize = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["shd_ration_size"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: shd_ration_size")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["shd_ration_size"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 shd_ration_size 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_size"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_size")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_size"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_size 类型，期望 float64/int/string")
		}
		// 处理 shd_ration_vol 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var shdRationVol float64
		if item["shd_ration_vol"] == nil {
			// 字段值为 null，使用零值
			shdRationVol = 0
		} else if v, ok := item["shd_ration_vol"].(float64); ok {
			shdRationVol = v
		} else if v, ok := item["shd_ration_vol"].(int); ok {
			shdRationVol = float64(v)
		} else if v, ok := item["shd_ration_vol"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				shdRationVol = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					shdRationVol = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["shd_ration_vol"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: shd_ration_vol")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["shd_ration_vol"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 shd_ration_vol 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_vol"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_vol")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_vol"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_vol 类型，期望 float64/int/string")
		}
		// 处理 shd_ration_num 的简单类型
		// 处理 int 类型 - JSON 数字解析为 float64，需要转换
		var shdRationNum int
		if item["shd_ration_num"] == nil {
			// 字段值为 null，使用零值
			shdRationNum = 0
		} else if v, ok := item["shd_ration_num"].(float64); ok {
			shdRationNum = int(v)
		} else if v, ok := item["shd_ration_num"].(int); ok {
			shdRationNum = v
		} else {
			return nil, fmt.Errorf("无效的 shd_ration_num 类型，期望 int 或 float64")
		}
		// 处理 shd_ration_excess 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var shdRationExcess float64
		if item["shd_ration_excess"] == nil {
			// 字段值为 null，使用零值
			shdRationExcess = 0
		} else if v, ok := item["shd_ration_excess"].(float64); ok {
			shdRationExcess = v
		} else if v, ok := item["shd_ration_excess"].(int); ok {
			shdRationExcess = float64(v)
		} else if v, ok := item["shd_ration_excess"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				shdRationExcess = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					shdRationExcess = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["shd_ration_excess"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: shd_ration_excess")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["shd_ration_excess"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 shd_ration_excess 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["shd_ration_excess"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: shd_ration_excess")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["shd_ration_excess"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 shd_ration_excess 类型，期望 float64/int/string")
		}
		// 处理 offl_size 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var offlSize float64
		if item["offl_size"] == nil {
			// 字段值为 null，使用零值
			offlSize = 0
		} else if v, ok := item["offl_size"].(float64); ok {
			offlSize = v
		} else if v, ok := item["offl_size"].(int); ok {
			offlSize = float64(v)
		} else if v, ok := item["offl_size"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				offlSize = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					offlSize = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["offl_size"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: offl_size")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["offl_size"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 offl_size 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["offl_size"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: offl_size")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["offl_size"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 offl_size 类型，期望 float64/int/string")
		}
		// 处理 offl_deposit 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var offlDeposit float64
		if item["offl_deposit"] == nil {
			// 字段值为 null，使用零值
			offlDeposit = 0
		} else if v, ok := item["offl_deposit"].(float64); ok {
			offlDeposit = v
		} else if v, ok := item["offl_deposit"].(int); ok {
			offlDeposit = float64(v)
		} else if v, ok := item["offl_deposit"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				offlDeposit = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					offlDeposit = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["offl_deposit"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: offl_deposit")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["offl_deposit"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 offl_deposit 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["offl_deposit"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: offl_deposit")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["offl_deposit"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 offl_deposit 类型，期望 float64/int/string")
		}
		// 处理 offl_pch_vol 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var offlPchVol float64
		if item["offl_pch_vol"] == nil {
			// 字段值为 null，使用零值
			offlPchVol = 0
		} else if v, ok := item["offl_pch_vol"].(float64); ok {
			offlPchVol = v
		} else if v, ok := item["offl_pch_vol"].(int); ok {
			offlPchVol = float64(v)
		} else if v, ok := item["offl_pch_vol"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				offlPchVol = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					offlPchVol = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["offl_pch_vol"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: offl_pch_vol")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["offl_pch_vol"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 offl_pch_vol 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["offl_pch_vol"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: offl_pch_vol")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["offl_pch_vol"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 offl_pch_vol 类型，期望 float64/int/string")
		}
		// 处理 offl_pch_num 的简单类型
		// 处理 int 类型 - JSON 数字解析为 float64，需要转换
		var offlPchNum int
		if item["offl_pch_num"] == nil {
			// 字段值为 null，使用零值
			offlPchNum = 0
		} else if v, ok := item["offl_pch_num"].(float64); ok {
			offlPchNum = int(v)
		} else if v, ok := item["offl_pch_num"].(int); ok {
			offlPchNum = v
		} else {
			return nil, fmt.Errorf("无效的 offl_pch_num 类型，期望 int 或 float64")
		}
		// 处理 offl_pch_excess 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var offlPchExcess float64
		if item["offl_pch_excess"] == nil {
			// 字段值为 null，使用零值
			offlPchExcess = 0
		} else if v, ok := item["offl_pch_excess"].(float64); ok {
			offlPchExcess = v
		} else if v, ok := item["offl_pch_excess"].(int); ok {
			offlPchExcess = float64(v)
		} else if v, ok := item["offl_pch_excess"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				offlPchExcess = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					offlPchExcess = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["offl_pch_excess"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: offl_pch_excess")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["offl_pch_excess"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 offl_pch_excess 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["offl_pch_excess"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: offl_pch_excess")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["offl_pch_excess"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 offl_pch_excess 类型，期望 float64/int/string")
		}
		// 处理 offl_winning_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var offlWinningRate float64
		if item["offl_winning_rate"] == nil {
			// 字段值为 null，使用零值
			offlWinningRate = 0
		} else if v, ok := item["offl_winning_rate"].(float64); ok {
			offlWinningRate = v
		} else if v, ok := item["offl_winning_rate"].(int); ok {
			offlWinningRate = float64(v)
		} else if v, ok := item["offl_winning_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				offlWinningRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					offlWinningRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["offl_winning_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: offl_winning_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["offl_winning_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 offl_winning_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["offl_winning_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: offl_winning_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["offl_winning_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 offl_winning_rate 类型，期望 float64/int/string")
		}
		// 处理 lead_underwriter 的简单类型
		// 对 string 类型尝试多种转换
		var leadUnderwriter string
		if item["lead_underwriter"] == nil {
			// 字段值为 null，使用零值
			leadUnderwriter = ""
		} else if v, ok := item["lead_underwriter"].(string); ok {
			leadUnderwriter = v
		} else if v, ok := item["lead_underwriter"].(float64); ok {
			leadUnderwriter = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["lead_underwriter"].(int); ok {
			leadUnderwriter = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["lead_underwriter"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: lead_underwriter")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["lead_underwriter"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 lead_underwriter 类型")
		}
		// 处理 lead_underwriter_vol 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var leadUnderwriterVol float64
		if item["lead_underwriter_vol"] == nil {
			// 字段值为 null，使用零值
			leadUnderwriterVol = 0
		} else if v, ok := item["lead_underwriter_vol"].(float64); ok {
			leadUnderwriterVol = v
		} else if v, ok := item["lead_underwriter_vol"].(int); ok {
			leadUnderwriterVol = float64(v)
		} else if v, ok := item["lead_underwriter_vol"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				leadUnderwriterVol = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					leadUnderwriterVol = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["lead_underwriter_vol"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: cb_issue")
					log.Printf("字段: lead_underwriter_vol")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["lead_underwriter_vol"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 lead_underwriter_vol 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["lead_underwriter_vol"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_issue")
			log.Printf("字段: lead_underwriter_vol")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["lead_underwriter_vol"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 lead_underwriter_vol 类型，期望 float64/int/string")
		}
		items[i] = CbIssueItem{
			TsCode: tsCode,
			AnnDate: annDate,
			ResAnnDate: resAnnDate,
			PlanIssueSize: planIssueSize,
			IssueSize: issueSize,
			IssuePrice: issuePrice,
			IssueType: issueType,
			IssueCost: issueCost,
			OnlCode: onlCode,
			OnlName: onlName,
			OnlDate: onlDate,
			OnlSize: onlSize,
			OnlPchVol: onlPchVol,
			OnlPchNum: onlPchNum,
			OnlPchExcess: onlPchExcess,
			OnlWinningRate: onlWinningRate,
			ShdRationCode: shdRationCode,
			ShdRationName: shdRationName,
			ShdRationDate: shdRationDate,
			ShdRationRecordDate: shdRationRecordDate,
			ShdRationPayDate: shdRationPayDate,
			ShdRationPrice: shdRationPrice,
			ShdRationRatio: shdRationRatio,
			ShdRationSize: shdRationSize,
			ShdRationVol: shdRationVol,
			ShdRationNum: shdRationNum,
			ShdRationExcess: shdRationExcess,
			OfflSize: offlSize,
			OfflDeposit: offlDeposit,
			OfflPchVol: offlPchVol,
			OfflPchNum: offlPchNum,
			OfflPchExcess: offlPchExcess,
			OfflWinningRate: offlWinningRate,
			LeadUnderwriter: leadUnderwriter,
			LeadUnderwriterVol: leadUnderwriterVol,
		}
	}

	return items, nil
}
