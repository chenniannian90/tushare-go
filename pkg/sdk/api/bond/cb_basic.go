// Code generated by tushare-gen. DO NOT EDIT.

package bond

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// CbBasicRequest 表示 可转债基础信息 API 的请求
type CbBasicRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 转债代码
	ListDate string `json:"list_date,omitempty"` // 上市日期
	Exchange string `json:"exchange,omitempty"` // 上市交易所
}

// CbBasicItem 表示单个 可转债基础信息 数据项
type CbBasicItem struct {
	TsCode string `json:"ts_code"` // 转债代码
	BondFullName string `json:"bond_full_name"` // 转债名称
	BondShortName string `json:"bond_short_name"` // 转债简称
	CbCode string `json:"cb_code"` // 转股申报代码
	StkCode string `json:"stk_code"` // 正股代码
	StkShortName string `json:"stk_short_name"` // 正股简称
	Maturity float64 `json:"maturity"` // 发行期限（年）
	Par float64 `json:"par"` // 面值
	IssuePrice float64 `json:"issue_price"` // 发行价格
	IssueSize float64 `json:"issue_size"` // 发行总额（元）
	RemainSize float64 `json:"remain_size"` // 债券余额（元）
	ValueDate string `json:"value_date"` // 起息日期
	MaturityDate string `json:"maturity_date"` // 到期日期
	RateType string `json:"rate_type"` // 利率类型
	CouponRate float64 `json:"coupon_rate"` // 票面利率（%）
	AddRate float64 `json:"add_rate"` // 补偿利率（%）
	PayPerYear int `json:"pay_per_year"` // 年付息次数
	ListDate string `json:"list_date"` // 上市日期
	DelistDate string `json:"delist_date"` // 摘牌日
	Exchange string `json:"exchange"` // 上市交易所
	ConvStartDate string `json:"conv_start_date"` // 转股起始日
	ConvEndDate string `json:"conv_end_date"` // 转股截止日
	ConvStopDate string `json:"conv_stop_date"` // 停止转股日(提前到期)
	FirstConvPrice float64 `json:"first_conv_price"` // 初始转股价
	ConvPrice float64 `json:"conv_price"` // 最新转股价
	RateClause string `json:"rate_clause"` // 利率说明
	PutClause string `json:"put_clause"` // 赎回条款
	MaturityPutPrice string `json:"maturity_put_price"` // 到期赎回价格(含税)
	CallClause string `json:"call_clause"` // 回售条款
	ResetClause string `json:"reset_clause"` // 特别向下修正条款
	ConvClause string `json:"conv_clause"` // 转股条款
	Guarantor string `json:"guarantor"` // 担保人
	GuaranteeType string `json:"guarantee_type"` // 担保方式
	IssueRating string `json:"issue_rating"` // 发行信用等级
	NewestRating string `json:"newest_rating"` // 最新信用等级
	RatingComp string `json:"rating_comp"` // 最新评级机构
}

// CbBasic 调用 可转债基础信息 API
// 获取可转债基本信息
// 注意：积分要求等使用限制请参考Tushare官方文档
func CbBasic(ctx context.Context, client *sdk.Client, req *CbBasicRequest) ([]CbBasicItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.ListDate != "" {
		params["list_date"] = req.ListDate
	}
	if req.Exchange != "" {
		params["exchange"] = req.Exchange
	}

	fields := []string{"ts_code", "bond_full_name", "bond_short_name", "cb_code", "stk_code", "stk_short_name", "maturity", "par", "issue_price", "issue_size", "remain_size", "value_date", "maturity_date", "rate_type", "coupon_rate", "add_rate", "pay_per_year", "list_date", "delist_date", "exchange", "conv_start_date", "conv_end_date", "conv_stop_date", "first_conv_price", "conv_price", "rate_clause", "put_clause", "maturity_put_price", "call_clause", "reset_clause", "conv_clause", "guarantor", "guarantee_type", "issue_rating", "newest_rating", "rating_comp"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "cb_basic", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]CbBasicItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 bond_full_name 的简单类型
		// 对 string 类型尝试多种转换
		var bondFullName string
		if item["bond_full_name"] == nil {
			// 字段值为 null，使用零值
			bondFullName = ""
		} else if v, ok := item["bond_full_name"].(string); ok {
			bondFullName = v
		} else if v, ok := item["bond_full_name"].(float64); ok {
			bondFullName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["bond_full_name"].(int); ok {
			bondFullName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["bond_full_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: bond_full_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["bond_full_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 bond_full_name 类型")
		}
		// 处理 bond_short_name 的简单类型
		// 对 string 类型尝试多种转换
		var bondShortName string
		if item["bond_short_name"] == nil {
			// 字段值为 null，使用零值
			bondShortName = ""
		} else if v, ok := item["bond_short_name"].(string); ok {
			bondShortName = v
		} else if v, ok := item["bond_short_name"].(float64); ok {
			bondShortName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["bond_short_name"].(int); ok {
			bondShortName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["bond_short_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: bond_short_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["bond_short_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 bond_short_name 类型")
		}
		// 处理 cb_code 的简单类型
		// 对 string 类型尝试多种转换
		var cbCode string
		if item["cb_code"] == nil {
			// 字段值为 null，使用零值
			cbCode = ""
		} else if v, ok := item["cb_code"].(string); ok {
			cbCode = v
		} else if v, ok := item["cb_code"].(float64); ok {
			cbCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["cb_code"].(int); ok {
			cbCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["cb_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: cb_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["cb_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 cb_code 类型")
		}
		// 处理 stk_code 的简单类型
		// 对 string 类型尝试多种转换
		var stkCode string
		if item["stk_code"] == nil {
			// 字段值为 null，使用零值
			stkCode = ""
		} else if v, ok := item["stk_code"].(string); ok {
			stkCode = v
		} else if v, ok := item["stk_code"].(float64); ok {
			stkCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["stk_code"].(int); ok {
			stkCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["stk_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: stk_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["stk_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 stk_code 类型")
		}
		// 处理 stk_short_name 的简单类型
		// 对 string 类型尝试多种转换
		var stkShortName string
		if item["stk_short_name"] == nil {
			// 字段值为 null，使用零值
			stkShortName = ""
		} else if v, ok := item["stk_short_name"].(string); ok {
			stkShortName = v
		} else if v, ok := item["stk_short_name"].(float64); ok {
			stkShortName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["stk_short_name"].(int); ok {
			stkShortName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["stk_short_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: stk_short_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["stk_short_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 stk_short_name 类型")
		}
		// 处理 maturity 的简单类型
		maturity, ok := item["maturity"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 maturity 类型")
		}
		// 处理 par 的简单类型
		par, ok := item["par"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 par 类型")
		}
		// 处理 issue_price 的简单类型
		issuePrice, ok := item["issue_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 issue_price 类型")
		}
		// 处理 issue_size 的简单类型
		issueSize, ok := item["issue_size"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 issue_size 类型")
		}
		// 处理 remain_size 的简单类型
		remainSize, ok := item["remain_size"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 remain_size 类型")
		}
		// 处理 value_date 的简单类型
		// 对 string 类型尝试多种转换
		var valueDate string
		if item["value_date"] == nil {
			// 字段值为 null，使用零值
			valueDate = ""
		} else if v, ok := item["value_date"].(string); ok {
			valueDate = v
		} else if v, ok := item["value_date"].(float64); ok {
			valueDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["value_date"].(int); ok {
			valueDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["value_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: value_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["value_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 value_date 类型")
		}
		// 处理 maturity_date 的简单类型
		// 对 string 类型尝试多种转换
		var maturityDate string
		if item["maturity_date"] == nil {
			// 字段值为 null，使用零值
			maturityDate = ""
		} else if v, ok := item["maturity_date"].(string); ok {
			maturityDate = v
		} else if v, ok := item["maturity_date"].(float64); ok {
			maturityDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["maturity_date"].(int); ok {
			maturityDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["maturity_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: maturity_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["maturity_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 maturity_date 类型")
		}
		// 处理 rate_type 的简单类型
		// 对 string 类型尝试多种转换
		var rateType string
		if item["rate_type"] == nil {
			// 字段值为 null，使用零值
			rateType = ""
		} else if v, ok := item["rate_type"].(string); ok {
			rateType = v
		} else if v, ok := item["rate_type"].(float64); ok {
			rateType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["rate_type"].(int); ok {
			rateType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["rate_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: rate_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["rate_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 rate_type 类型")
		}
		// 处理 coupon_rate 的简单类型
		couponRate, ok := item["coupon_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 coupon_rate 类型")
		}
		// 处理 add_rate 的简单类型
		addRate, ok := item["add_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 add_rate 类型")
		}
		// 处理 pay_per_year 的简单类型
		// 处理 int 类型 - JSON 数字解析为 float64，需要转换
		var payPerYear int
		if item["pay_per_year"] == nil {
			// 字段值为 null，使用零值
			payPerYear = 0
		} else if v, ok := item["pay_per_year"].(float64); ok {
			payPerYear = int(v)
		} else if v, ok := item["pay_per_year"].(int); ok {
			payPerYear = v
		} else {
			return nil, fmt.Errorf("无效的 pay_per_year 类型，期望 int 或 float64")
		}
		// 处理 list_date 的简单类型
		// 对 string 类型尝试多种转换
		var listDate string
		if item["list_date"] == nil {
			// 字段值为 null，使用零值
			listDate = ""
		} else if v, ok := item["list_date"].(string); ok {
			listDate = v
		} else if v, ok := item["list_date"].(float64); ok {
			listDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["list_date"].(int); ok {
			listDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["list_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: list_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["list_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 list_date 类型")
		}
		// 处理 delist_date 的简单类型
		// 对 string 类型尝试多种转换
		var delistDate string
		if item["delist_date"] == nil {
			// 字段值为 null，使用零值
			delistDate = ""
		} else if v, ok := item["delist_date"].(string); ok {
			delistDate = v
		} else if v, ok := item["delist_date"].(float64); ok {
			delistDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["delist_date"].(int); ok {
			delistDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["delist_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: delist_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["delist_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 delist_date 类型")
		}
		// 处理 exchange 的简单类型
		// 对 string 类型尝试多种转换
		var exchange string
		if item["exchange"] == nil {
			// 字段值为 null，使用零值
			exchange = ""
		} else if v, ok := item["exchange"].(string); ok {
			exchange = v
		} else if v, ok := item["exchange"].(float64); ok {
			exchange = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["exchange"].(int); ok {
			exchange = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["exchange"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: exchange")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["exchange"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 exchange 类型")
		}
		// 处理 conv_start_date 的简单类型
		// 对 string 类型尝试多种转换
		var convStartDate string
		if item["conv_start_date"] == nil {
			// 字段值为 null，使用零值
			convStartDate = ""
		} else if v, ok := item["conv_start_date"].(string); ok {
			convStartDate = v
		} else if v, ok := item["conv_start_date"].(float64); ok {
			convStartDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["conv_start_date"].(int); ok {
			convStartDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["conv_start_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: conv_start_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["conv_start_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 conv_start_date 类型")
		}
		// 处理 conv_end_date 的简单类型
		// 对 string 类型尝试多种转换
		var convEndDate string
		if item["conv_end_date"] == nil {
			// 字段值为 null，使用零值
			convEndDate = ""
		} else if v, ok := item["conv_end_date"].(string); ok {
			convEndDate = v
		} else if v, ok := item["conv_end_date"].(float64); ok {
			convEndDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["conv_end_date"].(int); ok {
			convEndDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["conv_end_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: conv_end_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["conv_end_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 conv_end_date 类型")
		}
		// 处理 conv_stop_date 的简单类型
		// 对 string 类型尝试多种转换
		var convStopDate string
		if item["conv_stop_date"] == nil {
			// 字段值为 null，使用零值
			convStopDate = ""
		} else if v, ok := item["conv_stop_date"].(string); ok {
			convStopDate = v
		} else if v, ok := item["conv_stop_date"].(float64); ok {
			convStopDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["conv_stop_date"].(int); ok {
			convStopDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["conv_stop_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: conv_stop_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["conv_stop_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 conv_stop_date 类型")
		}
		// 处理 first_conv_price 的简单类型
		firstConvPrice, ok := item["first_conv_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 first_conv_price 类型")
		}
		// 处理 conv_price 的简单类型
		convPrice, ok := item["conv_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 conv_price 类型")
		}
		// 处理 rate_clause 的简单类型
		// 对 string 类型尝试多种转换
		var rateClause string
		if item["rate_clause"] == nil {
			// 字段值为 null，使用零值
			rateClause = ""
		} else if v, ok := item["rate_clause"].(string); ok {
			rateClause = v
		} else if v, ok := item["rate_clause"].(float64); ok {
			rateClause = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["rate_clause"].(int); ok {
			rateClause = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["rate_clause"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: rate_clause")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["rate_clause"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 rate_clause 类型")
		}
		// 处理 put_clause 的简单类型
		// 对 string 类型尝试多种转换
		var putClause string
		if item["put_clause"] == nil {
			// 字段值为 null，使用零值
			putClause = ""
		} else if v, ok := item["put_clause"].(string); ok {
			putClause = v
		} else if v, ok := item["put_clause"].(float64); ok {
			putClause = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["put_clause"].(int); ok {
			putClause = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["put_clause"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: put_clause")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["put_clause"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 put_clause 类型")
		}
		// 处理 maturity_put_price 的简单类型
		// 对 string 类型尝试多种转换
		var maturityPutPrice string
		if item["maturity_put_price"] == nil {
			// 字段值为 null，使用零值
			maturityPutPrice = ""
		} else if v, ok := item["maturity_put_price"].(string); ok {
			maturityPutPrice = v
		} else if v, ok := item["maturity_put_price"].(float64); ok {
			maturityPutPrice = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["maturity_put_price"].(int); ok {
			maturityPutPrice = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["maturity_put_price"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: maturity_put_price")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["maturity_put_price"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 maturity_put_price 类型")
		}
		// 处理 call_clause 的简单类型
		// 对 string 类型尝试多种转换
		var callClause string
		if item["call_clause"] == nil {
			// 字段值为 null，使用零值
			callClause = ""
		} else if v, ok := item["call_clause"].(string); ok {
			callClause = v
		} else if v, ok := item["call_clause"].(float64); ok {
			callClause = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["call_clause"].(int); ok {
			callClause = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["call_clause"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: call_clause")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["call_clause"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 call_clause 类型")
		}
		// 处理 reset_clause 的简单类型
		// 对 string 类型尝试多种转换
		var resetClause string
		if item["reset_clause"] == nil {
			// 字段值为 null，使用零值
			resetClause = ""
		} else if v, ok := item["reset_clause"].(string); ok {
			resetClause = v
		} else if v, ok := item["reset_clause"].(float64); ok {
			resetClause = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["reset_clause"].(int); ok {
			resetClause = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["reset_clause"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: reset_clause")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["reset_clause"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 reset_clause 类型")
		}
		// 处理 conv_clause 的简单类型
		// 对 string 类型尝试多种转换
		var convClause string
		if item["conv_clause"] == nil {
			// 字段值为 null，使用零值
			convClause = ""
		} else if v, ok := item["conv_clause"].(string); ok {
			convClause = v
		} else if v, ok := item["conv_clause"].(float64); ok {
			convClause = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["conv_clause"].(int); ok {
			convClause = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["conv_clause"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: conv_clause")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["conv_clause"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 conv_clause 类型")
		}
		// 处理 guarantor 的简单类型
		// 对 string 类型尝试多种转换
		var guarantor string
		if item["guarantor"] == nil {
			// 字段值为 null，使用零值
			guarantor = ""
		} else if v, ok := item["guarantor"].(string); ok {
			guarantor = v
		} else if v, ok := item["guarantor"].(float64); ok {
			guarantor = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["guarantor"].(int); ok {
			guarantor = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["guarantor"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: guarantor")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["guarantor"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 guarantor 类型")
		}
		// 处理 guarantee_type 的简单类型
		// 对 string 类型尝试多种转换
		var guaranteeType string
		if item["guarantee_type"] == nil {
			// 字段值为 null，使用零值
			guaranteeType = ""
		} else if v, ok := item["guarantee_type"].(string); ok {
			guaranteeType = v
		} else if v, ok := item["guarantee_type"].(float64); ok {
			guaranteeType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["guarantee_type"].(int); ok {
			guaranteeType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["guarantee_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: guarantee_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["guarantee_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 guarantee_type 类型")
		}
		// 处理 issue_rating 的简单类型
		// 对 string 类型尝试多种转换
		var issueRating string
		if item["issue_rating"] == nil {
			// 字段值为 null，使用零值
			issueRating = ""
		} else if v, ok := item["issue_rating"].(string); ok {
			issueRating = v
		} else if v, ok := item["issue_rating"].(float64); ok {
			issueRating = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["issue_rating"].(int); ok {
			issueRating = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["issue_rating"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: issue_rating")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["issue_rating"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 issue_rating 类型")
		}
		// 处理 newest_rating 的简单类型
		// 对 string 类型尝试多种转换
		var newestRating string
		if item["newest_rating"] == nil {
			// 字段值为 null，使用零值
			newestRating = ""
		} else if v, ok := item["newest_rating"].(string); ok {
			newestRating = v
		} else if v, ok := item["newest_rating"].(float64); ok {
			newestRating = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["newest_rating"].(int); ok {
			newestRating = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["newest_rating"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: newest_rating")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["newest_rating"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 newest_rating 类型")
		}
		// 处理 rating_comp 的简单类型
		// 对 string 类型尝试多种转换
		var ratingComp string
		if item["rating_comp"] == nil {
			// 字段值为 null，使用零值
			ratingComp = ""
		} else if v, ok := item["rating_comp"].(string); ok {
			ratingComp = v
		} else if v, ok := item["rating_comp"].(float64); ok {
			ratingComp = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["rating_comp"].(int); ok {
			ratingComp = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["rating_comp"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cb_basic")
			log.Printf("字段: rating_comp")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["rating_comp"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 rating_comp 类型")
		}
		items[i] = CbBasicItem{
			TsCode: tsCode,
			BondFullName: bondFullName,
			BondShortName: bondShortName,
			CbCode: cbCode,
			StkCode: stkCode,
			StkShortName: stkShortName,
			Maturity: maturity,
			Par: par,
			IssuePrice: issuePrice,
			IssueSize: issueSize,
			RemainSize: remainSize,
			ValueDate: valueDate,
			MaturityDate: maturityDate,
			RateType: rateType,
			CouponRate: couponRate,
			AddRate: addRate,
			PayPerYear: payPerYear,
			ListDate: listDate,
			DelistDate: delistDate,
			Exchange: exchange,
			ConvStartDate: convStartDate,
			ConvEndDate: convEndDate,
			ConvStopDate: convStopDate,
			FirstConvPrice: firstConvPrice,
			ConvPrice: convPrice,
			RateClause: rateClause,
			PutClause: putClause,
			MaturityPutPrice: maturityPutPrice,
			CallClause: callClause,
			ResetClause: resetClause,
			ConvClause: convClause,
			Guarantor: guarantor,
			GuaranteeType: guaranteeType,
			IssueRating: issueRating,
			NewestRating: newestRating,
			RatingComp: ratingComp,
		}
	}

	return items, nil
}
