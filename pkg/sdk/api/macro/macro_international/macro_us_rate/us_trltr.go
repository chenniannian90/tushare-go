// Code generated by tushare-gen. DO NOT EDIT.

package macro_us_rate

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// UsTrltrRequest 表示 国债长期利率平均值 API 的请求
type UsTrltrRequest struct {
	Date string `json:"date,omitempty"` // 日期
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
	Fields string `json:"fields,omitempty"` // 指定字段
}

// UsTrltrItem 表示单个 国债长期利率平均值 数据项
type UsTrltrItem struct {
	Date string `json:"date"` // 日期
	LtrAvg float64 `json:"ltr_avg"` // 实际平均利率LT Real Average (10> Yrs)
}

// UsTrltr 调用 国债长期利率平均值 API
func UsTrltr(ctx context.Context, client *sdk.Client, req *UsTrltrRequest) ([]UsTrltrItem, error) {
	params := map[string]interface{}{}
	if req.Date != "" {
		params["date"] = req.Date
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Fields != "" {
		params["fields"] = req.Fields
	}

	fields := []string{"date", "ltr_avg"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "us_trltr", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]UsTrltrItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 date 的简单类型
		date, ok := item["date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 date 类型")
		}
		// 处理 ltr_avg 的简单类型
		ltrAvg, ok := item["ltr_avg"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ltr_avg 类型")
		}
		items[i] = UsTrltrItem{
			Date: date,
			LtrAvg: ltrAvg,
		}
	}

	return items, nil
}
