// Code generated by tushare-gen. DO NOT EDIT.

package macro_us_rate

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// UsTycrRequest 表示 国债收益率曲线利率 API 的请求
type UsTycrRequest struct {
	Date string `json:"date,omitempty"` // 日期 （YYYYMMDD格式，下同）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
	Fields string `json:"fields,omitempty"` // 指定输出字段（e.g. fields='m1,y1'）
}

// UsTycrItem 表示单个 国债收益率曲线利率 数据项
type UsTycrItem struct {
	Date string `json:"date"` // 日期
	M1 float64 `json:"m1"` // 1月期
	M2 float64 `json:"m2"` // 2月期
	M3 float64 `json:"m3"` // 3月期
	M4 float64 `json:"m4"` // 4月期（数据从20221019开始）
	M6 float64 `json:"m6"` // 6月期
	Y1 float64 `json:"y1"` // 1年期
	Y2 float64 `json:"y2"` // 2年期
	Y3 float64 `json:"y3"` // 3年期
	Y5 float64 `json:"y5"` // 5年期
	Y7 float64 `json:"y7"` // 7年期
	Y10 float64 `json:"y10"` // 10年期
	Y20 float64 `json:"y20"` // 20年期
	Y30 float64 `json:"y30"` // 30年期
}

// UsTycr 调用 国债收益率曲线利率 API
func UsTycr(ctx context.Context, client *sdk.Client, req *UsTycrRequest) ([]UsTycrItem, error) {
	params := map[string]interface{}{}
	if req.Date != "" {
		params["date"] = req.Date
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Fields != "" {
		params["fields"] = req.Fields
	}

	fields := []string{"date", "m1", "m2", "m3", "m4", "m6", "y1", "y2", "y3", "y5", "y7", "y10", "y20", "y30"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "us_tycr", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]UsTycrItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 date 的简单类型
		date, ok := item["date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 date 类型")
		}
		// 处理 m1 的简单类型
		m1, ok := item["m1"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 m1 类型")
		}
		// 处理 m2 的简单类型
		m2, ok := item["m2"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 m2 类型")
		}
		// 处理 m3 的简单类型
		m3, ok := item["m3"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 m3 类型")
		}
		// 处理 m4 的简单类型
		m4, ok := item["m4"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 m4 类型")
		}
		// 处理 m6 的简单类型
		m6, ok := item["m6"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 m6 类型")
		}
		// 处理 y1 的简单类型
		y1, ok := item["y1"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y1 类型")
		}
		// 处理 y2 的简单类型
		y2, ok := item["y2"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y2 类型")
		}
		// 处理 y3 的简单类型
		y3, ok := item["y3"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y3 类型")
		}
		// 处理 y5 的简单类型
		y5, ok := item["y5"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y5 类型")
		}
		// 处理 y7 的简单类型
		y7, ok := item["y7"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y7 类型")
		}
		// 处理 y10 的简单类型
		y10, ok := item["y10"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y10 类型")
		}
		// 处理 y20 的简单类型
		y20, ok := item["y20"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y20 类型")
		}
		// 处理 y30 的简单类型
		y30, ok := item["y30"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 y30 类型")
		}
		items[i] = UsTycrItem{
			Date: date,
			M1: m1,
			M2: m2,
			M3: m3,
			M4: m4,
			M6: m6,
			Y1: y1,
			Y2: y2,
			Y3: y3,
			Y5: y5,
			Y7: y7,
			Y10: y10,
			Y20: y20,
			Y30: y30,
		}
	}

	return items, nil
}
