// Code generated by tushare-gen. DO NOT EDIT.

package api

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// StockBasicRequest 表示 stock_basic API 的请求
type StockBasicRequest struct {
	TsCode string `json:"ts_code,omitempty"`
	ListStatus string `json:"list_status,omitempty"`
	Exchange string `json:"exchange,omitempty"`
}

// Validate 验证请求参数
func (r *StockBasicRequest) Validate() error {
	// 验证 TsCode 格式（如果提供）
	if r.TsCode != "" {
		if len(r.TsCode) != 9 {
			return fmt.Errorf("ts_code 必须为9个字符（例如：000001.SZ），当前为%d个字符", len(r.TsCode))
		}
		suffix := r.TsCode[6:]
		if suffix != ".SZ" && suffix != ".SH" {
			return fmt.Errorf("ts_code 必须以.SZ或.SH结尾，当前为%s", suffix)
		}
	}

	// 验证 ListStatus（如果提供）
	if r.ListStatus != "" {
		validStatuses := map[string]bool{
			"L":    true, // 上市
			"D":    true, // 退市
			"P":    true, // 暂停
			"del": true, // 已经退市
		}
		if !validStatuses[r.ListStatus] {
			return fmt.Errorf("list_status 必须为 L、D、P、del 之一，当前为%s", r.ListStatus)
		}
	}

	// 验证 Exchange（如果提供）
	if r.Exchange != "" {
		validExchanges := map[string]bool{
			"SSE":  true, // 上交所
			"SZSE": true, // 深交所
			"CZCE": true, // 郑商所
			"SHFE": true, // 上期所
			"INE":  true, // 能源所
			"BJE":  true, // 北交所
		}
		if !validExchanges[r.Exchange] {
			return fmt.Errorf("exchange 必须为 SSE、SZSE、CZCE、SHFE、INE、BJE 之一，当前为%s", r.Exchange)
		}
	}

	return nil
}

// StockBasicItem 表示单个 stock_basic 数据项
type StockBasicItem struct {
	TsCode string `json:"ts_code"`
	Symbol string `json:"symbol"`
	Name string `json:"name"`
	Area string `json:"area"`
	Industry string `json:"industry"`
}

// StockBasic 调用 stock_basic API
func StockBasic(ctx context.Context, client *sdk.Client, req *StockBasicRequest) ([]StockBasicItem, error) {
	// 验证请求参数
	if err := req.Validate(); err != nil {
		return nil, fmt.Errorf("无效的请求: %w", err)
	}

	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.ListStatus != "" {
		params["list_status"] = req.ListStatus
	}
	if req.Exchange != "" {
		params["exchange"] = req.Exchange
	}

	fields := []string{"ts_code", "symbol", "name", "area", "industry"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "stock_basic", params, fields, &result); err != nil {
		return nil, err
	}

	items := make([]StockBasicItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 symbol 的简单类型
		symbol, ok := item["symbol"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 symbol 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 area 的简单类型
		area, ok := item["area"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 area 类型")
		}
		// 处理 industry 的简单类型
		industry, ok := item["industry"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 industry 类型")
		}
		items[i] = StockBasicItem{
			TsCode: tsCode,
			Symbol: symbol,
			Name: name,
			Area: area,
			Industry: industry,
		}
	}

	return items, nil
}
