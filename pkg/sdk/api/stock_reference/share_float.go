// Code generated by tushare-gen. DO NOT EDIT.

package stock_reference

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// ShareFloatRequest 表示 限售股解禁 API 的请求
type ShareFloatRequest struct {
	TsCode string `json:"ts_code,omitempty"` // TS股票代码
	AnnDate string `json:"ann_date,omitempty"` // 公告日期（日期格式：YYYYMMDD，下同）
	FloatDate string `json:"float_date,omitempty"` // 解禁日期
	StartDate string `json:"start_date,omitempty"` // 解禁开始日期
	EndDate string `json:"end_date,omitempty"` // 解禁结束日期
}

// ShareFloatItem 表示单个 限售股解禁 数据项
type ShareFloatItem struct {
	TsCode string `json:"ts_code"` // TS代码
	AnnDate string `json:"ann_date"` // 公告日期
	FloatDate string `json:"float_date"` // 解禁日期
	FloatShare float64 `json:"float_share"` // 流通股份(股)
	FloatRatio float64 `json:"float_ratio"` // 流通股份占总股本比率
	HolderName string `json:"holder_name"` // 股东名称
	ShareType string `json:"share_type"` // 股份类型
}

// ShareFloat 调用 限售股解禁 API
// 获取限售股解禁
// 注意：积分要求等使用限制请参考Tushare官方文档
func ShareFloat(ctx context.Context, client *sdk.Client, req *ShareFloatRequest) ([]ShareFloatItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.AnnDate != "" {
		params["ann_date"] = req.AnnDate
	}
	if req.FloatDate != "" {
		params["float_date"] = req.FloatDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "ann_date", "float_date", "float_share", "float_ratio", "holder_name", "share_type"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "share_float", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]ShareFloatItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: share_float")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 ann_date 的简单类型
		// 对 string 类型尝试多种转换
		var annDate string
		if item["ann_date"] == nil {
			// 字段值为 null，使用零值
			annDate = ""
		} else if v, ok := item["ann_date"].(string); ok {
			annDate = v
		} else if v, ok := item["ann_date"].(float64); ok {
			annDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ann_date"].(int); ok {
			annDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ann_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: share_float")
			log.Printf("字段: ann_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ann_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ann_date 类型")
		}
		// 处理 float_date 的简单类型
		// 对 string 类型尝试多种转换
		var floatDate string
		if item["float_date"] == nil {
			// 字段值为 null，使用零值
			floatDate = ""
		} else if v, ok := item["float_date"].(string); ok {
			floatDate = v
		} else if v, ok := item["float_date"].(float64); ok {
			floatDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["float_date"].(int); ok {
			floatDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["float_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: share_float")
			log.Printf("字段: float_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["float_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 float_date 类型")
		}
		// 处理 float_share 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var floatShare float64
		if item["float_share"] == nil {
			// 字段值为 null，使用零值
			floatShare = 0
		} else if v, ok := item["float_share"].(float64); ok {
			floatShare = v
		} else if v, ok := item["float_share"].(int); ok {
			floatShare = float64(v)
		} else if v, ok := item["float_share"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				floatShare = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					floatShare = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["float_share"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: share_float")
					log.Printf("字段: float_share")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["float_share"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 float_share 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["float_share"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: share_float")
			log.Printf("字段: float_share")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["float_share"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 float_share 类型，期望 float64/int/string")
		}
		// 处理 float_ratio 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var floatRatio float64
		if item["float_ratio"] == nil {
			// 字段值为 null，使用零值
			floatRatio = 0
		} else if v, ok := item["float_ratio"].(float64); ok {
			floatRatio = v
		} else if v, ok := item["float_ratio"].(int); ok {
			floatRatio = float64(v)
		} else if v, ok := item["float_ratio"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				floatRatio = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					floatRatio = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["float_ratio"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: share_float")
					log.Printf("字段: float_ratio")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["float_ratio"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 float_ratio 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["float_ratio"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: share_float")
			log.Printf("字段: float_ratio")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["float_ratio"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 float_ratio 类型，期望 float64/int/string")
		}
		// 处理 holder_name 的简单类型
		// 对 string 类型尝试多种转换
		var holderName string
		if item["holder_name"] == nil {
			// 字段值为 null，使用零值
			holderName = ""
		} else if v, ok := item["holder_name"].(string); ok {
			holderName = v
		} else if v, ok := item["holder_name"].(float64); ok {
			holderName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["holder_name"].(int); ok {
			holderName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["holder_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: share_float")
			log.Printf("字段: holder_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["holder_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 holder_name 类型")
		}
		// 处理 share_type 的简单类型
		// 对 string 类型尝试多种转换
		var shareType string
		if item["share_type"] == nil {
			// 字段值为 null，使用零值
			shareType = ""
		} else if v, ok := item["share_type"].(string); ok {
			shareType = v
		} else if v, ok := item["share_type"].(float64); ok {
			shareType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["share_type"].(int); ok {
			shareType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["share_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: share_float")
			log.Printf("字段: share_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["share_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 share_type 类型")
		}
		items[i] = ShareFloatItem{
			TsCode: tsCode,
			AnnDate: annDate,
			FloatDate: floatDate,
			FloatShare: floatShare,
			FloatRatio: floatRatio,
			HolderName: holderName,
			ShareType: shareType,
		}
	}

	return items, nil
}
