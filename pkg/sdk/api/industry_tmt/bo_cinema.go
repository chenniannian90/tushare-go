// Code generated by tushare-gen. DO NOT EDIT.

package industry_tmt

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// BoCinemaRequest 表示 影院日度票房 API 的请求
type BoCinemaRequest struct {
	Date string `json:"date,omitempty"` // 日期(格式:YYYYMMDD)
}

// BoCinemaItem 表示单个 影院日度票房 数据项
type BoCinemaItem struct {
	Date string `json:"date"` // 日期
	CName string `json:"c_name"` // 影院名称
	AudCount int `json:"aud_count"` // 观众人数
	AttRatio float64 `json:"att_ratio"` // 上座率
	DayAmount float64 `json:"day_amount"` // 当日票房
	DayShowcount float64 `json:"day_showcount"` // 当日场次
	AvgPrice float64 `json:"avg_price"` // 场均票价（元）
	PPc float64 `json:"p_pc"` // 场均人次
	Rank int `json:"rank"` // 排名
}

// BoCinema 调用 影院日度票房 API
// 获取每日各影院的票房数据数据历史： 数据从2018年9月开始，更多历史数据正在补充数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func BoCinema(ctx context.Context, client *sdk.Client, req *BoCinemaRequest) ([]BoCinemaItem, error) {
	params := map[string]interface{}{}
	if req.Date != "" {
		params["date"] = req.Date
	}

	fields := []string{"date", "c_name", "aud_count", "att_ratio", "day_amount", "day_showcount", "avg_price", "p_pc", "rank"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "bo_cinema", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]BoCinemaItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 date 的简单类型
		// 对 string 类型尝试多种转换
		var date string
		if item["date"] == nil {
			// 字段值为 null，使用零值
			date = ""
		} else if v, ok := item["date"].(string); ok {
			date = v
		} else if v, ok := item["date"].(float64); ok {
			date = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["date"].(int); ok {
			date = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: bo_cinema")
			log.Printf("字段: date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 date 类型")
		}
		// 处理 c_name 的简单类型
		// 对 string 类型尝试多种转换
		var cName string
		if item["c_name"] == nil {
			// 字段值为 null，使用零值
			cName = ""
		} else if v, ok := item["c_name"].(string); ok {
			cName = v
		} else if v, ok := item["c_name"].(float64); ok {
			cName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["c_name"].(int); ok {
			cName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["c_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: bo_cinema")
			log.Printf("字段: c_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["c_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 c_name 类型")
		}
		// 处理 aud_count 的简单类型
		// 处理 int 类型 - JSON 数字解析为 float64，需要转换
		var audCount int
		if item["aud_count"] == nil {
			// 字段值为 null，使用零值
			audCount = 0
		} else if v, ok := item["aud_count"].(float64); ok {
			audCount = int(v)
		} else if v, ok := item["aud_count"].(int); ok {
			audCount = v
		} else {
			return nil, fmt.Errorf("无效的 aud_count 类型，期望 int 或 float64")
		}
		// 处理 att_ratio 的简单类型
		attRatio, ok := item["att_ratio"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 att_ratio 类型")
		}
		// 处理 day_amount 的简单类型
		dayAmount, ok := item["day_amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 day_amount 类型")
		}
		// 处理 day_showcount 的简单类型
		dayShowcount, ok := item["day_showcount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 day_showcount 类型")
		}
		// 处理 avg_price 的简单类型
		avgPrice, ok := item["avg_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 avg_price 类型")
		}
		// 处理 p_pc 的简单类型
		pPc, ok := item["p_pc"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 p_pc 类型")
		}
		// 处理 rank 的简单类型
		// 处理 int 类型 - JSON 数字解析为 float64，需要转换
		var rank int
		if item["rank"] == nil {
			// 字段值为 null，使用零值
			rank = 0
		} else if v, ok := item["rank"].(float64); ok {
			rank = int(v)
		} else if v, ok := item["rank"].(int); ok {
			rank = v
		} else {
			return nil, fmt.Errorf("无效的 rank 类型，期望 int 或 float64")
		}
		items[i] = BoCinemaItem{
			Date: date,
			CName: cName,
			AudCount: audCount,
			AttRatio: attRatio,
			DayAmount: dayAmount,
			DayShowcount: dayShowcount,
			AvgPrice: avgPrice,
			PPc: pPc,
			Rank: rank,
		}
	}

	return items, nil
}
