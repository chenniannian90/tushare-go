// Code generated by tushare-gen. DO NOT EDIT.

package index

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// IndexDailybasicRequest 表示 大盘指数每日指标 API 的请求
type IndexDailybasicRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期 （格式：YYYYMMDD，比如20181018，下同）
	TsCode string `json:"ts_code,omitempty"` // TS代码
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// IndexDailybasicItem 表示单个 大盘指数每日指标 数据项
type IndexDailybasicItem struct {
	TsCode string `json:"ts_code"` // TS代码
	TradeDate string `json:"trade_date"` // 交易日期
	TotalMv float64 `json:"total_mv"` // 当日总市值（元）
	FloatMv float64 `json:"float_mv"` // 当日流通市值（元）
	TotalShare float64 `json:"total_share"` // 当日总股本（股）
	FloatShare float64 `json:"float_share"` // 当日流通股本（股）
	FreeShare float64 `json:"free_share"` // 当日自由流通股本（股）
	TurnoverRate float64 `json:"turnover_rate"` // 换手率
	TurnoverRateF float64 `json:"turnover_rate_f"` // 换手率(基于自由流通股本)
	Pe float64 `json:"pe"` // 市盈率
	PeTtm float64 `json:"pe_ttm"` // 市盈率TTM
	Pb float64 `json:"pb"` // 市净率
}

// IndexDailybasic 调用 大盘指数每日指标 API
// 目前只提供上证综指，深证成指，上证50，中证500，中小板指，创业板指的每日指标数据数据来源：Tushare社区统计计算数据历史：从2004年1月开始提供数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func IndexDailybasic(ctx context.Context, client *sdk.Client, req *IndexDailybasicRequest) ([]IndexDailybasicItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "total_mv", "float_mv", "total_share", "float_share", "free_share", "turnover_rate", "turnover_rate_f", "pe", "pe_ttm", "pb"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "index_dailybasic", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]IndexDailybasicItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 total_mv 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var totalMv float64
		if item["total_mv"] == nil {
			// 字段值为 null，使用零值
			totalMv = 0
		} else if v, ok := item["total_mv"].(float64); ok {
			totalMv = v
		} else if v, ok := item["total_mv"].(int); ok {
			totalMv = float64(v)
		} else if v, ok := item["total_mv"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				totalMv = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					totalMv = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["total_mv"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: total_mv")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["total_mv"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 total_mv 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["total_mv"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: total_mv")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["total_mv"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 total_mv 类型，期望 float64/int/string")
		}
		// 处理 float_mv 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var floatMv float64
		if item["float_mv"] == nil {
			// 字段值为 null，使用零值
			floatMv = 0
		} else if v, ok := item["float_mv"].(float64); ok {
			floatMv = v
		} else if v, ok := item["float_mv"].(int); ok {
			floatMv = float64(v)
		} else if v, ok := item["float_mv"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				floatMv = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					floatMv = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["float_mv"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: float_mv")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["float_mv"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 float_mv 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["float_mv"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: float_mv")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["float_mv"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 float_mv 类型，期望 float64/int/string")
		}
		// 处理 total_share 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var totalShare float64
		if item["total_share"] == nil {
			// 字段值为 null，使用零值
			totalShare = 0
		} else if v, ok := item["total_share"].(float64); ok {
			totalShare = v
		} else if v, ok := item["total_share"].(int); ok {
			totalShare = float64(v)
		} else if v, ok := item["total_share"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				totalShare = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					totalShare = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["total_share"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: total_share")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["total_share"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 total_share 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["total_share"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: total_share")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["total_share"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 total_share 类型，期望 float64/int/string")
		}
		// 处理 float_share 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var floatShare float64
		if item["float_share"] == nil {
			// 字段值为 null，使用零值
			floatShare = 0
		} else if v, ok := item["float_share"].(float64); ok {
			floatShare = v
		} else if v, ok := item["float_share"].(int); ok {
			floatShare = float64(v)
		} else if v, ok := item["float_share"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				floatShare = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					floatShare = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["float_share"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: float_share")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["float_share"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 float_share 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["float_share"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: float_share")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["float_share"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 float_share 类型，期望 float64/int/string")
		}
		// 处理 free_share 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var freeShare float64
		if item["free_share"] == nil {
			// 字段值为 null，使用零值
			freeShare = 0
		} else if v, ok := item["free_share"].(float64); ok {
			freeShare = v
		} else if v, ok := item["free_share"].(int); ok {
			freeShare = float64(v)
		} else if v, ok := item["free_share"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				freeShare = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					freeShare = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["free_share"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: free_share")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["free_share"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 free_share 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["free_share"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: free_share")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["free_share"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 free_share 类型，期望 float64/int/string")
		}
		// 处理 turnover_rate 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var turnoverRate float64
		if item["turnover_rate"] == nil {
			// 字段值为 null，使用零值
			turnoverRate = 0
		} else if v, ok := item["turnover_rate"].(float64); ok {
			turnoverRate = v
		} else if v, ok := item["turnover_rate"].(int); ok {
			turnoverRate = float64(v)
		} else if v, ok := item["turnover_rate"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				turnoverRate = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					turnoverRate = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["turnover_rate"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: turnover_rate")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["turnover_rate"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 turnover_rate 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["turnover_rate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: turnover_rate")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["turnover_rate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 turnover_rate 类型，期望 float64/int/string")
		}
		// 处理 turnover_rate_f 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var turnoverRateF float64
		if item["turnover_rate_f"] == nil {
			// 字段值为 null，使用零值
			turnoverRateF = 0
		} else if v, ok := item["turnover_rate_f"].(float64); ok {
			turnoverRateF = v
		} else if v, ok := item["turnover_rate_f"].(int); ok {
			turnoverRateF = float64(v)
		} else if v, ok := item["turnover_rate_f"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				turnoverRateF = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					turnoverRateF = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["turnover_rate_f"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: turnover_rate_f")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["turnover_rate_f"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 turnover_rate_f 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["turnover_rate_f"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: turnover_rate_f")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["turnover_rate_f"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 turnover_rate_f 类型，期望 float64/int/string")
		}
		// 处理 pe 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var pe float64
		if item["pe"] == nil {
			// 字段值为 null，使用零值
			pe = 0
		} else if v, ok := item["pe"].(float64); ok {
			pe = v
		} else if v, ok := item["pe"].(int); ok {
			pe = float64(v)
		} else if v, ok := item["pe"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				pe = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					pe = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["pe"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: pe")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["pe"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 pe 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pe"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: pe")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pe"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pe 类型，期望 float64/int/string")
		}
		// 处理 pe_ttm 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var peTtm float64
		if item["pe_ttm"] == nil {
			// 字段值为 null，使用零值
			peTtm = 0
		} else if v, ok := item["pe_ttm"].(float64); ok {
			peTtm = v
		} else if v, ok := item["pe_ttm"].(int); ok {
			peTtm = float64(v)
		} else if v, ok := item["pe_ttm"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				peTtm = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					peTtm = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["pe_ttm"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: pe_ttm")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["pe_ttm"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 pe_ttm 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pe_ttm"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: pe_ttm")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pe_ttm"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pe_ttm 类型，期望 float64/int/string")
		}
		// 处理 pb 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var pb float64
		if item["pb"] == nil {
			// 字段值为 null，使用零值
			pb = 0
		} else if v, ok := item["pb"].(float64); ok {
			pb = v
		} else if v, ok := item["pb"].(int); ok {
			pb = float64(v)
		} else if v, ok := item["pb"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				pb = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					pb = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["pb"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: index_dailybasic")
					log.Printf("字段: pb")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["pb"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 pb 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pb"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: index_dailybasic")
			log.Printf("字段: pb")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pb"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pb 类型，期望 float64/int/string")
		}
		items[i] = IndexDailybasicItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			TotalMv: totalMv,
			FloatMv: floatMv,
			TotalShare: totalShare,
			FloatShare: floatShare,
			FreeShare: freeShare,
			TurnoverRate: turnoverRate,
			TurnoverRateF: turnoverRateF,
			Pe: pe,
			PeTtm: peTtm,
			Pb: pb,
		}
	}

	return items, nil
}
