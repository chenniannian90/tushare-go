// Code generated by tushare-gen. DO NOT EDIT.

package index

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// IndexMemberAllRequest 表示 申万行业成分（分级） API 的请求
type IndexMemberAllRequest struct {
	L1Code string `json:"l1_code,omitempty"` // 一级行业代码
	L2Code string `json:"l2_code,omitempty"` // 二级行业代码
	L3Code string `json:"l3_code,omitempty"` // 三级行业代码
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	IsNew string `json:"is_new,omitempty"` // 是否最新（默认为“Y是”）
}

// IndexMemberAllItem 表示单个 申万行业成分（分级） 数据项
type IndexMemberAllItem struct {
	L1Code string `json:"l1_code"` // 一级行业代码
	L1Name string `json:"l1_name"` // 一级行业名称
	L2Code string `json:"l2_code"` // 二级行业代码
	L2Name string `json:"l2_name"` // 二级行业名称
	L3Code string `json:"l3_code"` // 三级行业代码
	L3Name string `json:"l3_name"` // 三级行业名称
	TsCode string `json:"ts_code"` // 成分股票代码
	Name string `json:"name"` // 成分股票名称
	InDate string `json:"in_date"` // 纳入日期
	OutDate string `json:"out_date"` // 剔除日期
	IsNew string `json:"is_new"` // 是否最新Y是N否
}

// IndexMemberAll 调用 申万行业成分（分级） API
func IndexMemberAll(ctx context.Context, client *sdk.Client, req *IndexMemberAllRequest) ([]IndexMemberAllItem, error) {
	params := map[string]interface{}{}
	if req.L1Code != "" {
		params["l1_code"] = req.L1Code
	}
	if req.L2Code != "" {
		params["l2_code"] = req.L2Code
	}
	if req.L3Code != "" {
		params["l3_code"] = req.L3Code
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.IsNew != "" {
		params["is_new"] = req.IsNew
	}

	fields := []string{"l1_code", "l1_name", "l2_code", "l2_name", "l3_code", "l3_name", "ts_code", "name", "in_date", "out_date", "is_new"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "index_member_all", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]IndexMemberAllItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 l1_code 的简单类型
		l1Code, ok := item["l1_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 l1_code 类型")
		}
		// 处理 l1_name 的简单类型
		l1Name, ok := item["l1_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 l1_name 类型")
		}
		// 处理 l2_code 的简单类型
		l2Code, ok := item["l2_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 l2_code 类型")
		}
		// 处理 l2_name 的简单类型
		l2Name, ok := item["l2_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 l2_name 类型")
		}
		// 处理 l3_code 的简单类型
		l3Code, ok := item["l3_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 l3_code 类型")
		}
		// 处理 l3_name 的简单类型
		l3Name, ok := item["l3_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 l3_name 类型")
		}
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 in_date 的简单类型
		inDate, ok := item["in_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 in_date 类型")
		}
		// 处理 out_date 的简单类型
		outDate, ok := item["out_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 out_date 类型")
		}
		// 处理 is_new 的简单类型
		isNew, ok := item["is_new"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 is_new 类型")
		}
		items[i] = IndexMemberAllItem{
			L1Code: l1Code,
			L1Name: l1Name,
			L2Code: l2Code,
			L2Name: l2Name,
			L3Code: l3Code,
			L3Name: l3Name,
			TsCode: tsCode,
			Name: name,
			InDate: inDate,
			OutDate: outDate,
			IsNew: isNew,
		}
	}

	return items, nil
}
