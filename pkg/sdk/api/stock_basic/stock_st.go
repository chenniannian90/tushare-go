// Code generated by tushare-gen. DO NOT EDIT.

package stock_basic

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// StockStRequest 表示 ST股票列表 API 的请求
type StockStRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（格式：YYYYMMDD下同）
	StartDate string `json:"start_date,omitempty"` // 开始时间
	EndDate string `json:"end_date,omitempty"` // 结束时间
}

// StockStItem 表示单个 ST股票列表 数据项
type StockStItem struct {
	TsCode string `json:"ts_code"` // 股票代码
	Name string `json:"name"` // 股票名称
	TradeDate string `json:"trade_date"` // 交易日期
	Type string `json:"type"` // 类型
	TypeName string `json:"type_name"` // 类型名称
}

// StockSt 调用 ST股票列表 API
// 获取ST股票列表，可根据交易日期获取历史上每天的ST列表
// 注意：积分要求等使用限制请参考Tushare官方文档
func StockSt(ctx context.Context, client *sdk.Client, req *StockStRequest) ([]StockStItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "name", "trade_date", "type", "type_name"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "stock_st", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]StockStItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 type 的简单类型
		typeValue, ok := item["type"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 type 类型")
		}
		// 处理 type_name 的简单类型
		typeName, ok := item["type_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 type_name 类型")
		}
		items[i] = StockStItem{
			TsCode: tsCode,
			Name: name,
			TradeDate: tradeDate,
			Type: typeValue,
			TypeName: typeName,
		}
	}

	return items, nil
}
