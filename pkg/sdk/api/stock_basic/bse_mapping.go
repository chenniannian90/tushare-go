// Code generated by tushare-gen. DO NOT EDIT.

package stock_basic

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// BseMappingRequest 表示 北交所新旧代码对照 API 的请求
type BseMappingRequest struct {
	OCode string `json:"o_code,omitempty"` // 旧代码
	NCode string `json:"n_code,omitempty"` // 新代码
}

// BseMappingItem 表示单个 北交所新旧代码对照 数据项
type BseMappingItem struct {
	Name string `json:"name"` // 股票名称
	OCode string `json:"o_code"` // 原代码
	NCode string `json:"n_code"` // 新代码
	ListDate string `json:"list_date"` // 上市日期
}

// BseMapping 调用 北交所新旧代码对照 API
// 获取北交所股票代码变更后新旧代码映射表数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func BseMapping(ctx context.Context, client *sdk.Client, req *BseMappingRequest) ([]BseMappingItem, error) {
	params := map[string]interface{}{}
	if req.OCode != "" {
		params["o_code"] = req.OCode
	}
	if req.NCode != "" {
		params["n_code"] = req.NCode
	}

	fields := []string{"name", "o_code", "n_code", "list_date"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "bse_mapping", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]BseMappingItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 o_code 的简单类型
		oCode, ok := item["o_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 o_code 类型")
		}
		// 处理 n_code 的简单类型
		nCode, ok := item["n_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 n_code 类型")
		}
		// 处理 list_date 的简单类型
		listDate, ok := item["list_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 list_date 类型")
		}
		items[i] = BseMappingItem{
			Name: name,
			OCode: oCode,
			NCode: nCode,
			ListDate: listDate,
		}
	}

	return items, nil
}
