// Code generated by tushare-gen. DO NOT EDIT.

package stock_basic

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// NewShareRequest 表示 IPO新股上市 API 的请求
type NewShareRequest struct {
	StartDate string `json:"start_date,omitempty"` // 上网发行开始日期
	EndDate string `json:"end_date,omitempty"` // 上网发行结束日期
}

// NewShareItem 表示单个 IPO新股上市 数据项
type NewShareItem struct {
	TsCode string `json:"ts_code"` // TS股票代码
	SubCode string `json:"sub_code"` // 申购代码
	Name string `json:"name"` // 名称
	IpoDate string `json:"ipo_date"` // 上网发行日期
	IssueDate string `json:"issue_date"` // 上市日期
	Amount float64 `json:"amount"` // 发行总量（万股）
	MarketAmount float64 `json:"market_amount"` // 上网发行总量（万股）
	Price float64 `json:"price"` // 发行价格
	Pe float64 `json:"pe"` // 市盈率
	LimitAmount float64 `json:"limit_amount"` // 个人申购上限（万股）
	Funds float64 `json:"funds"` // 募集资金（亿元）
	Ballot float64 `json:"ballot"` // 中签率
}

// NewShare 调用 IPO新股上市 API
// 获取新股上市列表数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func NewShare(ctx context.Context, client *sdk.Client, req *NewShareRequest) ([]NewShareItem, error) {
	params := map[string]interface{}{}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "sub_code", "name", "ipo_date", "issue_date", "amount", "market_amount", "price", "pe", "limit_amount", "funds", "ballot"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "new_share", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]NewShareItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 sub_code 的简单类型
		// 对 string 类型尝试多种转换
		var subCode string
		if item["sub_code"] == nil {
			// 字段值为 null，使用零值
			subCode = ""
		} else if v, ok := item["sub_code"].(string); ok {
			subCode = v
		} else if v, ok := item["sub_code"].(float64); ok {
			subCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["sub_code"].(int); ok {
			subCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["sub_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: sub_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["sub_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 sub_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if item["name"] == nil {
			// 字段值为 null，使用零值
			name = ""
		} else if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 ipo_date 的简单类型
		// 对 string 类型尝试多种转换
		var ipoDate string
		if item["ipo_date"] == nil {
			// 字段值为 null，使用零值
			ipoDate = ""
		} else if v, ok := item["ipo_date"].(string); ok {
			ipoDate = v
		} else if v, ok := item["ipo_date"].(float64); ok {
			ipoDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ipo_date"].(int); ok {
			ipoDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ipo_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: ipo_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ipo_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ipo_date 类型")
		}
		// 处理 issue_date 的简单类型
		// 对 string 类型尝试多种转换
		var issueDate string
		if item["issue_date"] == nil {
			// 字段值为 null，使用零值
			issueDate = ""
		} else if v, ok := item["issue_date"].(string); ok {
			issueDate = v
		} else if v, ok := item["issue_date"].(float64); ok {
			issueDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["issue_date"].(int); ok {
			issueDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["issue_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: issue_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["issue_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 issue_date 类型")
		}
		// 处理 amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var amount float64
		if item["amount"] == nil {
			// 字段值为 null，使用零值
			amount = 0
		} else if v, ok := item["amount"].(float64); ok {
			amount = v
		} else if v, ok := item["amount"].(int); ok {
			amount = float64(v)
		} else if v, ok := item["amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				amount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					amount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: new_share")
					log.Printf("字段: amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 amount 类型，期望 float64/int/string")
		}
		// 处理 market_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var marketAmount float64
		if item["market_amount"] == nil {
			// 字段值为 null，使用零值
			marketAmount = 0
		} else if v, ok := item["market_amount"].(float64); ok {
			marketAmount = v
		} else if v, ok := item["market_amount"].(int); ok {
			marketAmount = float64(v)
		} else if v, ok := item["market_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				marketAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					marketAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["market_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: new_share")
					log.Printf("字段: market_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["market_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 market_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["market_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: market_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["market_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 market_amount 类型，期望 float64/int/string")
		}
		// 处理 price 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var price float64
		if item["price"] == nil {
			// 字段值为 null，使用零值
			price = 0
		} else if v, ok := item["price"].(float64); ok {
			price = v
		} else if v, ok := item["price"].(int); ok {
			price = float64(v)
		} else if v, ok := item["price"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				price = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					price = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["price"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: new_share")
					log.Printf("字段: price")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["price"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 price 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["price"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: price")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["price"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 price 类型，期望 float64/int/string")
		}
		// 处理 pe 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var pe float64
		if item["pe"] == nil {
			// 字段值为 null，使用零值
			pe = 0
		} else if v, ok := item["pe"].(float64); ok {
			pe = v
		} else if v, ok := item["pe"].(int); ok {
			pe = float64(v)
		} else if v, ok := item["pe"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				pe = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					pe = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["pe"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: new_share")
					log.Printf("字段: pe")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["pe"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 pe 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pe"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: pe")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pe"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pe 类型，期望 float64/int/string")
		}
		// 处理 limit_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var limitAmount float64
		if item["limit_amount"] == nil {
			// 字段值为 null，使用零值
			limitAmount = 0
		} else if v, ok := item["limit_amount"].(float64); ok {
			limitAmount = v
		} else if v, ok := item["limit_amount"].(int); ok {
			limitAmount = float64(v)
		} else if v, ok := item["limit_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				limitAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					limitAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["limit_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: new_share")
					log.Printf("字段: limit_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["limit_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 limit_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["limit_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: limit_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["limit_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 limit_amount 类型，期望 float64/int/string")
		}
		// 处理 funds 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var funds float64
		if item["funds"] == nil {
			// 字段值为 null，使用零值
			funds = 0
		} else if v, ok := item["funds"].(float64); ok {
			funds = v
		} else if v, ok := item["funds"].(int); ok {
			funds = float64(v)
		} else if v, ok := item["funds"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				funds = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					funds = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["funds"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: new_share")
					log.Printf("字段: funds")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["funds"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 funds 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["funds"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: funds")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["funds"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 funds 类型，期望 float64/int/string")
		}
		// 处理 ballot 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var ballot float64
		if item["ballot"] == nil {
			// 字段值为 null，使用零值
			ballot = 0
		} else if v, ok := item["ballot"].(float64); ok {
			ballot = v
		} else if v, ok := item["ballot"].(int); ok {
			ballot = float64(v)
		} else if v, ok := item["ballot"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				ballot = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					ballot = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["ballot"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: new_share")
					log.Printf("字段: ballot")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["ballot"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 ballot 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ballot"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: new_share")
			log.Printf("字段: ballot")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ballot"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ballot 类型，期望 float64/int/string")
		}
		items[i] = NewShareItem{
			TsCode: tsCode,
			SubCode: subCode,
			Name: name,
			IpoDate: ipoDate,
			IssueDate: issueDate,
			Amount: amount,
			MarketAmount: marketAmount,
			Price: price,
			Pe: pe,
			LimitAmount: limitAmount,
			Funds: funds,
			Ballot: ballot,
		}
	}

	return items, nil
}
