// Code generated by tushare-gen. DO NOT EDIT.

package wealth_fund_sales

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// FundSalesVolRequest 表示 销售机构公募基金销售保有规模 API 的请求
type FundSalesVolRequest struct {
	Year string `json:"year,omitempty"` // 年度
	Quarter string `json:"quarter,omitempty"` // 季度
	Name string `json:"name,omitempty"` // 机构名称
}

// FundSalesVolItem 表示单个 销售机构公募基金销售保有规模 数据项
type FundSalesVolItem struct {
	Year int `json:"year"` // 年度
	Quarter string `json:"quarter"` // 季度
	InstName string `json:"inst_name"` // 销售机构
	FundScale float64 `json:"fund_scale"` // 股票+混合公募基金保有规模（亿元）
	Scale float64 `json:"scale"` // 非货币市场公募基金保有规模（亿元）
	Rank int `json:"rank"` // 排名
}

// FundSalesVol 调用 销售机构公募基金销售保有规模 API
// 获取销售机构公募基金销售保有规模数据，本数据从2021年Q1开始公布，季度更新
// 注意：积分要求等使用限制请参考Tushare官方文档
func FundSalesVol(ctx context.Context, client *sdk.Client, req *FundSalesVolRequest) ([]FundSalesVolItem, error) {
	params := map[string]interface{}{}
	if req.Year != "" {
		params["year"] = req.Year
	}
	if req.Quarter != "" {
		params["quarter"] = req.Quarter
	}
	if req.Name != "" {
		params["name"] = req.Name
	}

	fields := []string{"year", "quarter", "inst_name", "fund_scale", "scale", "rank"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "fund_sales_vol", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]FundSalesVolItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 year 的简单类型
		year, ok := item["year"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 year 类型")
		}
		// 处理 quarter 的简单类型
		quarter, ok := item["quarter"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 quarter 类型")
		}
		// 处理 inst_name 的简单类型
		instName, ok := item["inst_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 inst_name 类型")
		}
		// 处理 fund_scale 的简单类型
		fundScale, ok := item["fund_scale"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 fund_scale 类型")
		}
		// 处理 scale 的简单类型
		scale, ok := item["scale"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 scale 类型")
		}
		// 处理 rank 的简单类型
		rank, ok := item["rank"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 rank 类型")
		}
		items[i] = FundSalesVolItem{
			Year: year,
			Quarter: quarter,
			InstName: instName,
			FundScale: fundScale,
			Scale: scale,
			Rank: rank,
		}
	}

	return items, nil
}
