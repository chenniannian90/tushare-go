// Code generated by tushare-gen. DO NOT EDIT.

package fund

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// FundBasicRequest 表示 基金列表 API 的请求
type FundBasicRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 基金代码
	Market string `json:"market,omitempty"` // 交易市场: E场内 O场外（默认E）
	Status string `json:"status,omitempty"` // 存续状态 D摘牌 I发行 L上市中
}

// FundBasicItem 表示单个 基金列表 数据项
type FundBasicItem struct {
	TsCode string `json:"ts_code"` // 基金代码
	Name string `json:"name"` // 简称
	Management string `json:"management"` // 管理人
	Custodian string `json:"custodian"` // 托管人
	FundType string `json:"fund_type"` // 投资类型
	FoundDate string `json:"found_date"` // 成立日期
	DueDate string `json:"due_date"` // 到期日期
	ListDate string `json:"list_date"` // 上市时间
	IssueDate string `json:"issue_date"` // 发行日期
	DelistDate string `json:"delist_date"` // 退市日期
	IssueAmount float64 `json:"issue_amount"` // 发行份额(亿)
	MFee float64 `json:"m_fee"` // 管理费
	CFee float64 `json:"c_fee"` // 托管费
	DurationYear float64 `json:"duration_year"` // 存续期
	PValue float64 `json:"p_value"` // 面值
	MinAmount float64 `json:"min_amount"` // 起点金额(万元)
	ExpReturn float64 `json:"exp_return"` // 预期收益率
	Benchmark string `json:"benchmark"` // 业绩比较基准
	Status string `json:"status"` // 存续状态D摘牌 I发行 L已上市
	InvestType string `json:"invest_type"` // 投资风格
	Type string `json:"type"` // 基金类型
	Trustee string `json:"trustee"` // 受托人
	PurcStartdate string `json:"purc_startdate"` // 日常申购起始日
	RedmStartdate string `json:"redm_startdate"` // 日常赎回起始日
	Market string `json:"market"` // E场内O场外
}

// FundBasic 调用 基金列表 API
// 获取公募基金数据列表，包括场内和场外基金
// 注意：积分要求等使用限制请参考Tushare官方文档
func FundBasic(ctx context.Context, client *sdk.Client, req *FundBasicRequest) ([]FundBasicItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.Market != "" {
		params["market"] = req.Market
	}
	if req.Status != "" {
		params["status"] = req.Status
	}

	fields := []string{"ts_code", "name", "management", "custodian", "fund_type", "found_date", "due_date", "list_date", "issue_date", "delist_date", "issue_amount", "m_fee", "c_fee", "duration_year", "p_value", "min_amount", "exp_return", "benchmark", "status", "invest_type", "type", "trustee", "purc_startdate", "redm_startdate", "market"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "fund_basic", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]FundBasicItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if item["name"] == nil {
			// 字段值为 null，使用零值
			name = ""
		} else if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 management 的简单类型
		// 对 string 类型尝试多种转换
		var management string
		if item["management"] == nil {
			// 字段值为 null，使用零值
			management = ""
		} else if v, ok := item["management"].(string); ok {
			management = v
		} else if v, ok := item["management"].(float64); ok {
			management = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["management"].(int); ok {
			management = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["management"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: management")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["management"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 management 类型")
		}
		// 处理 custodian 的简单类型
		// 对 string 类型尝试多种转换
		var custodian string
		if item["custodian"] == nil {
			// 字段值为 null，使用零值
			custodian = ""
		} else if v, ok := item["custodian"].(string); ok {
			custodian = v
		} else if v, ok := item["custodian"].(float64); ok {
			custodian = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["custodian"].(int); ok {
			custodian = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["custodian"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: custodian")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["custodian"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 custodian 类型")
		}
		// 处理 fund_type 的简单类型
		// 对 string 类型尝试多种转换
		var fundType string
		if item["fund_type"] == nil {
			// 字段值为 null，使用零值
			fundType = ""
		} else if v, ok := item["fund_type"].(string); ok {
			fundType = v
		} else if v, ok := item["fund_type"].(float64); ok {
			fundType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["fund_type"].(int); ok {
			fundType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["fund_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: fund_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["fund_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 fund_type 类型")
		}
		// 处理 found_date 的简单类型
		// 对 string 类型尝试多种转换
		var foundDate string
		if item["found_date"] == nil {
			// 字段值为 null，使用零值
			foundDate = ""
		} else if v, ok := item["found_date"].(string); ok {
			foundDate = v
		} else if v, ok := item["found_date"].(float64); ok {
			foundDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["found_date"].(int); ok {
			foundDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["found_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: found_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["found_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 found_date 类型")
		}
		// 处理 due_date 的简单类型
		// 对 string 类型尝试多种转换
		var dueDate string
		if item["due_date"] == nil {
			// 字段值为 null，使用零值
			dueDate = ""
		} else if v, ok := item["due_date"].(string); ok {
			dueDate = v
		} else if v, ok := item["due_date"].(float64); ok {
			dueDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["due_date"].(int); ok {
			dueDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["due_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: due_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["due_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 due_date 类型")
		}
		// 处理 list_date 的简单类型
		// 对 string 类型尝试多种转换
		var listDate string
		if item["list_date"] == nil {
			// 字段值为 null，使用零值
			listDate = ""
		} else if v, ok := item["list_date"].(string); ok {
			listDate = v
		} else if v, ok := item["list_date"].(float64); ok {
			listDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["list_date"].(int); ok {
			listDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["list_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: list_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["list_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 list_date 类型")
		}
		// 处理 issue_date 的简单类型
		// 对 string 类型尝试多种转换
		var issueDate string
		if item["issue_date"] == nil {
			// 字段值为 null，使用零值
			issueDate = ""
		} else if v, ok := item["issue_date"].(string); ok {
			issueDate = v
		} else if v, ok := item["issue_date"].(float64); ok {
			issueDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["issue_date"].(int); ok {
			issueDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["issue_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: issue_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["issue_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 issue_date 类型")
		}
		// 处理 delist_date 的简单类型
		// 对 string 类型尝试多种转换
		var delistDate string
		if item["delist_date"] == nil {
			// 字段值为 null，使用零值
			delistDate = ""
		} else if v, ok := item["delist_date"].(string); ok {
			delistDate = v
		} else if v, ok := item["delist_date"].(float64); ok {
			delistDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["delist_date"].(int); ok {
			delistDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["delist_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: delist_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["delist_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 delist_date 类型")
		}
		// 处理 issue_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var issueAmount float64
		if item["issue_amount"] == nil {
			// 字段值为 null，使用零值
			issueAmount = 0
		} else if v, ok := item["issue_amount"].(float64); ok {
			issueAmount = v
		} else if v, ok := item["issue_amount"].(int); ok {
			issueAmount = float64(v)
		} else if v, ok := item["issue_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				issueAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					issueAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["issue_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: fund_basic")
					log.Printf("字段: issue_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["issue_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 issue_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["issue_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: issue_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["issue_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 issue_amount 类型，期望 float64/int/string")
		}
		// 处理 m_fee 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var mFee float64
		if item["m_fee"] == nil {
			// 字段值为 null，使用零值
			mFee = 0
		} else if v, ok := item["m_fee"].(float64); ok {
			mFee = v
		} else if v, ok := item["m_fee"].(int); ok {
			mFee = float64(v)
		} else if v, ok := item["m_fee"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				mFee = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					mFee = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["m_fee"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: fund_basic")
					log.Printf("字段: m_fee")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["m_fee"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 m_fee 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["m_fee"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: m_fee")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["m_fee"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 m_fee 类型，期望 float64/int/string")
		}
		// 处理 c_fee 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var cFee float64
		if item["c_fee"] == nil {
			// 字段值为 null，使用零值
			cFee = 0
		} else if v, ok := item["c_fee"].(float64); ok {
			cFee = v
		} else if v, ok := item["c_fee"].(int); ok {
			cFee = float64(v)
		} else if v, ok := item["c_fee"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				cFee = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					cFee = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["c_fee"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: fund_basic")
					log.Printf("字段: c_fee")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["c_fee"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 c_fee 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["c_fee"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: c_fee")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["c_fee"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 c_fee 类型，期望 float64/int/string")
		}
		// 处理 duration_year 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var durationYear float64
		if item["duration_year"] == nil {
			// 字段值为 null，使用零值
			durationYear = 0
		} else if v, ok := item["duration_year"].(float64); ok {
			durationYear = v
		} else if v, ok := item["duration_year"].(int); ok {
			durationYear = float64(v)
		} else if v, ok := item["duration_year"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				durationYear = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					durationYear = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["duration_year"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: fund_basic")
					log.Printf("字段: duration_year")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["duration_year"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 duration_year 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["duration_year"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: duration_year")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["duration_year"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 duration_year 类型，期望 float64/int/string")
		}
		// 处理 p_value 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var pValue float64
		if item["p_value"] == nil {
			// 字段值为 null，使用零值
			pValue = 0
		} else if v, ok := item["p_value"].(float64); ok {
			pValue = v
		} else if v, ok := item["p_value"].(int); ok {
			pValue = float64(v)
		} else if v, ok := item["p_value"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				pValue = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					pValue = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["p_value"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: fund_basic")
					log.Printf("字段: p_value")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["p_value"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 p_value 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["p_value"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: p_value")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["p_value"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 p_value 类型，期望 float64/int/string")
		}
		// 处理 min_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var minAmount float64
		if item["min_amount"] == nil {
			// 字段值为 null，使用零值
			minAmount = 0
		} else if v, ok := item["min_amount"].(float64); ok {
			minAmount = v
		} else if v, ok := item["min_amount"].(int); ok {
			minAmount = float64(v)
		} else if v, ok := item["min_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				minAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					minAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["min_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: fund_basic")
					log.Printf("字段: min_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["min_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 min_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["min_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: min_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["min_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 min_amount 类型，期望 float64/int/string")
		}
		// 处理 exp_return 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var expReturn float64
		if item["exp_return"] == nil {
			// 字段值为 null，使用零值
			expReturn = 0
		} else if v, ok := item["exp_return"].(float64); ok {
			expReturn = v
		} else if v, ok := item["exp_return"].(int); ok {
			expReturn = float64(v)
		} else if v, ok := item["exp_return"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				expReturn = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					expReturn = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["exp_return"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: fund_basic")
					log.Printf("字段: exp_return")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["exp_return"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 exp_return 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["exp_return"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: exp_return")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["exp_return"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 exp_return 类型，期望 float64/int/string")
		}
		// 处理 benchmark 的简单类型
		// 对 string 类型尝试多种转换
		var benchmark string
		if item["benchmark"] == nil {
			// 字段值为 null，使用零值
			benchmark = ""
		} else if v, ok := item["benchmark"].(string); ok {
			benchmark = v
		} else if v, ok := item["benchmark"].(float64); ok {
			benchmark = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["benchmark"].(int); ok {
			benchmark = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["benchmark"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: benchmark")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["benchmark"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 benchmark 类型")
		}
		// 处理 status 的简单类型
		// 对 string 类型尝试多种转换
		var status string
		if item["status"] == nil {
			// 字段值为 null，使用零值
			status = ""
		} else if v, ok := item["status"].(string); ok {
			status = v
		} else if v, ok := item["status"].(float64); ok {
			status = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["status"].(int); ok {
			status = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["status"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: status")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["status"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 status 类型")
		}
		// 处理 invest_type 的简单类型
		// 对 string 类型尝试多种转换
		var investType string
		if item["invest_type"] == nil {
			// 字段值为 null，使用零值
			investType = ""
		} else if v, ok := item["invest_type"].(string); ok {
			investType = v
		} else if v, ok := item["invest_type"].(float64); ok {
			investType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["invest_type"].(int); ok {
			investType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["invest_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: invest_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["invest_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 invest_type 类型")
		}
		// 处理 type 的简单类型
		typeValue, ok := item["type"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 type 类型")
		}
		// 处理 trustee 的简单类型
		// 对 string 类型尝试多种转换
		var trustee string
		if item["trustee"] == nil {
			// 字段值为 null，使用零值
			trustee = ""
		} else if v, ok := item["trustee"].(string); ok {
			trustee = v
		} else if v, ok := item["trustee"].(float64); ok {
			trustee = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trustee"].(int); ok {
			trustee = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trustee"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: trustee")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trustee"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trustee 类型")
		}
		// 处理 purc_startdate 的简单类型
		// 对 string 类型尝试多种转换
		var purcStartdate string
		if item["purc_startdate"] == nil {
			// 字段值为 null，使用零值
			purcStartdate = ""
		} else if v, ok := item["purc_startdate"].(string); ok {
			purcStartdate = v
		} else if v, ok := item["purc_startdate"].(float64); ok {
			purcStartdate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["purc_startdate"].(int); ok {
			purcStartdate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["purc_startdate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: purc_startdate")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["purc_startdate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 purc_startdate 类型")
		}
		// 处理 redm_startdate 的简单类型
		// 对 string 类型尝试多种转换
		var redmStartdate string
		if item["redm_startdate"] == nil {
			// 字段值为 null，使用零值
			redmStartdate = ""
		} else if v, ok := item["redm_startdate"].(string); ok {
			redmStartdate = v
		} else if v, ok := item["redm_startdate"].(float64); ok {
			redmStartdate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["redm_startdate"].(int); ok {
			redmStartdate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["redm_startdate"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: redm_startdate")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["redm_startdate"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 redm_startdate 类型")
		}
		// 处理 market 的简单类型
		// 对 string 类型尝试多种转换
		var market string
		if item["market"] == nil {
			// 字段值为 null，使用零值
			market = ""
		} else if v, ok := item["market"].(string); ok {
			market = v
		} else if v, ok := item["market"].(float64); ok {
			market = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["market"].(int); ok {
			market = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["market"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: fund_basic")
			log.Printf("字段: market")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["market"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 market 类型")
		}
		items[i] = FundBasicItem{
			TsCode: tsCode,
			Name: name,
			Management: management,
			Custodian: custodian,
			FundType: fundType,
			FoundDate: foundDate,
			DueDate: dueDate,
			ListDate: listDate,
			IssueDate: issueDate,
			DelistDate: delistDate,
			IssueAmount: issueAmount,
			MFee: mFee,
			CFee: cFee,
			DurationYear: durationYear,
			PValue: pValue,
			MinAmount: minAmount,
			ExpReturn: expReturn,
			Benchmark: benchmark,
			Status: status,
			InvestType: investType,
			Type: typeValue,
			Trustee: trustee,
			PurcStartdate: purcStartdate,
			RedmStartdate: redmStartdate,
			Market: market,
		}
	}

	return items, nil
}
