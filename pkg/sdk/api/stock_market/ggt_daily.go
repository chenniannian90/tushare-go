// Code generated by tushare-gen. DO NOT EDIT.

package stock_market

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// GgtDailyRequest 表示 港股通每日成交统计 API 的请求
type GgtDailyRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期 （格式YYYYMMDD，下同。支持单日和多日输入）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// GgtDailyItem 表示单个 港股通每日成交统计 数据项
type GgtDailyItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	BuyAmount float64 `json:"buy_amount"` // 买入成交金额（亿元）
	BuyVolume float64 `json:"buy_volume"` // 买入成交笔数（万笔）
	SellAmount float64 `json:"sell_amount"` // 卖出成交金额（亿元）
	SellVolume float64 `json:"sell_volume"` // 卖出成交笔数（万笔）
}

// GgtDaily 调用 港股通每日成交统计 API
// 获取港股通每日成交信息，数据从2014年开始
// 注意：积分要求等使用限制请参考Tushare官方文档
func GgtDaily(ctx context.Context, client *sdk.Client, req *GgtDailyRequest) ([]GgtDailyItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"trade_date", "buy_amount", "buy_volume", "sell_amount", "sell_volume"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "ggt_daily", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]GgtDailyItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ggt_daily")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 buy_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyAmount float64
		if item["buy_amount"] == nil {
			// 字段值为 null，使用零值
			buyAmount = 0
		} else if v, ok := item["buy_amount"].(float64); ok {
			buyAmount = v
		} else if v, ok := item["buy_amount"].(int); ok {
			buyAmount = float64(v)
		} else if v, ok := item["buy_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: ggt_daily")
					log.Printf("字段: buy_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ggt_daily")
			log.Printf("字段: buy_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_amount 类型，期望 float64/int/string")
		}
		// 处理 buy_volume 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var buyVolume float64
		if item["buy_volume"] == nil {
			// 字段值为 null，使用零值
			buyVolume = 0
		} else if v, ok := item["buy_volume"].(float64); ok {
			buyVolume = v
		} else if v, ok := item["buy_volume"].(int); ok {
			buyVolume = float64(v)
		} else if v, ok := item["buy_volume"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				buyVolume = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					buyVolume = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["buy_volume"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: ggt_daily")
					log.Printf("字段: buy_volume")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["buy_volume"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 buy_volume 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["buy_volume"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ggt_daily")
			log.Printf("字段: buy_volume")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["buy_volume"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 buy_volume 类型，期望 float64/int/string")
		}
		// 处理 sell_amount 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var sellAmount float64
		if item["sell_amount"] == nil {
			// 字段值为 null，使用零值
			sellAmount = 0
		} else if v, ok := item["sell_amount"].(float64); ok {
			sellAmount = v
		} else if v, ok := item["sell_amount"].(int); ok {
			sellAmount = float64(v)
		} else if v, ok := item["sell_amount"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				sellAmount = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					sellAmount = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["sell_amount"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: ggt_daily")
					log.Printf("字段: sell_amount")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["sell_amount"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 sell_amount 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["sell_amount"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ggt_daily")
			log.Printf("字段: sell_amount")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["sell_amount"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 sell_amount 类型，期望 float64/int/string")
		}
		// 处理 sell_volume 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var sellVolume float64
		if item["sell_volume"] == nil {
			// 字段值为 null，使用零值
			sellVolume = 0
		} else if v, ok := item["sell_volume"].(float64); ok {
			sellVolume = v
		} else if v, ok := item["sell_volume"].(int); ok {
			sellVolume = float64(v)
		} else if v, ok := item["sell_volume"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				sellVolume = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					sellVolume = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["sell_volume"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: ggt_daily")
					log.Printf("字段: sell_volume")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["sell_volume"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 sell_volume 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["sell_volume"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: ggt_daily")
			log.Printf("字段: sell_volume")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["sell_volume"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 sell_volume 类型，期望 float64/int/string")
		}
		items[i] = GgtDailyItem{
			TradeDate: tradeDate,
			BuyAmount: buyAmount,
			BuyVolume: buyVolume,
			SellAmount: sellAmount,
			SellVolume: sellVolume,
		}
	}

	return items, nil
}
