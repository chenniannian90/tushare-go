// Code generated by tushare-gen. DO NOT EDIT.

package stock_market

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// StkLimitRequest 表示 每日涨跌停价格 API 的请求
type StkLimitRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// StkLimitItem 表示单个 每日涨跌停价格 数据项
type StkLimitItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	TsCode string `json:"ts_code"` // TS股票代码
	PreClose float64 `json:"pre_close"` // 昨日收盘价
	UpLimit float64 `json:"up_limit"` // 涨停价
	DownLimit float64 `json:"down_limit"` // 跌停价
}

// StkLimit 调用 每日涨跌停价格 API
// 获取全市场（包含A/B股和基金）每日涨跌停价格，包括涨停价格，跌停价格等，每个交易日8点40左右更新当日股票涨跌停价格。
// 注意：积分要求等使用限制请参考Tushare官方文档
func StkLimit(ctx context.Context, client *sdk.Client, req *StkLimitRequest) ([]StkLimitItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"trade_date", "ts_code", "pre_close", "up_limit", "down_limit"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "stk_limit", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]StkLimitItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if item["trade_date"] == nil {
			// 字段值为 null，使用零值
			tradeDate = ""
		} else if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_limit")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if item["ts_code"] == nil {
			// 字段值为 null，使用零值
			tsCode = ""
		} else if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_limit")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 pre_close 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var preClose float64
		if item["pre_close"] == nil {
			// 字段值为 null，使用零值
			preClose = 0
		} else if v, ok := item["pre_close"].(float64); ok {
			preClose = v
		} else if v, ok := item["pre_close"].(int); ok {
			preClose = float64(v)
		} else if v, ok := item["pre_close"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				preClose = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					preClose = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["pre_close"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: stk_limit")
					log.Printf("字段: pre_close")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["pre_close"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 pre_close 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pre_close"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_limit")
			log.Printf("字段: pre_close")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pre_close"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pre_close 类型，期望 float64/int/string")
		}
		// 处理 up_limit 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var upLimit float64
		if item["up_limit"] == nil {
			// 字段值为 null，使用零值
			upLimit = 0
		} else if v, ok := item["up_limit"].(float64); ok {
			upLimit = v
		} else if v, ok := item["up_limit"].(int); ok {
			upLimit = float64(v)
		} else if v, ok := item["up_limit"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				upLimit = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					upLimit = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["up_limit"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: stk_limit")
					log.Printf("字段: up_limit")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["up_limit"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 up_limit 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["up_limit"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_limit")
			log.Printf("字段: up_limit")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["up_limit"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 up_limit 类型，期望 float64/int/string")
		}
		// 处理 down_limit 的简单类型
		// 处理 float64 类型 - 支持多种输入格式
		var downLimit float64
		if item["down_limit"] == nil {
			// 字段值为 null，使用零值
			downLimit = 0
		} else if v, ok := item["down_limit"].(float64); ok {
			downLimit = v
		} else if v, ok := item["down_limit"].(int); ok {
			downLimit = float64(v)
		} else if v, ok := item["down_limit"].(string); ok {
			// 尝试解析字符串
			if v == "" {
				downLimit = 0
			} else {
				// 使用 fmt.Sscanf 解析字符串
				var parsed float64
				if _, err := fmt.Sscanf(v, "%f", &parsed); err == nil {
					downLimit = parsed
				} else {
					itemJSON, _ := json.Marshal(item)
					fieldJSON, _ := json.Marshal(item["down_limit"])
					log.Printf("=== 字段解析失败 ===")
					log.Printf("API: stk_limit")
					log.Printf("字段: down_limit")
					log.Printf("错误: 无法解析字符串为 float64")
					log.Printf("字段原始值: %s", string(fieldJSON))
					log.Printf("字段实际类型: %T", item["down_limit"])
					log.Printf("当前Item: %s", string(itemJSON))
					log.Printf("===================")
					return nil, fmt.Errorf("无效的 down_limit 类型: 无法解析字符串 %q", v)
				}
			}
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["down_limit"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_limit")
			log.Printf("字段: down_limit")
			log.Printf("错误: 类型转换失败，期望类型 float64，支持 float64/int/string")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["down_limit"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 down_limit 类型，期望 float64/int/string")
		}
		items[i] = StkLimitItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			PreClose: preClose,
			UpLimit: upLimit,
			DownLimit: downLimit,
		}
	}

	return items, nil
}
