// Code generated by tushare-gen. DO NOT EDIT.

package macro_economy

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// CnGdpRequest 表示 国内生产总值（GDP） API 的请求
type CnGdpRequest struct {
	Q string `json:"q,omitempty"` // 季度（2019Q1表示，2019年第一季度）
	StartQ string `json:"start_q,omitempty"` // 开始季度
	EndQ string `json:"end_q,omitempty"` // 结束季度
	Fields string `json:"fields,omitempty"` // 指定输出字段（e.g. fields='quarter,gdp,gdp_yoy'）
}

// CnGdpItem 表示单个 国内生产总值（GDP） 数据项
type CnGdpItem struct {
	Quarter string `json:"quarter"` // 季度
	Gdp float64 `json:"gdp"` // GDP累计值（亿元）
	GdpYoy float64 `json:"gdp_yoy"` // 当季同比增速（%）
	Pi float64 `json:"pi"` // 第一产业累计值（亿元）
	PiYoy float64 `json:"pi_yoy"` // 第一产业同比增速（%）
	Si float64 `json:"si"` // 第二产业累计值（亿元）
	SiYoy float64 `json:"si_yoy"` // 第二产业同比增速（%）
	Ti float64 `json:"ti"` // 第三产业累计值（亿元）
	TiYoy float64 `json:"ti_yoy"` // 第三产业同比增速（%）
}

// CnGdp 调用 国内生产总值（GDP） API
// 获取国民经济之GDP数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func CnGdp(ctx context.Context, client *sdk.Client, req *CnGdpRequest) ([]CnGdpItem, error) {
	params := map[string]interface{}{}
	if req.Q != "" {
		params["q"] = req.Q
	}
	if req.StartQ != "" {
		params["start_q"] = req.StartQ
	}
	if req.EndQ != "" {
		params["end_q"] = req.EndQ
	}
	if req.Fields != "" {
		params["fields"] = req.Fields
	}

	fields := []string{"quarter", "gdp", "gdp_yoy", "pi", "pi_yoy", "si", "si_yoy", "ti", "ti_yoy"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "cn_gdp", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]CnGdpItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 quarter 的简单类型
		// 对 string 类型尝试多种转换
		var quarter string
		if item["quarter"] == nil {
			// 字段值为 null，使用零值
			quarter = ""
		} else if v, ok := item["quarter"].(string); ok {
			quarter = v
		} else if v, ok := item["quarter"].(float64); ok {
			quarter = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["quarter"].(int); ok {
			quarter = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["quarter"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: cn_gdp")
			log.Printf("字段: quarter")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["quarter"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 quarter 类型")
		}
		// 处理 gdp 的简单类型
		gdp, ok := item["gdp"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 gdp 类型")
		}
		// 处理 gdp_yoy 的简单类型
		gdpYoy, ok := item["gdp_yoy"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 gdp_yoy 类型")
		}
		// 处理 pi 的简单类型
		pi, ok := item["pi"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pi 类型")
		}
		// 处理 pi_yoy 的简单类型
		piYoy, ok := item["pi_yoy"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pi_yoy 类型")
		}
		// 处理 si 的简单类型
		si, ok := item["si"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 si 类型")
		}
		// 处理 si_yoy 的简单类型
		siYoy, ok := item["si_yoy"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 si_yoy 类型")
		}
		// 处理 ti 的简单类型
		ti, ok := item["ti"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ti 类型")
		}
		// 处理 ti_yoy 的简单类型
		tiYoy, ok := item["ti_yoy"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ti_yoy 类型")
		}
		items[i] = CnGdpItem{
			Quarter: quarter,
			Gdp: gdp,
			GdpYoy: gdpYoy,
			Pi: pi,
			PiYoy: piYoy,
			Si: si,
			SiYoy: siYoy,
			Ti: ti,
			TiYoy: tiYoy,
		}
	}

	return items, nil
}
