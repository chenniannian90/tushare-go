// Code generated by tushare-gen. DO NOT EDIT.

package stock_financial

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// DisclosureDateRequest 表示 财报披露日期表 API 的请求
type DisclosureDateRequest struct {
	TsCode string `json:"ts_code,omitempty"`
	EndDate string `json:"end_date,omitempty"`
	PreDate string `json:"pre_date,omitempty"`
	AnnDate string `json:"ann_date,omitempty"`
	ActualDate string `json:"actual_date,omitempty"`
}

// DisclosureDateItem 表示单个 财报披露日期表 数据项
type DisclosureDateItem struct {
	TsCode string `json:"ts_code"`
	AnnDate string `json:"ann_date"`
	EndDate string `json:"end_date"`
	PreDate string `json:"pre_date"`
	ActualDate string `json:"actual_date"`
	ModifyDate string `json:"modify_date"`
}

// DisclosureDate 调用 财报披露日期表 API
func DisclosureDate(ctx context.Context, client *sdk.Client, req *DisclosureDateRequest) ([]DisclosureDateItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.PreDate != "" {
		params["pre_date"] = req.PreDate
	}
	if req.AnnDate != "" {
		params["ann_date"] = req.AnnDate
	}
	if req.ActualDate != "" {
		params["actual_date"] = req.ActualDate
	}

	fields := []string{"ts_code", "ann_date", "end_date", "pre_date", "actual_date", "modify_date"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "disclosure_date", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]DisclosureDateItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 ann_date 的简单类型
		annDate, ok := item["ann_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ann_date 类型")
		}
		// 处理 end_date 的简单类型
		endDate, ok := item["end_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 end_date 类型")
		}
		// 处理 pre_date 的简单类型
		preDate, ok := item["pre_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 pre_date 类型")
		}
		// 处理 actual_date 的简单类型
		actualDate, ok := item["actual_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 actual_date 类型")
		}
		// 处理 modify_date 的简单类型
		modifyDate, ok := item["modify_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 modify_date 类型")
		}
		items[i] = DisclosureDateItem{
			TsCode: tsCode,
			AnnDate: annDate,
			EndDate: endDate,
			PreDate: preDate,
			ActualDate: actualDate,
			ModifyDate: modifyDate,
		}
	}

	return items, nil
}
