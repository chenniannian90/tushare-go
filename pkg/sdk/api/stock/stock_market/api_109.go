// Code generated by tushare-gen. DO NOT EDIT.

package stock_market

import (
	"context"

	"tushare-go/pkg/sdk"
)

// Api109Request 表示 通用行情接口 API 的请求
type Api109Request struct {
	TsCode string `json:"ts_code,omitempty"` // 证券代码，不支持多值输入，多值输入获取结果会有重复记录
	StartDate string `json:"start_date,omitempty"` // 开始日期 (日线格式：YYYYMMDD，提取分钟数据请用2019-09-01 09:00:00这种格式)
	EndDate string `json:"end_date,omitempty"` // 结束日期 (日线格式：YYYYMMDD)
	Asset string `json:"asset,omitempty"` // 资产类别：E股票 I沪深指数 C数字货币 FT期货 FD基金 O期权 CB可转债（v1.2.39），默认E
	Adj string `json:"adj,omitempty"` // 复权类型(只针对股票)：None未复权 qfq前复权 hfq后复权 , 默认None，目前只支持日线复权，同时复权机制是根据设定的end_date参数动态复权，采用分红再投模式，具体请参考常见问题列表里的说明。
	Freq string `json:"freq,omitempty"` // 数据频度 ：支持分钟(min)/日(D)/周(W)/月(M)K线，其中1min表示1分钟（类推1/5/15/30/60分钟） ，默认D。对于分钟数据有600积分用户可以试用（请求2次），正式权限可以参考权限列表说明 ，使用方法请参考股票分钟使用方法。
	Ma string `json:"ma,omitempty"` // 均线，支持任意合理int数值。注：均线是动态计算，要设置一定时间范围才能获得相应的均线，比如5日均线，开始和结束日期参数跨度必须要超过5日。目前只支持单一个股票提取均线，即需要输入ts_code参数。e.g: ma_5表示5日均价，ma_v_5表示5日均量
	Factors string `json:"factors,omitempty"` // 股票因子（asset='E'有效）支持 tor换手率 vr量比
	Adjfactor string `json:"adjfactor,omitempty"` // 复权因子，在复权数据时，如果此参数为True，返回的数据中则带复权因子，默认为False。 该功能从1.2.33版本开始生效
}

// Api109Item 表示单个 通用行情接口 数据项
type Api109Item struct {
}

// Api109 调用 通用行情接口 API
func Api109(ctx context.Context, client *sdk.Client, req *Api109Request) ([]Api109Item, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}
	if req.Asset != "" {
		params["asset"] = req.Asset
	}
	if req.Adj != "" {
		params["adj"] = req.Adj
	}
	if req.Freq != "" {
		params["freq"] = req.Freq
	}
	if req.Ma != "" {
		params["ma"] = req.Ma
	}
	if req.Factors != "" {
		params["factors"] = req.Factors
	}
	if req.Adjfactor != "" {
		params["adjfactor"] = req.Adjfactor
	}

	fields := []string{}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "api_109", params, fields, &result); err != nil {
		return nil, err
	}
	// No response fields defined, return empty items
	return []Api109Item{}, nil
}
