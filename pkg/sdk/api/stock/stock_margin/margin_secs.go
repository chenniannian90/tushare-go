// Code generated by tushare-gen. DO NOT EDIT.

package stock_margin

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// MarginSecsRequest 表示 融资融券标的（盘前） API 的请求
type MarginSecsRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 标的代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日
	Exchange string `json:"exchange,omitempty"` // 交易所（SSE上交所 SZSE深交所 BSE北交所）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// MarginSecsItem 表示单个 融资融券标的（盘前） 数据项
type MarginSecsItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	TsCode string `json:"ts_code"` // 标的代码
	Name string `json:"name"` // 标的名称
	Exchange string `json:"exchange"` // 交易所
}

// MarginSecs 调用 融资融券标的（盘前） API
// 获取沪深京三大交易所融资融券标的（包括ETF），每天盘前更新
// 注意：积分要求等使用限制请参考Tushare官方文档
func MarginSecs(ctx context.Context, client *sdk.Client, req *MarginSecsRequest) ([]MarginSecsItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.Exchange != "" {
		params["exchange"] = req.Exchange
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"trade_date", "ts_code", "name", "exchange"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "margin_secs", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]MarginSecsItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 exchange 的简单类型
		exchange, ok := item["exchange"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 exchange 类型")
		}
		items[i] = MarginSecsItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			Name: name,
			Exchange: exchange,
		}
	}

	return items, nil
}
