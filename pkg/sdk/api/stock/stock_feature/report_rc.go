// Code generated by tushare-gen. DO NOT EDIT.

package stock_feature

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// ReportRcRequest 表示 券商盈利预测数据 API 的请求
type ReportRcRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	ReportDate string `json:"report_date,omitempty"` // 报告日期
	StartDate string `json:"start_date,omitempty"` // 报告开始日期
	EndDate string `json:"end_date,omitempty"` // 报告结束日期
}

// ReportRcItem 表示单个 券商盈利预测数据 数据项
type ReportRcItem struct {
	TsCode string `json:"ts_code"` // 股票代码
	Name string `json:"name"` // 股票名称
	ReportDate string `json:"report_date"` // 研报日期
	ReportTitle string `json:"report_title"` // 报告标题
	ReportType string `json:"report_type"` // 报告类型
	Classify string `json:"classify"` // 报告分类
	OrgName string `json:"org_name"` // 机构名称
	AuthorName string `json:"author_name"` // 作者
	Quarter string `json:"quarter"` // 预测报告期
	OpRt float64 `json:"op_rt"` // 预测营业收入（万元）
	OpPr float64 `json:"op_pr"` // 预测营业利润（万元）
	Tp float64 `json:"tp"` // 预测利润总额（万元）
	Np float64 `json:"np"` // 预测净利润（万元）
	Eps float64 `json:"eps"` // 预测每股收益（元）
	Pe float64 `json:"pe"` // 预测市盈率
	Rd float64 `json:"rd"` // 预测股息率
	Roe float64 `json:"roe"` // 预测净资产收益率
	EvEbitda float64 `json:"ev_ebitda"` // 预测EV/EBITDA
	Rating string `json:"rating"` // 卖方评级
	MaxPrice float64 `json:"max_price"` // 预测最高目标价
	MinPrice float64 `json:"min_price"` // 预测最低目标价
	ImpDg string `json:"imp_dg"` // 机构关注度
	CreateTime string `json:"create_time"` // TS数据更新时间
}

// ReportRc 调用 券商盈利预测数据 API
// 获取券商（卖方）每天研报的盈利预测数据，数据从2010年开始，每晚19~22点更新当日数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func ReportRc(ctx context.Context, client *sdk.Client, req *ReportRcRequest) ([]ReportRcItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.ReportDate != "" {
		params["report_date"] = req.ReportDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "name", "report_date", "report_title", "report_type", "classify", "org_name", "author_name", "quarter", "op_rt", "op_pr", "tp", "np", "eps", "pe", "rd", "roe", "ev_ebitda", "rating", "max_price", "min_price", "imp_dg", "create_time"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "report_rc", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]ReportRcItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 report_date 的简单类型
		// 对 string 类型尝试多种转换
		var reportDate string
		if v, ok := item["report_date"].(string); ok {
			reportDate = v
		} else if v, ok := item["report_date"].(float64); ok {
			reportDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["report_date"].(int); ok {
			reportDate = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 report_date 类型")
		}
		// 处理 report_title 的简单类型
		// 对 string 类型尝试多种转换
		var reportTitle string
		if v, ok := item["report_title"].(string); ok {
			reportTitle = v
		} else if v, ok := item["report_title"].(float64); ok {
			reportTitle = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["report_title"].(int); ok {
			reportTitle = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 report_title 类型")
		}
		// 处理 report_type 的简单类型
		// 对 string 类型尝试多种转换
		var reportType string
		if v, ok := item["report_type"].(string); ok {
			reportType = v
		} else if v, ok := item["report_type"].(float64); ok {
			reportType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["report_type"].(int); ok {
			reportType = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 report_type 类型")
		}
		// 处理 classify 的简单类型
		// 对 string 类型尝试多种转换
		var classify string
		if v, ok := item["classify"].(string); ok {
			classify = v
		} else if v, ok := item["classify"].(float64); ok {
			classify = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["classify"].(int); ok {
			classify = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 classify 类型")
		}
		// 处理 org_name 的简单类型
		// 对 string 类型尝试多种转换
		var orgName string
		if v, ok := item["org_name"].(string); ok {
			orgName = v
		} else if v, ok := item["org_name"].(float64); ok {
			orgName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["org_name"].(int); ok {
			orgName = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 org_name 类型")
		}
		// 处理 author_name 的简单类型
		// 对 string 类型尝试多种转换
		var authorName string
		if v, ok := item["author_name"].(string); ok {
			authorName = v
		} else if v, ok := item["author_name"].(float64); ok {
			authorName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["author_name"].(int); ok {
			authorName = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 author_name 类型")
		}
		// 处理 quarter 的简单类型
		// 对 string 类型尝试多种转换
		var quarter string
		if v, ok := item["quarter"].(string); ok {
			quarter = v
		} else if v, ok := item["quarter"].(float64); ok {
			quarter = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["quarter"].(int); ok {
			quarter = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 quarter 类型")
		}
		// 处理 op_rt 的简单类型
		opRt, ok := item["op_rt"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 op_rt 类型")
		}
		// 处理 op_pr 的简单类型
		opPr, ok := item["op_pr"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 op_pr 类型")
		}
		// 处理 tp 的简单类型
		tp, ok := item["tp"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 tp 类型")
		}
		// 处理 np 的简单类型
		np, ok := item["np"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 np 类型")
		}
		// 处理 eps 的简单类型
		eps, ok := item["eps"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 eps 类型")
		}
		// 处理 pe 的简单类型
		pe, ok := item["pe"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pe 类型")
		}
		// 处理 rd 的简单类型
		rd, ok := item["rd"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 rd 类型")
		}
		// 处理 roe 的简单类型
		roe, ok := item["roe"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 roe 类型")
		}
		// 处理 ev_ebitda 的简单类型
		evEbitda, ok := item["ev_ebitda"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ev_ebitda 类型")
		}
		// 处理 rating 的简单类型
		// 对 string 类型尝试多种转换
		var rating string
		if v, ok := item["rating"].(string); ok {
			rating = v
		} else if v, ok := item["rating"].(float64); ok {
			rating = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["rating"].(int); ok {
			rating = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 rating 类型")
		}
		// 处理 max_price 的简单类型
		maxPrice, ok := item["max_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 max_price 类型")
		}
		// 处理 min_price 的简单类型
		minPrice, ok := item["min_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 min_price 类型")
		}
		// 处理 imp_dg 的简单类型
		// 对 string 类型尝试多种转换
		var impDg string
		if v, ok := item["imp_dg"].(string); ok {
			impDg = v
		} else if v, ok := item["imp_dg"].(float64); ok {
			impDg = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["imp_dg"].(int); ok {
			impDg = fmt.Sprintf("%d", v)
		} else {
			return nil, fmt.Errorf("无效的 imp_dg 类型")
		}
		// 处理 create_time 的简单类型
		createTime, ok := item["create_time"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 create_time 类型")
		}
		items[i] = ReportRcItem{
			TsCode: tsCode,
			Name: name,
			ReportDate: reportDate,
			ReportTitle: reportTitle,
			ReportType: reportType,
			Classify: classify,
			OrgName: orgName,
			AuthorName: authorName,
			Quarter: quarter,
			OpRt: opRt,
			OpPr: opPr,
			Tp: tp,
			Np: np,
			Eps: eps,
			Pe: pe,
			Rd: rd,
			Roe: roe,
			EvEbitda: evEbitda,
			Rating: rating,
			MaxPrice: maxPrice,
			MinPrice: minPrice,
			ImpDg: impDg,
			CreateTime: createTime,
		}
	}

	return items, nil
}
