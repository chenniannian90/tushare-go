// Code generated by tushare-gen. DO NOT EDIT.

package stock_feature

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// CyqChipsRequest 表示 每日筹码分布 API 的请求
type CyqChipsRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（YYYYMMDD）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// CyqChipsItem 表示单个 每日筹码分布 数据项
type CyqChipsItem struct {
	TsCode string `json:"ts_code"` // 股票代码
	TradeDate string `json:"trade_date"` // 交易日期
	Price float64 `json:"price"` // 成本价格
	Percent float64 `json:"percent"` // 价格占比（%）
}

// CyqChips 调用 每日筹码分布 API
// 获取A股每日的筹码分布情况，提供各价位占比，数据从2018年开始，每天18~19点之间更新当日数据 来源：Tushare社区
// 注意：积分要求等使用限制请参考Tushare官方文档
func CyqChips(ctx context.Context, client *sdk.Client, req *CyqChipsRequest) ([]CyqChipsItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "price", "percent"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "cyq_chips", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]CyqChipsItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 price 的简单类型
		price, ok := item["price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 price 类型")
		}
		// 处理 percent 的简单类型
		percent, ok := item["percent"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 percent 类型")
		}
		items[i] = CyqChipsItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			Price: price,
			Percent: percent,
		}
	}

	return items, nil
}
