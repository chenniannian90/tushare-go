// Code generated by tushare-gen. DO NOT EDIT.

package stock_feature

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// StkAhComparisonRequest 表示 AH股比价 API 的请求
type StkAhComparisonRequest struct {
	HkCode string `json:"hk_code,omitempty"` // 港股股票代码（xxxxx.HK)
	TsCode string `json:"ts_code,omitempty"` // A股票代码(xxxxxx.SH/SZ/BJ)
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（格式：YYYYMMDD下同）
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// StkAhComparisonItem 表示单个 AH股比价 数据项
type StkAhComparisonItem struct {
	HkCode string `json:"hk_code"` // 港股股票代码
	TsCode string `json:"ts_code"` // A股股票代码
	TradeDate string `json:"trade_date"` // 交易日期
	HkName string `json:"hk_name"` // 港股股票名称
	HkPctChg float64 `json:"hk_pct_chg"` // 港股股票涨跌幅
	HkClose float64 `json:"hk_close"` // 港股股票收盘价
	Name string `json:"name"` // A股股票名称
	Close float64 `json:"close"` // A股股票收盘价
	PctChg float64 `json:"pct_chg"` // A股股票涨跌幅
	AhComparison float64 `json:"ah_comparison"` // 比价(A/H)
	AhPremium float64 `json:"ah_premium"` // 溢价(A/H)%
}

// StkAhComparison 调用 AH股比价 API
// AH股比价数据，可根据交易日期获取历史
// 注意：积分要求等使用限制请参考Tushare官方文档
func StkAhComparison(ctx context.Context, client *sdk.Client, req *StkAhComparisonRequest) ([]StkAhComparisonItem, error) {
	params := map[string]interface{}{}
	if req.HkCode != "" {
		params["hk_code"] = req.HkCode
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"hk_code", "ts_code", "trade_date", "hk_name", "hk_pct_chg", "hk_close", "name", "close", "pct_chg", "ah_comparison", "ah_premium"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "stk_ah_comparison", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]StkAhComparisonItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 hk_code 的简单类型
		// 对 string 类型尝试多种转换
		var hkCode string
		if v, ok := item["hk_code"].(string); ok {
			hkCode = v
		} else if v, ok := item["hk_code"].(float64); ok {
			hkCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["hk_code"].(int); ok {
			hkCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["hk_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_ah_comparison")
			log.Printf("字段: hk_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["hk_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 hk_code 类型")
		}
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_ah_comparison")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_ah_comparison")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 hk_name 的简单类型
		// 对 string 类型尝试多种转换
		var hkName string
		if v, ok := item["hk_name"].(string); ok {
			hkName = v
		} else if v, ok := item["hk_name"].(float64); ok {
			hkName = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["hk_name"].(int); ok {
			hkName = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["hk_name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_ah_comparison")
			log.Printf("字段: hk_name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["hk_name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 hk_name 类型")
		}
		// 处理 hk_pct_chg 的简单类型
		hkPctChg, ok := item["hk_pct_chg"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 hk_pct_chg 类型")
		}
		// 处理 hk_close 的简单类型
		hkClose, ok := item["hk_close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 hk_close 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: stk_ah_comparison")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 close 的简单类型
		close, ok := item["close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close 类型")
		}
		// 处理 pct_chg 的简单类型
		pctChg, ok := item["pct_chg"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_chg 类型")
		}
		// 处理 ah_comparison 的简单类型
		ahComparison, ok := item["ah_comparison"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ah_comparison 类型")
		}
		// 处理 ah_premium 的简单类型
		ahPremium, ok := item["ah_premium"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ah_premium 类型")
		}
		items[i] = StkAhComparisonItem{
			HkCode: hkCode,
			TsCode: tsCode,
			TradeDate: tradeDate,
			HkName: hkName,
			HkPctChg: hkPctChg,
			HkClose: hkClose,
			Name: name,
			Close: close,
			PctChg: pctChg,
			AhComparison: ahComparison,
			AhPremium: ahPremium,
		}
	}

	return items, nil
}
