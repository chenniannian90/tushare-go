// Code generated by tushare-gen. DO NOT EDIT.

package stock_feature

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// StkAuctionCRequest 表示 股票收盘集合竞价数据 API 的请求
type StkAuctionCRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期(YYYYMMDD)
	StartDate string `json:"start_date,omitempty"` // 开始日期(YYYYMMDD)
	EndDate string `json:"end_date,omitempty"` // 结束日期(YYYYMMDD)
}

// StkAuctionCItem 表示单个 股票收盘集合竞价数据 数据项
type StkAuctionCItem struct {
	TsCode string `json:"ts_code"` // 股票代码
	TradeDate string `json:"trade_date"` // 交易日期
	Close float64 `json:"close"` // 收盘集合竞价收盘价
	Open float64 `json:"open"` // 收盘集合竞价开盘价
	High float64 `json:"high"` // 收盘集合竞价最高价
	Low float64 `json:"low"` // 收盘集合竞价最低价
	Vol float64 `json:"vol"` // 收盘集合竞价成交量
	Amount float64 `json:"amount"` // 收盘集合竞价成交额
	Vwap float64 `json:"vwap"` // 收盘集合竞价均价
}

// StkAuctionC 调用 股票收盘集合竞价数据 API
// 股票收盘15:00集合竞价数据，每天盘后更新
// 注意：积分要求等使用限制请参考Tushare官方文档
func StkAuctionC(ctx context.Context, client *sdk.Client, req *StkAuctionCRequest) ([]StkAuctionCItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "close", "open", "high", "low", "vol", "amount", "vwap"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "stk_auction_c", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]StkAuctionCItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 close 的简单类型
		close, ok := item["close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close 类型")
		}
		// 处理 open 的简单类型
		open, ok := item["open"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 open 类型")
		}
		// 处理 high 的简单类型
		high, ok := item["high"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 high 类型")
		}
		// 处理 low 的简单类型
		low, ok := item["low"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 low 类型")
		}
		// 处理 vol 的简单类型
		vol, ok := item["vol"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 vol 类型")
		}
		// 处理 amount 的简单类型
		amount, ok := item["amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amount 类型")
		}
		// 处理 vwap 的简单类型
		vwap, ok := item["vwap"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 vwap 类型")
		}
		items[i] = StkAuctionCItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			Close: close,
			Open: open,
			High: high,
			Low: low,
			Vol: vol,
			Amount: amount,
			Vwap: vwap,
		}
	}

	return items, nil
}
