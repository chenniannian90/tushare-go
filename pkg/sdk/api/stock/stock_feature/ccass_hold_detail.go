// Code generated by tushare-gen. DO NOT EDIT.

package stock_feature

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// CcassHoldDetailRequest 表示 中央结算系统持股明细 API 的请求
type CcassHoldDetailRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 股票代码 (e.g. 605009.SH)
	HkCode string `json:"hk_code,omitempty"` // 港交所代码 （e.g. 95009）
	TradeDate string `json:"trade_date,omitempty"` // 交易日期(YYYYMMDD格式，下同)
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// CcassHoldDetailItem 表示单个 中央结算系统持股明细 数据项
type CcassHoldDetailItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	TsCode string `json:"ts_code"` // 股票代号
	Name string `json:"name"` // 股票名称
	ColParticipantId string `json:"col_participant_id"` // 参与者编号
	ColParticipantName string `json:"col_participant_name"` // 机构名称
	ColShareholding string `json:"col_shareholding"` // 持股量(股)
	ColShareholdingPercent string `json:"col_shareholding_percent"` // 占已发行股份/权证/单位百分比(%)
}

// CcassHoldDetail 调用 中央结算系统持股明细 API
func CcassHoldDetail(ctx context.Context, client *sdk.Client, req *CcassHoldDetailRequest) ([]CcassHoldDetailItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.HkCode != "" {
		params["hk_code"] = req.HkCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"trade_date", "ts_code", "name", "col_participant_id", "col_participant_name", "col_shareholding", "col_shareholding_percent"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "ccass_hold_detail", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]CcassHoldDetailItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 col_participant_id 的简单类型
		colParticipantId, ok := item["col_participant_id"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 col_participant_id 类型")
		}
		// 处理 col_participant_name 的简单类型
		colParticipantName, ok := item["col_participant_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 col_participant_name 类型")
		}
		// 处理 col_shareholding 的简单类型
		colShareholding, ok := item["col_shareholding"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 col_shareholding 类型")
		}
		// 处理 col_shareholding_percent 的简单类型
		colShareholdingPercent, ok := item["col_shareholding_percent"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 col_shareholding_percent 类型")
		}
		items[i] = CcassHoldDetailItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			Name: name,
			ColParticipantId: colParticipantId,
			ColParticipantName: colParticipantName,
			ColShareholding: colShareholding,
			ColShareholdingPercent: colShareholdingPercent,
		}
	}

	return items, nil
}
