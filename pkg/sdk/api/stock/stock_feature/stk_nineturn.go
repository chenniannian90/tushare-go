// Code generated by tushare-gen. DO NOT EDIT.

package stock_feature

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// StkNineturnRequest 表示 神奇九转指标 API 的请求
type StkNineturnRequest struct {
	TsCode string `json:"ts_code,omitempty"`
	TradeDate string `json:"trade_date,omitempty"`
	Freq string `json:"freq,omitempty"`
	StartDate string `json:"start_date,omitempty"`
	EndDate string `json:"end_date,omitempty"`
}

// StkNineturnItem 表示单个 神奇九转指标 数据项
type StkNineturnItem struct {
	TsCode string `json:"ts_code"`
	TradeDate string `json:"trade_date"`
	Freq string `json:"freq"`
	Open float64 `json:"open"`
	High float64 `json:"high"`
	Low float64 `json:"low"`
	Close float64 `json:"close"`
	Vol float64 `json:"vol"`
	Amount float64 `json:"amount"`
	UpCount float64 `json:"up_count"`
	DownCount float64 `json:"down_count"`
	NineUpTurn string `json:"nine_up_turn"`
	NineDownTurn string `json:"nine_down_turn"`
}

// StkNineturn 调用 神奇九转指标 API
func StkNineturn(ctx context.Context, client *sdk.Client, req *StkNineturnRequest) ([]StkNineturnItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.Freq != "" {
		params["freq"] = req.Freq
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "freq", "open", "high", "low", "close", "vol", "amount", "up_count", "down_count", "nine_up_turn", "nine_down_turn"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "stk_nineturn", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]StkNineturnItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 freq 的简单类型
		freq, ok := item["freq"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 freq 类型")
		}
		// 处理 open 的简单类型
		open, ok := item["open"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 open 类型")
		}
		// 处理 high 的简单类型
		high, ok := item["high"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 high 类型")
		}
		// 处理 low 的简单类型
		low, ok := item["low"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 low 类型")
		}
		// 处理 close 的简单类型
		close, ok := item["close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close 类型")
		}
		// 处理 vol 的简单类型
		vol, ok := item["vol"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 vol 类型")
		}
		// 处理 amount 的简单类型
		amount, ok := item["amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amount 类型")
		}
		// 处理 up_count 的简单类型
		upCount, ok := item["up_count"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 up_count 类型")
		}
		// 处理 down_count 的简单类型
		downCount, ok := item["down_count"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 down_count 类型")
		}
		// 处理 nine_up_turn 的简单类型
		nineUpTurn, ok := item["nine_up_turn"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 nine_up_turn 类型")
		}
		// 处理 nine_down_turn 的简单类型
		nineDownTurn, ok := item["nine_down_turn"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 nine_down_turn 类型")
		}
		items[i] = StkNineturnItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			Freq: freq,
			Open: open,
			High: high,
			Low: low,
			Close: close,
			Vol: vol,
			Amount: amount,
			UpCount: upCount,
			DownCount: downCount,
			NineUpTurn: nineUpTurn,
			NineDownTurn: nineDownTurn,
		}
	}

	return items, nil
}
