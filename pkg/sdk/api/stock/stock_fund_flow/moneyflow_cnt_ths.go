// Code generated by tushare-gen. DO NOT EDIT.

package stock_fund_flow

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// MoneyflowCntThsRequest 表示 板块资金流向（THS) API 的请求
type MoneyflowCntThsRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期(格式：YYYYMMDD，下同)
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// MoneyflowCntThsItem 表示单个 板块资金流向（THS) 数据项
type MoneyflowCntThsItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	TsCode string `json:"ts_code"` // 板块代码
	Name string `json:"name"` // 板块名称
	LeadStock string `json:"lead_stock"` // 领涨股票名称
	ClosePrice float64 `json:"close_price"` // 最新价
	PctChange float64 `json:"pct_change"` // 行业涨跌幅
	IndustryIndex float64 `json:"industry_index"` // 板块指数
	CompanyNum int `json:"company_num"` // 公司数量
	PctChangeStock float64 `json:"pct_change_stock"` // 领涨股涨跌幅
	NetBuyAmount float64 `json:"net_buy_amount"` // 流入资金(亿元)
	NetSellAmount float64 `json:"net_sell_amount"` // 流出资金(亿元)
	NetAmount float64 `json:"net_amount"` // 净额(亿元)
}

// MoneyflowCntThs 调用 板块资金流向（THS) API
func MoneyflowCntThs(ctx context.Context, client *sdk.Client, req *MoneyflowCntThsRequest) ([]MoneyflowCntThsItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"trade_date", "ts_code", "name", "lead_stock", "close_price", "pct_change", "industry_index", "company_num", "pct_change_stock", "net_buy_amount", "net_sell_amount", "net_amount"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "moneyflow_cnt_ths", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]MoneyflowCntThsItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 lead_stock 的简单类型
		leadStock, ok := item["lead_stock"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 lead_stock 类型")
		}
		// 处理 close_price 的简单类型
		closePrice, ok := item["close_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close_price 类型")
		}
		// 处理 pct_change 的简单类型
		pctChange, ok := item["pct_change"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_change 类型")
		}
		// 处理 industry_index 的简单类型
		industryIndex, ok := item["industry_index"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 industry_index 类型")
		}
		// 处理 company_num 的简单类型
		companyNum, ok := item["company_num"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 company_num 类型")
		}
		// 处理 pct_change_stock 的简单类型
		pctChangeStock, ok := item["pct_change_stock"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_change_stock 类型")
		}
		// 处理 net_buy_amount 的简单类型
		netBuyAmount, ok := item["net_buy_amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 net_buy_amount 类型")
		}
		// 处理 net_sell_amount 的简单类型
		netSellAmount, ok := item["net_sell_amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 net_sell_amount 类型")
		}
		// 处理 net_amount 的简单类型
		netAmount, ok := item["net_amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 net_amount 类型")
		}
		items[i] = MoneyflowCntThsItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			Name: name,
			LeadStock: leadStock,
			ClosePrice: closePrice,
			PctChange: pctChange,
			IndustryIndex: industryIndex,
			CompanyNum: companyNum,
			PctChangeStock: pctChangeStock,
			NetBuyAmount: netBuyAmount,
			NetSellAmount: netSellAmount,
			NetAmount: netAmount,
		}
	}

	return items, nil
}
