// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// DcMemberRequest 表示 东方财富概念成分 API 的请求
type DcMemberRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 板块指数代码
	ConCode string `json:"con_code,omitempty"` // 成分股票代码
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（YYYYMMDD格式）
}

// DcMemberItem 表示单个 东方财富概念成分 数据项
type DcMemberItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	TsCode string `json:"ts_code"` // 概念代码
	ConCode string `json:"con_code"` // 成分代码
	Name string `json:"name"` // 成分股名称
}

// DcMember 调用 东方财富概念成分 API
func DcMember(ctx context.Context, client *sdk.Client, req *DcMemberRequest) ([]DcMemberItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.ConCode != "" {
		params["con_code"] = req.ConCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}

	fields := []string{"trade_date", "ts_code", "con_code", "name"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "dc_member", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]DcMemberItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 con_code 的简单类型
		conCode, ok := item["con_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 con_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		items[i] = DcMemberItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			ConCode: conCode,
			Name: name,
		}
	}

	return items, nil
}
