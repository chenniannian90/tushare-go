// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// LimitListThsRequest 表示 同花顺涨跌停榜单 API 的请求
type LimitListThsRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期
	TsCode string `json:"ts_code,omitempty"` // 股票代码
	LimitType string `json:"limit_type,omitempty"` // 涨停池、连扳池、冲刺涨停、炸板池、跌停池，默认：涨停池
	Market string `json:"market,omitempty"` // HS-沪深主板 GEM-创业板 STAR-科创板
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// LimitListThsItem 表示单个 同花顺涨跌停榜单 数据项
type LimitListThsItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	TsCode string `json:"ts_code"` // 股票代码
	Name string `json:"name"` // 股票名称
	Price float64 `json:"price"` // 收盘价(元)
	PctChg float64 `json:"pct_chg"` // 涨跌幅%
	OpenNum int `json:"open_num"` // 打开次数
	LuDesc string `json:"lu_desc"` // 涨停原因
	LimitType string `json:"limit_type"` // 板单类别
	Tag string `json:"tag"` // 涨停标签
	Status string `json:"status"` // 涨停状态（N连板、一字板）
	FirstLuTime string `json:"first_lu_time"` // 首次涨停时间
	LastLuTime string `json:"last_lu_time"` // 最后涨停时间
	FirstLdTime string `json:"first_ld_time"` // 首次跌停时间
	LastLdTime string `json:"last_ld_time"` // 最后跌停时间
	LimitOrder float64 `json:"limit_order"` // 封单量(元
	LimitAmount float64 `json:"limit_amount"` // 封单额(元
	TurnoverRate float64 `json:"turnover_rate"` // 换手率%
	FreeFloat float64 `json:"free_float"` // 实际流通(元
	LuLimitOrder float64 `json:"lu_limit_order"` // 最大封单(元
	LimitUpSucRate float64 `json:"limit_up_suc_rate"` // 近一年涨停封板率
	Turnover float64 `json:"turnover"` // 成交额
	RiseRate float64 `json:"rise_rate"` // 涨速
	SumFloat float64 `json:"sum_float"` // 总市值（亿元）
	MarketType string `json:"market_type"` // 股票类型：HS沪深主板、GEM创业板、STAR科创板
}

// LimitListThs 调用 同花顺涨跌停榜单 API
// 获取同花顺每日涨跌停榜单数据，历史数据从20231101开始提供，增量每天16点左右更新
// 注意：积分要求等使用限制请参考Tushare官方文档
func LimitListThs(ctx context.Context, client *sdk.Client, req *LimitListThsRequest) ([]LimitListThsItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.LimitType != "" {
		params["limit_type"] = req.LimitType
	}
	if req.Market != "" {
		params["market"] = req.Market
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"trade_date", "ts_code", "name", "price", "pct_chg", "open_num", "lu_desc", "limit_type", "tag", "status", "first_lu_time", "last_lu_time", "first_ld_time", "last_ld_time", "limit_order", "limit_amount", "turnover_rate", "free_float", "lu_limit_order", "limit_up_suc_rate", "turnover", "rise_rate", "sum_float", "market_type"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "limit_list_ths", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]LimitListThsItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		// 对 string 类型尝试多种转换
		var name string
		if v, ok := item["name"].(string); ok {
			name = v
		} else if v, ok := item["name"].(float64); ok {
			name = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["name"].(int); ok {
			name = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["name"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: name")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["name"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 price 的简单类型
		price, ok := item["price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 price 类型")
		}
		// 处理 pct_chg 的简单类型
		pctChg, ok := item["pct_chg"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_chg 类型")
		}
		// 处理 open_num 的简单类型
		openNum, ok := item["open_num"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 open_num 类型")
		}
		// 处理 lu_desc 的简单类型
		// 对 string 类型尝试多种转换
		var luDesc string
		if v, ok := item["lu_desc"].(string); ok {
			luDesc = v
		} else if v, ok := item["lu_desc"].(float64); ok {
			luDesc = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["lu_desc"].(int); ok {
			luDesc = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["lu_desc"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: lu_desc")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["lu_desc"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 lu_desc 类型")
		}
		// 处理 limit_type 的简单类型
		// 对 string 类型尝试多种转换
		var limitType string
		if v, ok := item["limit_type"].(string); ok {
			limitType = v
		} else if v, ok := item["limit_type"].(float64); ok {
			limitType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["limit_type"].(int); ok {
			limitType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["limit_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: limit_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["limit_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 limit_type 类型")
		}
		// 处理 tag 的简单类型
		// 对 string 类型尝试多种转换
		var tag string
		if v, ok := item["tag"].(string); ok {
			tag = v
		} else if v, ok := item["tag"].(float64); ok {
			tag = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["tag"].(int); ok {
			tag = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["tag"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: tag")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["tag"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 tag 类型")
		}
		// 处理 status 的简单类型
		// 对 string 类型尝试多种转换
		var status string
		if v, ok := item["status"].(string); ok {
			status = v
		} else if v, ok := item["status"].(float64); ok {
			status = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["status"].(int); ok {
			status = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["status"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: status")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["status"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 status 类型")
		}
		// 处理 first_lu_time 的简单类型
		// 对 string 类型尝试多种转换
		var firstLuTime string
		if v, ok := item["first_lu_time"].(string); ok {
			firstLuTime = v
		} else if v, ok := item["first_lu_time"].(float64); ok {
			firstLuTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["first_lu_time"].(int); ok {
			firstLuTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["first_lu_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: first_lu_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["first_lu_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 first_lu_time 类型")
		}
		// 处理 last_lu_time 的简单类型
		// 对 string 类型尝试多种转换
		var lastLuTime string
		if v, ok := item["last_lu_time"].(string); ok {
			lastLuTime = v
		} else if v, ok := item["last_lu_time"].(float64); ok {
			lastLuTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["last_lu_time"].(int); ok {
			lastLuTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["last_lu_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: last_lu_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["last_lu_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 last_lu_time 类型")
		}
		// 处理 first_ld_time 的简单类型
		// 对 string 类型尝试多种转换
		var firstLdTime string
		if v, ok := item["first_ld_time"].(string); ok {
			firstLdTime = v
		} else if v, ok := item["first_ld_time"].(float64); ok {
			firstLdTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["first_ld_time"].(int); ok {
			firstLdTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["first_ld_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: first_ld_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["first_ld_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 first_ld_time 类型")
		}
		// 处理 last_ld_time 的简单类型
		// 对 string 类型尝试多种转换
		var lastLdTime string
		if v, ok := item["last_ld_time"].(string); ok {
			lastLdTime = v
		} else if v, ok := item["last_ld_time"].(float64); ok {
			lastLdTime = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["last_ld_time"].(int); ok {
			lastLdTime = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["last_ld_time"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: last_ld_time")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["last_ld_time"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 last_ld_time 类型")
		}
		// 处理 limit_order 的简单类型
		limitOrder, ok := item["limit_order"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 limit_order 类型")
		}
		// 处理 limit_amount 的简单类型
		limitAmount, ok := item["limit_amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 limit_amount 类型")
		}
		// 处理 turnover_rate 的简单类型
		turnoverRate, ok := item["turnover_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 turnover_rate 类型")
		}
		// 处理 free_float 的简单类型
		freeFloat, ok := item["free_float"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 free_float 类型")
		}
		// 处理 lu_limit_order 的简单类型
		luLimitOrder, ok := item["lu_limit_order"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 lu_limit_order 类型")
		}
		// 处理 limit_up_suc_rate 的简单类型
		limitUpSucRate, ok := item["limit_up_suc_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 limit_up_suc_rate 类型")
		}
		// 处理 turnover 的简单类型
		turnover, ok := item["turnover"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 turnover 类型")
		}
		// 处理 rise_rate 的简单类型
		riseRate, ok := item["rise_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 rise_rate 类型")
		}
		// 处理 sum_float 的简单类型
		sumFloat, ok := item["sum_float"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 sum_float 类型")
		}
		// 处理 market_type 的简单类型
		// 对 string 类型尝试多种转换
		var marketType string
		if v, ok := item["market_type"].(string); ok {
			marketType = v
		} else if v, ok := item["market_type"].(float64); ok {
			marketType = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["market_type"].(int); ok {
			marketType = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["market_type"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: limit_list_ths")
			log.Printf("字段: market_type")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["market_type"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 market_type 类型")
		}
		items[i] = LimitListThsItem{
			TradeDate: tradeDate,
			TsCode: tsCode,
			Name: name,
			Price: price,
			PctChg: pctChg,
			OpenNum: openNum,
			LuDesc: luDesc,
			LimitType: limitType,
			Tag: tag,
			Status: status,
			FirstLuTime: firstLuTime,
			LastLuTime: lastLuTime,
			FirstLdTime: firstLdTime,
			LastLdTime: lastLdTime,
			LimitOrder: limitOrder,
			LimitAmount: limitAmount,
			TurnoverRate: turnoverRate,
			FreeFloat: freeFloat,
			LuLimitOrder: luLimitOrder,
			LimitUpSucRate: limitUpSucRate,
			Turnover: turnover,
			RiseRate: riseRate,
			SumFloat: sumFloat,
			MarketType: marketType,
		}
	}

	return items, nil
}
