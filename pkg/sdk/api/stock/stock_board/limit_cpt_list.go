// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// LimitCptListRequest 表示 涨停最强板块统计 API 的请求
type LimitCptListRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期（格式：YYYYMMDD，下同）
	TsCode string `json:"ts_code,omitempty"` // 板块代码
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// LimitCptListItem 表示单个 涨停最强板块统计 数据项
type LimitCptListItem struct {
	TsCode string `json:"ts_code"` // 板块代码
	Name string `json:"name"` // 板块名称
	TradeDate string `json:"trade_date"` // 交易日期
	Days int `json:"days"` // 上榜天数
	UpStat string `json:"up_stat"` // 连板高度
	ConsNums int `json:"cons_nums"` // 连板家数
	UpNums int `json:"up_nums"` // 涨停家数
	PctChg float64 `json:"pct_chg"` // 涨跌幅%
	Rank string `json:"rank"` // 板块热点排名
}

// LimitCptList 调用 涨停最强板块统计 API
func LimitCptList(ctx context.Context, client *sdk.Client, req *LimitCptListRequest) ([]LimitCptListItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "name", "trade_date", "days", "up_stat", "cons_nums", "up_nums", "pct_chg", "rank"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "limit_cpt_list", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]LimitCptListItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 days 的简单类型
		days, ok := item["days"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 days 类型")
		}
		// 处理 up_stat 的简单类型
		upStat, ok := item["up_stat"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 up_stat 类型")
		}
		// 处理 cons_nums 的简单类型
		consNums, ok := item["cons_nums"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 cons_nums 类型")
		}
		// 处理 up_nums 的简单类型
		upNums, ok := item["up_nums"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 up_nums 类型")
		}
		// 处理 pct_chg 的简单类型
		pctChg, ok := item["pct_chg"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_chg 类型")
		}
		// 处理 rank 的简单类型
		rank, ok := item["rank"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 rank 类型")
		}
		items[i] = LimitCptListItem{
			TsCode: tsCode,
			Name: name,
			TradeDate: tradeDate,
			Days: days,
			UpStat: upStat,
			ConsNums: consNums,
			UpNums: upNums,
			PctChg: pctChg,
			Rank: rank,
		}
	}

	return items, nil
}
