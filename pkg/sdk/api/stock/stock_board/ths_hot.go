// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"fmt"

	"tushare-go/pkg/sdk"
)

// ThsHotRequest 表示 同花顺App热榜数 API 的请求
type ThsHotRequest struct {
	TradeDate string `json:"trade_date,omitempty"` // 交易日期
	TsCode string `json:"ts_code,omitempty"` // TS代码
	Market string `json:"market,omitempty"` // 热榜类型(热股、ETF、可转债、行业板块、概念板块、期货、港股、热基、美股)
	IsNew string `json:"is_new,omitempty"` // 是否最新（默认Y，如果为N则为盘中和盘后阶段采集，具体时间可参考rank_time字段，状态N每小时更新一次，状态Y更新时间为22：30）
}

// ThsHotItem 表示单个 同花顺App热榜数 数据项
type ThsHotItem struct {
	TradeDate string `json:"trade_date"` // 交易日期
	DataType string `json:"data_type"` // 数据类型
	TsCode string `json:"ts_code"` // 股票代码
	TsName string `json:"ts_name"` // 股票名称
	Rank int `json:"rank"` // 排行
	PctChange float64 `json:"pct_change"` // 涨跌幅%
	CurrentPrice float64 `json:"current_price"` // 当前价格
	Concept string `json:"concept"` // 标签
	RankReason string `json:"rank_reason"` // 上榜解读
	Hot float64 `json:"hot"` // 热度值
	RankTime string `json:"rank_time"` // 排行榜获取时间
}

// ThsHot 调用 同花顺App热榜数 API
// 获取同花顺App热榜数据，包括热股、概念板块、ETF、可转债、港美股等等，每日盘中提取4次，收盘后4次，最晚22点提取一次。
// 注意：积分要求等使用限制请参考Tushare官方文档
func ThsHot(ctx context.Context, client *sdk.Client, req *ThsHotRequest) ([]ThsHotItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.Market != "" {
		params["market"] = req.Market
	}
	if req.IsNew != "" {
		params["is_new"] = req.IsNew
	}

	fields := []string{"trade_date", "data_type", "ts_code", "ts_name", "rank", "pct_change", "current_price", "concept", "rank_reason", "hot", "rank_time"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "ths_hot", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]ThsHotItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 data_type 的简单类型
		dataType, ok := item["data_type"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 data_type 类型")
		}
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 ts_name 的简单类型
		tsName, ok := item["ts_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_name 类型")
		}
		// 处理 rank 的简单类型
		rank, ok := item["rank"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 rank 类型")
		}
		// 处理 pct_change 的简单类型
		pctChange, ok := item["pct_change"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_change 类型")
		}
		// 处理 current_price 的简单类型
		currentPrice, ok := item["current_price"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 current_price 类型")
		}
		// 处理 concept 的简单类型
		concept, ok := item["concept"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 concept 类型")
		}
		// 处理 rank_reason 的简单类型
		rankReason, ok := item["rank_reason"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 rank_reason 类型")
		}
		// 处理 hot 的简单类型
		hot, ok := item["hot"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 hot 类型")
		}
		// 处理 rank_time 的简单类型
		rankTime, ok := item["rank_time"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 rank_time 类型")
		}
		items[i] = ThsHotItem{
			TradeDate: tradeDate,
			DataType: dataType,
			TsCode: tsCode,
			TsName: tsName,
			Rank: rank,
			PctChange: pctChange,
			CurrentPrice: currentPrice,
			Concept: concept,
			RankReason: rankReason,
			Hot: hot,
			RankTime: rankTime,
		}
	}

	return items, nil
}
