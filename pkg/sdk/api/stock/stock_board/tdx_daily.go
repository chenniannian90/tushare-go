// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"tushare-go/pkg/sdk"
)

// TdxDailyRequest 表示 通达信板块行情 API 的请求
type TdxDailyRequest struct {
	TsCode string `json:"ts_code,omitempty"` // 板块代码：xxxxxx.TDX
	TradeDate string `json:"trade_date,omitempty"` // 交易日期，格式YYYYMMDD,下同
	StartDate string `json:"start_date,omitempty"` // 开始日期
	EndDate string `json:"end_date,omitempty"` // 结束日期
}

// TdxDailyItem 表示单个 通达信板块行情 数据项
type TdxDailyItem struct {
	TsCode string `json:"ts_code"` // 板块代码
	TradeDate string `json:"trade_date"` // 交易日期
	Close float64 `json:"close"` // 收盘点位
	Open float64 `json:"open"` // 开盘点位
	High float64 `json:"high"` // 最高点位
	Low float64 `json:"low"` // 最低点位
	PreClose float64 `json:"pre_close"` // 昨日收盘点
	Change float64 `json:"change"` // 涨跌点位
	PctChange float64 `json:"pct_change"` // 涨跌幅%
	Vol float64 `json:"vol"` // 成交量（手）
	Amount float64 `json:"amount"` // 成交额（万元）, 对于期货指数，该字段存储持仓量
	Rise string `json:"rise"` // 收盘涨速%
	VolRatio float64 `json:"vol_ratio"` // 量比
	TurnoverRate float64 `json:"turnover_rate"` // 换手%
	Swing float64 `json:"swing"` // 振幅%
	UpNum int `json:"up_num"` // 上涨家数
	DownNum int `json:"down_num"` // 下跌家数
	LimitUpNum int `json:"limit_up_num"` // 涨停家数
	LimitDownNum int `json:"limit_down_num"` // 跌停家数
	LuDays int `json:"lu_days"` // 连涨天数
	Field3day float64 `json:"3day"` // 3日涨幅%
	Field5day float64 `json:"5day"` // 5日涨幅%
	Field10day float64 `json:"10day"` // 10日涨幅%
	Field20day float64 `json:"20day"` // 20日涨幅%
	Field60day float64 `json:"60day"` // 60日涨幅%
	Mtd float64 `json:"mtd"` // 月初至今%
	Ytd float64 `json:"ytd"` // 年初至今%
	Field1year float64 `json:"1year"` // 一年涨幅%
	Pe string `json:"pe"` // 市盈率
	Pb string `json:"pb"` // 市净率
	FloatMv float64 `json:"float_mv"` // 流通市值(亿)
	AbTotalMv float64 `json:"ab_total_mv"` // AB股总市值（亿）
	FloatShare float64 `json:"float_share"` // 流通股(亿)
	TotalShare float64 `json:"total_share"` // 总股本(亿)
	BmBuyNet float64 `json:"bm_buy_net"` // 主买净额(元)
	BmBuyRatio float64 `json:"bm_buy_ratio"` // 主买占比%
	BmNet float64 `json:"bm_net"` // 主力净额
	BmRatio float64 `json:"bm_ratio"` // 主力占比%
}

// TdxDaily 调用 通达信板块行情 API
// 获取通达信各板块行情，包括成交和估值等数据
// 注意：积分要求等使用限制请参考Tushare官方文档
func TdxDaily(ctx context.Context, client *sdk.Client, req *TdxDailyRequest) ([]TdxDailyItem, error) {
	params := map[string]interface{}{}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.StartDate != "" {
		params["start_date"] = req.StartDate
	}
	if req.EndDate != "" {
		params["end_date"] = req.EndDate
	}

	fields := []string{"ts_code", "trade_date", "close", "open", "high", "low", "pre_close", "change", "pct_change", "vol", "amount", "rise", "vol_ratio", "turnover_rate", "swing", "up_num", "down_num", "limit_up_num", "limit_down_num", "lu_days", "3day", "5day", "10day", "20day", "60day", "mtd", "ytd", "1year", "pe", "pb", "float_mv", "ab_total_mv", "float_share", "total_share", "bm_buy_net", "bm_buy_ratio", "bm_net", "bm_ratio"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPIFlexible(ctx, "tdx_daily", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]TdxDailyItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		// 对 string 类型尝试多种转换
		var tsCode string
		if v, ok := item["ts_code"].(string); ok {
			tsCode = v
		} else if v, ok := item["ts_code"].(float64); ok {
			tsCode = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["ts_code"].(int); ok {
			tsCode = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["ts_code"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: tdx_daily")
			log.Printf("字段: ts_code")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["ts_code"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 trade_date 的简单类型
		// 对 string 类型尝试多种转换
		var tradeDate string
		if v, ok := item["trade_date"].(string); ok {
			tradeDate = v
		} else if v, ok := item["trade_date"].(float64); ok {
			tradeDate = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["trade_date"].(int); ok {
			tradeDate = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["trade_date"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: tdx_daily")
			log.Printf("字段: trade_date")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["trade_date"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 close 的简单类型
		close, ok := item["close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 close 类型")
		}
		// 处理 open 的简单类型
		open, ok := item["open"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 open 类型")
		}
		// 处理 high 的简单类型
		high, ok := item["high"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 high 类型")
		}
		// 处理 low 的简单类型
		low, ok := item["low"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 low 类型")
		}
		// 处理 pre_close 的简单类型
		preClose, ok := item["pre_close"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pre_close 类型")
		}
		// 处理 change 的简单类型
		change, ok := item["change"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 change 类型")
		}
		// 处理 pct_change 的简单类型
		pctChange, ok := item["pct_change"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 pct_change 类型")
		}
		// 处理 vol 的简单类型
		vol, ok := item["vol"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 vol 类型")
		}
		// 处理 amount 的简单类型
		amount, ok := item["amount"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 amount 类型")
		}
		// 处理 rise 的简单类型
		// 对 string 类型尝试多种转换
		var rise string
		if v, ok := item["rise"].(string); ok {
			rise = v
		} else if v, ok := item["rise"].(float64); ok {
			rise = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["rise"].(int); ok {
			rise = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["rise"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: tdx_daily")
			log.Printf("字段: rise")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["rise"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 rise 类型")
		}
		// 处理 vol_ratio 的简单类型
		volRatio, ok := item["vol_ratio"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 vol_ratio 类型")
		}
		// 处理 turnover_rate 的简单类型
		turnoverRate, ok := item["turnover_rate"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 turnover_rate 类型")
		}
		// 处理 swing 的简单类型
		swing, ok := item["swing"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 swing 类型")
		}
		// 处理 up_num 的简单类型
		upNum, ok := item["up_num"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 up_num 类型")
		}
		// 处理 down_num 的简单类型
		downNum, ok := item["down_num"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 down_num 类型")
		}
		// 处理 limit_up_num 的简单类型
		limitUpNum, ok := item["limit_up_num"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 limit_up_num 类型")
		}
		// 处理 limit_down_num 的简单类型
		limitDownNum, ok := item["limit_down_num"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 limit_down_num 类型")
		}
		// 处理 lu_days 的简单类型
		luDays, ok := item["lu_days"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 lu_days 类型")
		}
		// 处理 3day 的简单类型
		field3day, ok := item["3day"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 3day 类型")
		}
		// 处理 5day 的简单类型
		field5day, ok := item["5day"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 5day 类型")
		}
		// 处理 10day 的简单类型
		field10day, ok := item["10day"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 10day 类型")
		}
		// 处理 20day 的简单类型
		field20day, ok := item["20day"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 20day 类型")
		}
		// 处理 60day 的简单类型
		field60day, ok := item["60day"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 60day 类型")
		}
		// 处理 mtd 的简单类型
		mtd, ok := item["mtd"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 mtd 类型")
		}
		// 处理 ytd 的简单类型
		ytd, ok := item["ytd"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ytd 类型")
		}
		// 处理 1year 的简单类型
		field1year, ok := item["1year"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 1year 类型")
		}
		// 处理 pe 的简单类型
		// 对 string 类型尝试多种转换
		var pe string
		if v, ok := item["pe"].(string); ok {
			pe = v
		} else if v, ok := item["pe"].(float64); ok {
			pe = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["pe"].(int); ok {
			pe = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pe"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: tdx_daily")
			log.Printf("字段: pe")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pe"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pe 类型")
		}
		// 处理 pb 的简单类型
		// 对 string 类型尝试多种转换
		var pb string
		if v, ok := item["pb"].(string); ok {
			pb = v
		} else if v, ok := item["pb"].(float64); ok {
			pb = fmt.Sprintf("%.0f", v)
		} else if v, ok := item["pb"].(int); ok {
			pb = fmt.Sprintf("%d", v)
		} else {
			itemJSON, _ := json.Marshal(item)
			fieldJSON, _ := json.Marshal(item["pb"])
			log.Printf("=== 字段解析失败 ===")
			log.Printf("API: tdx_daily")
			log.Printf("字段: pb")
			log.Printf("错误: 类型转换失败，期望类型 string，支持 string/float64/int")
			log.Printf("字段原始值: %s", string(fieldJSON))
			log.Printf("字段实际类型: %T", item["pb"])
			log.Printf("当前Item: %s", string(itemJSON))
			log.Printf("===================")
			return nil, fmt.Errorf("无效的 pb 类型")
		}
		// 处理 float_mv 的简单类型
		floatMv, ok := item["float_mv"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 float_mv 类型")
		}
		// 处理 ab_total_mv 的简单类型
		abTotalMv, ok := item["ab_total_mv"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 ab_total_mv 类型")
		}
		// 处理 float_share 的简单类型
		floatShare, ok := item["float_share"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 float_share 类型")
		}
		// 处理 total_share 的简单类型
		totalShare, ok := item["total_share"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 total_share 类型")
		}
		// 处理 bm_buy_net 的简单类型
		bmBuyNet, ok := item["bm_buy_net"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bm_buy_net 类型")
		}
		// 处理 bm_buy_ratio 的简单类型
		bmBuyRatio, ok := item["bm_buy_ratio"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bm_buy_ratio 类型")
		}
		// 处理 bm_net 的简单类型
		bmNet, ok := item["bm_net"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bm_net 类型")
		}
		// 处理 bm_ratio 的简单类型
		bmRatio, ok := item["bm_ratio"].(float64)
		if !ok {
			return nil, fmt.Errorf("无效的 bm_ratio 类型")
		}
		items[i] = TdxDailyItem{
			TsCode: tsCode,
			TradeDate: tradeDate,
			Close: close,
			Open: open,
			High: high,
			Low: low,
			PreClose: preClose,
			Change: change,
			PctChange: pctChange,
			Vol: vol,
			Amount: amount,
			Rise: rise,
			VolRatio: volRatio,
			TurnoverRate: turnoverRate,
			Swing: swing,
			UpNum: upNum,
			DownNum: downNum,
			LimitUpNum: limitUpNum,
			LimitDownNum: limitDownNum,
			LuDays: luDays,
			Field3day: field3day,
			Field5day: field5day,
			Field10day: field10day,
			Field20day: field20day,
			Field60day: field60day,
			Mtd: mtd,
			Ytd: ytd,
			Field1year: field1year,
			Pe: pe,
			Pb: pb,
			FloatMv: floatMv,
			AbTotalMv: abTotalMv,
			FloatShare: floatShare,
			TotalShare: totalShare,
			BmBuyNet: bmBuyNet,
			BmBuyRatio: bmBuyRatio,
			BmNet: bmNet,
			BmRatio: bmRatio,
		}
	}

	return items, nil
}
