// Code generated by tushare-gen. DO NOT EDIT.

package stock_board

import (
	"context"
	"fmt"

	"github.com/chenniannian90/tushare-go/pkg/sdk"
)

// KplConceptConsRequest 表示 题材成分（开盘啦） API 的请求
type KplConceptConsRequest struct {
	TradeDate string `json:"trade_date,omitempty"`
	TsCode string `json:"ts_code,omitempty"`
	ConCode string `json:"con_code,omitempty"`
}

// KplConceptConsItem 表示单个 题材成分（开盘啦） 数据项
type KplConceptConsItem struct {
	TsCode string `json:"ts_code"`
	Name string `json:"name"`
	ConName string `json:"con_name"`
	ConCode string `json:"con_code"`
	TradeDate string `json:"trade_date"`
	Desc string `json:"desc"`
	HotNum int `json:"hot_num"`
}

// KplConceptCons 调用 题材成分（开盘啦） API
func KplConceptCons(ctx context.Context, client *sdk.Client, req *KplConceptConsRequest) ([]KplConceptConsItem, error) {
	params := map[string]interface{}{}
	if req.TradeDate != "" {
		params["trade_date"] = req.TradeDate
	}
	if req.TsCode != "" {
		params["ts_code"] = req.TsCode
	}
	if req.ConCode != "" {
		params["con_code"] = req.ConCode
	}

	fields := []string{"ts_code", "name", "con_name", "con_code", "trade_date", "desc", "hot_num"}

	var result struct {
		Fields []string                 `json:"fields"`
		Items  []map[string]interface{} `json:"items"`
	}

	if err := client.CallAPI(ctx, "kpl_concept_cons", params, fields, &result); err != nil {
		return nil, err
	}
	items := make([]KplConceptConsItem, len(result.Items))
	for i, item := range result.Items {
		// 处理 ts_code 的简单类型
		tsCode, ok := item["ts_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 ts_code 类型")
		}
		// 处理 name 的简单类型
		name, ok := item["name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 name 类型")
		}
		// 处理 con_name 的简单类型
		conName, ok := item["con_name"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 con_name 类型")
		}
		// 处理 con_code 的简单类型
		conCode, ok := item["con_code"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 con_code 类型")
		}
		// 处理 trade_date 的简单类型
		tradeDate, ok := item["trade_date"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 trade_date 类型")
		}
		// 处理 desc 的简单类型
		desc, ok := item["desc"].(string)
		if !ok {
			return nil, fmt.Errorf("无效的 desc 类型")
		}
		// 处理 hot_num 的简单类型
		hotNum, ok := item["hot_num"].(int)
		if !ok {
			return nil, fmt.Errorf("无效的 hot_num 类型")
		}
		items[i] = KplConceptConsItem{
			TsCode: tsCode,
			Name: name,
			ConName: conName,
			ConCode: conCode,
			TradeDate: tradeDate,
			Desc: desc,
			HotNum: hotNum,
		}
	}

	return items, nil
}
